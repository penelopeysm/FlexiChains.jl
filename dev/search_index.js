var documenterSearchIndex = {"docs":
[{"category":"section","location":"turing/#Usage-with-Turing.jl","page":"Usage with Turing.jl","text":"This page provides a fairly high-level overview of how to use FlexiChains with Turing.jl.","title":"Usage with Turing.jl"},{"category":"section","location":"turing/#Sampling","page":"Usage with Turing.jl","text":"Since Turing.jl v0.45, FlexiChains is the default chain type returned by MCMC sampling.\n\nnote: Not on Turing v0.45 yet?\nTo obtain a FlexiChain with older versions of Turing.jl, you can specify the chain_type keyword argument when calling sample:using FlexiChains\nsample(model, sampler, N; chain_type=VNChain)\n\nLet's use a non-trivial model so that we can illustrate some features of FlexiChains.\n\nusing Turing, FlexiChains\n\ny = [28, 8, -3, 7, -1, 1, 18, 12]\nsigma = [15, 10, 16, 11, 9, 11, 10, 18]\n@model function eight_schools(y, sigma)\n    mu ~ Normal(0, 5)\n    tau ~ truncated(Cauchy(0, 5); lower=0)\n    theta ~ MvNormal(fill(mu, length(y)), tau^2 * I)\n    for i in eachindex(y)\n        y[i] ~ Normal(theta[i], sigma[i])\n    end\n    return (mu=mu, tau=tau)\nend\nmodel = eight_schools(y, sigma)\nchain = sample(model, NUTS(), 5; chain_type=VNChain)\n\nnote: Note\nWe only run 5 MCMC iterations here to keep the output in the following sections small.","title":"Sampling"},{"category":"section","location":"turing/#Key-types","page":"Usage with Turing.jl","text":"First, notice in the printout above that a FlexiChain stores 'parameters' and 'extra keys' separately. Parameters correspond to random variables of the model you sampled from, whereas other keys are extra data associated with the samples drawn (for example, the log-joint probability of each sample).\n\nIn FlexiChains, these are wrapped in the FlexiChains.Parameter and FlexiChains.Extra types respectively. Thus, the parameter mu is really stored as Parameter(@varname(mu)), and the log-joint probability is Extra(:logjoint).\n\nFor a FlexiChain{T}, all Parameter keys must wrap objects that subtype T. Extras on the other hand can wrap anything.\n\nVNChain is an alias for FlexiChain{VarName}, i.e., the parameters are stored as VarNames, which is the natural output for Turing.jl.\n\nnote: SymChain\nFor other modelling packages it may sometimes be more natural to store parameters as Symbols instead of VarNames. In that case, you can use SymChain (an alias for FlexiChain{Symbol}) instead of VNChain.","title":"Key types"},{"category":"section","location":"turing/#Accessing-data","page":"Usage with Turing.jl","text":"FlexiChains provides multiple different ways to access the data for a given key.","title":"Accessing data"},{"category":"section","location":"turing/#Parameters","page":"Usage with Turing.jl","text":"To access parameters, the recommended way is to use VarNames to index into the chain. VarName is a data structure defined in AbstractPPL.jl, and is what Turing.jl uses to represent the name of a random variable (appearing on the left-hand side of a tilde-statement).\n\nVarNames are most easily constructed by applying the @varname macro to the name of the variable that you want to access. For example, this directly gives us the value of mu in each iteration as a plain old vector of floats.\n\nchain[@varname(mu)]\n\nnote: Wrapping in Parameter and Extra\nWhen looking up a parameter, you do not need to wrap the VarName in FlexiChains.Parameter(...): this will be automatically done for you. Extra keys always need to be wrapped (but see also the shortcut for Symbol-based indexing below).\n\nnote: DimMatrix\nIndexing into a FlexiChain returns a DimensionalData.DimMatrix. This behaves exactly like a regular Matrix, but additionally carries extra information about its dimensions.This allows you to keep track of what each dimension means, and also allows for more advanced indexing operations, which are described in the 'indexing' page.\n\nFor vector-valued parameters like theta, this works in exactly the same way, except that you get a DimMatrix of vectors.\n\nchain[@varname(theta)]\n\nThis is probably the biggest difference between FlexiChains and MCMCChains. MCMCChains by default will break vector-valued parameters into multiple scalar-valued parameters called theta[1], theta[2], etc., whereas FlexiChains keeps them together as they were defined in the model.\n\ntip: If you want a 3D array...\nIf you want to access theta as a 3D array of shape (num_iterations, num_chains, length(theta)), you can also pass the keyword argument stack=true to getindex:chain[@varname(theta), stack=true]\n\nIf you want to obtain only the first element of theta, you don't need to manipulate the DimMatrix. You can just index into the chain with the corresponding VarName:\n\nchain[@varname(theta[1])]\n\nIn this way, you can 'break down', or access nested fields of, larger parameters. That is, if your model has x ~ dist, FlexiChains will let you access some field or index of x.\n\nnote: Heterogeneous data\nIf some samples of x have one element and others have two elements, attempting to access x[2] will return an array with missing values for the samples where x only has one element.\n\nYou can also use keyword arguments when indexing to specify which chains or iterations you are interested in. Note that when using square brackets to index, keyword arguments must be separated from positional arguments by a comma, not a semicolon!\n\nchain[@varname(mu), iter=2:4, chain=1]\n\nYou can also use selectors from DimensionalData.jl to specify which iterations or chains you want.\n\nchain[@varname(mu), iter=Not(At(7)), chain=At(1)]\n\nThe indexing behaviour of FlexiChains is described fully on the Indexing page.","title":"Parameters"},{"category":"section","location":"turing/#Other-keys","page":"Usage with Turing.jl","text":"In general Turing.jl tries to package up some extra metadata into the chain that may be helpful. For example, the log-joint probability of each sample is stored with the key :logjoint. To access non-parameter information like this in an unambiguous fashion, you should use the Extra wrapper.\n\nusing FlexiChains: Extra\n\nchain[Extra(:logjoint)]\n\nwarning: Warning\nIn older versions of Turing.jl, MCMCChains would store the log-joint probability as :lp. FlexiChains uses :logjoint instead, which is clearer. Since Turing v0.42, both MCMCChains and FlexiChains use :logjoint.\n\nIf there is no ambiguity in the symbol :logjoint, then you can use a shortcut which is described in the next section.","title":"Other keys"},{"category":"section","location":"turing/#symbol-indexing","page":"Usage with Turing.jl","text":"If you are used to MCMCChains.jl, you may find this more cumbersome than before. So, FlexiChains provides some shortcuts for accessing data. You can index into a FlexiChain with a single Symbol, and as long as it is unambiguous, it will return the corresponding data.\n\nchain[:mu] # parameter\n\nnote: What does unambiguous mean?\nIn this case, because the only key k for which Symbol(k.name) == :mu is Parameter(@varname(mu)), we can safely identify Parameter(@varname(mu)) as the key that we want. If this chain also had an extra key called Extra(:mu), then this would be ambiguous, and FlexiChains would throw an error.\n\nnote: No sub-varnames\nYou cannot use chain[Symbol(\"theta[1]\")] as a replacement for chain[@varname(theta[1])].\n\nLikewise, we can omit wrapping :logjoint in Extra(...):\n\nchain[:logjoint] # other key","title":"Indexing by Symbol: a shortcut"},{"category":"section","location":"turing/#Prefix-agnostic-indexing","page":"Usage with Turing.jl","text":"If you want to access a variable that has been prefixed (e.g. because it is part of a submodel) but you don't want to specify the full prefix, you can use FlexiChains.Prefixed:\n\nusing Turing\nusing FlexiChains: Prefixed, VNChain\n\n@model inner() = x ~ MvNormal(zeros(2), I)\n\n@model function outer()\n    a ~ to_submodel(inner())\n    return nothing\nend\n\npfx_chain = sample(outer(), MH(), 5; chain_type=VNChain)\n\n# Inside the chain, the actual key is `@varname(a.x)`;\n# this will pick out that key.\npfx_chain[Prefixed(@varname(x))]\n\nSub-VarNames are also supported:\n\npfx_chain[Prefixed(@varname(x[1]))]","title":"Prefix-agnostic indexing"},{"category":"section","location":"turing/#Summary-statistics","page":"Usage with Turing.jl","text":"","title":"Summary statistics"},{"category":"section","location":"turing/#Overall-summaries","page":"Usage with Turing.jl","text":"For a very quick summary of the chain, you can use StatsBase.summarystats (which FlexiChains reexports):\n\nusing FlexiChains: summarystats\n\nsummarystats(chain)\n\nnote: Note\nThe large number of NaN's and Inf's here are just because of the very short chain length. On a real chain you would get proper statistics.\n\nTo index into this, you can use a similar syntax as for FlexiChains:\n\nss = summarystats(chain)\nss[@varname(mu)]\n\nor to access an individual statistic\n\nss[@varname(mu), stat=At(:mean)]\n\nnote: Note\nNote that the At selector from DimensionalData.jl is needed here to specify the name of the statistic we're interested in.\n\nAlternatively, you can directly convert the FlexiSummary into an array and manipulate it as you would any other array. (Note that Array(ss) also works, but you lose the dimension information in that case.)\n\nDimArray(ss)\n\nBy default, summarystats will split VarNames up. This is done because summary statistics often only make sense for scalar-valued parameters. If you want to avoid this, you can set split_varnames=false:\n\nsummarystats(chain; split_varnames=false)\n\nNotice how many of the statistics for theta are now missing. This is because statistics like the quantiles cannot be meaningfully calculated for vector-valued parameters.","title":"Overall summaries"},{"category":"section","location":"turing/#Individual-summaries","page":"Usage with Turing.jl","text":"You can obtain, for example, the mean of each key in the chain using Statistics.mean. This returns a FlexiSummary object:\n\nusing Statistics: mean\n\nmn = mean(chain)\n\nYou can index into a FlexiSummary in exactly the same ways as a FlexiChain.\n\nmn[@varname(mu)]\n\nOut of the box, FlexiChains provides many commonly used summary functions, such as Statistics.mean and Statistics.std (a full list is given in the Summarising page). These functions can all be applied to a FlexiChain with their usual signatures (for example, Statistics.quantile will require a second argument). Keyword arguments of the original functions are also supported, for example MCMCDiagnosticTools.ess(chain; kind=:tail) returns the tail ESS.\n\nnote: Other summary functions\nIf you want to apply a summary function that isn't listed above, you can manually use FlexiChains.collapse. If it is something that is worth appearing in FlexiChains proper, please do open an issue!","title":"Individual summaries"},{"category":"section","location":"turing/#Collapsed-dimensions","page":"Usage with Turing.jl","text":"By default, applying summary functions will collapse the data in both the iteration and chain dimensions (the latter is only relevant if multiple chains are present).\n\nTo only collapse over one dimension you can use\n\nmean(chain; dims=:iter)[@varname(mu)]\n\nor dims=:chain (although that is probably less useful).","title":"Collapsed dimensions"},{"category":"section","location":"turing/#Saving-and-resuming-MCMC-sampling-progress","page":"Usage with Turing.jl","text":"If you want to sample a fewer number of iterations first and then resume it later, you can use the following:\n\nchn1 = sample(model, NUTS(), 10;\n    chain_type=VNChain, save_state=true\n)\nchn2 = sample(model, NUTS(), 10;\n    chain_type=VNChain, initial_state=only(FlexiChains.last_sampler_state(chn1))\n)\n\nThe chains can be combined using vcat:\n\ncombined_chn = vcat(chn1, chn2)\n\nnote: The `initial_state` argument\nWhen performing single-chain sampling with sample(model, spl, N; initial_state=state), initial_state should be either nothing (to start a new chain) or the state to resume from. For multiple-chain sampling with sample(model, spl, MCMCThreads(), N, C), initial_state should be a vector of length C, where initial_state[i] is the state to resume the i-th chain from (or nothing to start a new chain).To obtain the saved final state of a chain, you can use FlexiChains.last_sampler_state. This always returns a vector of states with length equal to the number of chains. Note that this applies also if you only sampled a single chain, in which case the returned value is a vector of length 1: you will therefore have to use only() to extract the state itself.The above applies equally to MCMCSerial() and MCMCDistributed().","title":"Saving and resuming MCMC sampling progress"},{"category":"section","location":"turing/#Initialising-MCMC-sampling-from-a-FlexiChain","page":"Usage with Turing.jl","text":"You can also use the parameters stored in a FlexiChain to initialise MCMC sampling. Note that this is different from resuming sampling from a saved sampler state, because all other sampler information (e.g. momentum, ...) will be re-initialised.\n\nFor example, to start a new chain from the fifth iteration and first chain contained inside chain, you can do\n\nchn3 = sample(model, MH(), 5; chain_type=VNChain, initial_params=InitFromParams(chain, 5, 1))\n\nSince this only uses the parameters which are already part of the chain, this does not require you to use save_state=true for the original chain.","title":"Initialising MCMC sampling from a FlexiChain"},{"category":"section","location":"turing/#Posterior-predictions-and-friends","page":"Usage with Turing.jl","text":"The functions predict, returned, logjoint, loglikelihood, and logprior all work 'as expected' using FlexiChains with exactly the same signatures that you are used to.\n\nreturned(model, chain)\n\npointwise_logdensities, pointwise_loglikelihoods, and pointwise_prior_logdensities are also supported, and will return a new FlexiChain containing the log-probabilities for each variable.\n\nLOO-CV is also supported if you load PosteriorStats.jl: see the PosteriorStats.jl integration for more details.","title":"Posterior predictions and friends"},{"category":"section","location":"turing/#Plotting","page":"Usage with Turing.jl","text":"FlexiChains contains a few recipes for plotting with Plots.jl and Makie.jl; please see the plotting pages for more details. Here we demonstrate usage with Plots.jl.\n\nWhen plotting a VNChain, array-valued parameters will automatically be split up into their individual components. In this example we plot only tau and theta[1] to save space, but if you were to plot theta, you would get eight separate plots for each element of theta.\n\nusing StatsPlots\n\n# Or omit the second argument to plot all parameters.\nplot(chain, [@varname(tau), @varname(theta[1])])\nsavefig(\"plot_ex.svg\"); nothing # hide\n\n(Image: Trace and density plots for mu, tau, and theta1)","title":"Plotting"},{"category":"section","location":"contributing/#Contributing","page":"Contributing","text":"Contributions to FlexiChains are very much welcome, either in the form of issues or pull requests! If you'd like to discuss a potential new feature or improvement please also don't hesitate to open an issue.\n\nIn particular, I am very keen to to improve interoperability with the wider Julia ecosystem (e.g. via package extensions). If you have a package that you think could benefit from some custom behaviour with FlexiChains please do reach out!","title":"Contributing"},{"category":"section","location":"contributing/#Formatting","page":"Contributing","text":"FlexiChains currently uses Runic.jl for formatting. This is enforced on GitHub CI.","title":"Formatting"},{"category":"section","location":"contributing/#Tests","page":"Contributing","text":"To run the tests you can run julia --project=. from the root of the repository, and then run ]test from the REPL, much like any other Julia package.\n\nEquivalently, you can also run\n\njulia --project=. -e 'using Pkg; Pkg.test()'\n\nThe tests in FlexiChains are also set up to work with TestPicker.jl which allows you to run a subset of the tests by entering ! at the Julia REPL.\n\nFlexiChains contains some tests for plotting, which compare the output of the plotting functions against reference images. The reference images are stored in test/plots/images and are part of the repository itself.\n\nIf these need to be updated (e.g. because FlexiChains' plotting functionality has changed, or because of changes in the underlying plotting libraries), the reference images can be updated by running\n\nUPDATE_REFIMAGES=1 julia --project=. -e 'using Pkg; Pkg.test()'\n\nThis will skip all other tests and only rerun the plotting tests to update the reference images.","title":"Tests"},{"category":"section","location":"plots/#Plotting:-Plots.jl","page":"Plotting: Plots.jl","text":"FlexiChains defines a few plot recipes which allows you to use the Plots.jl ecosystem to visualise chains. In particular, you will want to import StatsPlots.jl.","title":"Plotting: Plots.jl"},{"category":"section","location":"plots/#Plot-types","page":"Plotting: Plots.jl","text":"What kind of plot you get in when using Plots.jl is controlled mainly by the seriestype keyword argument. For example, plot(..., seriestype=:histogram) will produce a histogram. In fact, calling histogram(...) simply redirects to plot(..., seriestype=:histogram).\n\nThe following series types are supported for FlexiChain objects.\n\nseriestype= Equivalent function Description\n:traceplot FlexiChains.Plots.traceplot Trace plot of samples\n:histogram Plots.histogram Histogram of samples\n:density Plots.density Kernel density estimate of samples\n:mixeddensity FlexiChains.Plots.mixeddensity Density plot or histogram, depending on whether the parameter is continuous or discrete\n:meanplot FlexiChains.Plots.meanplot Running mean of samples\n:autocorplot FlexiChains.Plots.autocorplot Autocorrelation of samples\n:traceplot_and_density Plots.plot (with no seriestype argument) Trace plot and mixed density side-by-side\n:rankplot FlexiChains.Plots.rankplot with overlay=false Rank plot with separate histograms per chain\n:rankplot_overlay FlexiChains.Plots.rankplot with overlay=true Rank plot with all chains' data overlaid\n\nThere is currently one exception to this: StatsPlots.cornerplot is manually overloaded because it does not use the usual seriestype mechanism.\n\nwarning: Identifier conflicts\nPlease note that the identifiers traceplot, meanplot, mixeddensity, and autocorplot are also exported by MCMCChains.jl (which uses Plots.jl as its backend), as well as the FlexiChains.Makie submodule. If you have imported more than one of these modules, you will need to disambiguate which function you want to use by prefixing it with the module name, e.g. FlexiChains.Plots.traceplot.\n\nnote: Feature parity with MCMCChains.jl\nThere are still a couple fewer plotting options than in MCMCChains.jl. Other plot types will be added over time, but in the meantime if you need features from MCMCChains, you can convert a FlexiChain to an MCMCChains.Chains object using MCMCChains.Chains(chn). Help with adding new plots is very much welcome!","title":"Plot types"},{"category":"section","location":"plots/#General-interface","page":"Plotting: Plots.jl","text":"The above plotting functions should be called with the following signature:\n\nplotfunc(\n    chn[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...\n)\n\nPositional arguments\n\nchn is a FlexiChain object.\nparam_or_params is optional, and can be anything that is used to index into a chain. If not provided, all parameters in the chain will be plotted.\n\nKeyword arguments\n\nIf pool_chains=true, then samples from all chains are concatenated before plotting densities or histograms. Otherwise, each chain is plotted separately.\nSome plotting functions like autocorplot and rankplot have additional keyword arguments which control the details of the plot; please see the docstrings for those functions for more details.\nOther keyword arguments are passed through to the underlying Plots.jl functions which allow you to, for example, control the appearance of the plot.","title":"General interface"},{"category":"section","location":"plots/#Setup","page":"Plotting: Plots.jl","text":"Here, we demonstrate the plotting features with a typical chain sampled from a Turing model. However, the general principles are applicable to any FlexiChain object.\n\nWe'll make a model with different types of parameters (continuous, discrete, and vector-valued).\n\nusing FlexiChains, StatsPlots, Turing\n\nimport FlexiChains.Plots as FP # For the plotting functions.\n\n@model function f()\n    x ~ Normal()\n    y ~ Poisson(3)\n    z ~ MvNormal(zeros(2), I)\nend\n\nchn = sample(\n    f(), MH(), MCMCThreads(), 1000, 3;\n    discard_initial=100, chain_type=VNChain, progress=false\n)","title":"Setup"},{"category":"section","location":"plots/#Default-plot","page":"Plotting: Plots.jl","text":"Calling plot(chn) produces a trace plot and mixed density side-by-side for each parameter.\n\nNotice that the chain has not split z up into z[1] and z[2]. However, when plotting, it will be automatically split up for you. Also notice that Extra keys, like the log probabilities, are not plotted by default.\n\nplot(chn)\nsavefig(\"plot1.svg\"); nothing # hide\n\n(Image: Trace and density plots of the sampled chain)\n\nIf you want to plot specific parameter(s), you can specify them as the second positional argument. In general, the second argument can be anything that you can index into a chain with. This means a symbol, a parameter, a FlexiChains.Extra, a sub-VarName, or a vector thereof:\n\nplot(chn, [@varname(x), :logjoint])\nsavefig(\"plot2.svg\"); nothing # hide\n\n(Image: Trace and density plots of x and the logjoint)","title":"Default plot"},{"category":"section","location":"plots/#Trace-plots","page":"Plotting: Plots.jl","text":"FP.traceplot(chn)\nsavefig(\"traceplot.svg\"); nothing # hide\n\n(Image: Trace plots of the sampled chain)","title":"Trace plots"},{"category":"section","location":"plots/#plots-density","page":"Plotting: Plots.jl","text":"Density plots are produced using the standard Plots.density function, which works with FlexiChain objects.\n\ndensity(chn)\nsavefig(\"density.svg\"); nothing # hide\n\n(Image: Density plots of the sampled chain)","title":"Density plots"},{"category":"section","location":"plots/#plots-histogram","page":"Plotting: Plots.jl","text":"Similarly, Plots.histogram works with FlexiChain objects:\n\nhistogram(chn)\nsavefig(\"histogram.svg\"); nothing # hide\n\n(Image: Histograms of the sampled chain)","title":"Histograms"},{"category":"section","location":"plots/#Running-mean-plots","page":"Plotting: Plots.jl","text":"FP.meanplot(chn)\nsavefig(\"meanplot.svg\"); nothing # hide\n\n(Image: Running mean plots of the sampled chain)","title":"Running mean plots"},{"category":"section","location":"plots/#Rank-plots","page":"Plotting: Plots.jl","text":"FP.rankplot(chn)\nsavefig(\"rankplot.svg\"); nothing # hide\n\n(Image: Rank plots of the sampled chain)","title":"Rank plots"},{"category":"section","location":"plots/#Autocorrelation-plots","page":"Plotting: Plots.jl","text":"FP.autocorplot(chn)\nsavefig(\"autocorplot.svg\"); nothing # hide\n\n(Image: Autocorrelation plots of the sampled chain)","title":"Autocorrelation plots"},{"category":"section","location":"plots/#Mixed-density-plots","page":"Plotting: Plots.jl","text":"FP.mixeddensity(chn)\nsavefig(\"mixeddensity.svg\"); nothing # hide\n\n(Image: Mixed density plots of the sampled chain)","title":"Mixed density plots"},{"category":"section","location":"plots/#Corner-plots","page":"Plotting: Plots.jl","text":"StatsPlots.cornerplot(chn)\nsavefig(\"cornerplot.svg\"); nothing # hide\n\n(Image: Corner plot of the sampled chain)","title":"Corner plots"},{"category":"section","location":"plots/#Violin-plots","page":"Plotting: Plots.jl","text":"Plots.violin(chn)\nsavefig(\"violin.svg\"); nothing # hide\n\n(Image: Violin plots of the sampled chain)","title":"Violin plots"},{"category":"function","location":"plots/#RecipesBase.plot","page":"Plotting: Plots.jl","text":"Plots.plot(\n    chn::FlexiChain[, param_or_params];\n    pool_chains=false,\n    kwargs...\n)\n\nPlot a FlexiChain using Plots.jl. By default, this produces a trace plot and mixed density side-by-side for each parameter.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with plot(chn, :).\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"RecipesBase.plot"},{"category":"function","location":"plots/#FlexiChains.Plots.traceplot","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.traceplot(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nPlot the sample values against iteration number for the specified parameter(s) in the given FlexiChain using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with traceplot(chn, :).\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"FlexiChains.Plots.traceplot"},{"category":"function","location":"plots/#FlexiChains.Plots.traceplot!","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.traceplot!(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nSame as FlexiChains.Plots.traceplot, but uses plot! instead of plot.\n\n\n\n\n\n","title":"FlexiChains.Plots.traceplot!"},{"category":"function","location":"plots/#Plots.density","page":"Plotting: Plots.jl","text":"Plots.density(\n    chn::FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...\n)\n\nMake a density plot of the parameter values in chn using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with density(chn, :).\n\nThe pool_chains keyword argument specifies whether to pool samples across multiple chains when plotting. If true (the default), samples from all chains are pooled together; if false, samples from each chain are plotted separately.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"Plots.density"},{"category":"function","location":"plots/#Plots.histogram","page":"Plotting: Plots.jl","text":"Plots.histogram(\n    chn::FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...\n)\n\nMake a histogram of the parameter values in chn using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with histogram(chn, :).\n\nThe pool_chains keyword argument specifies whether to pool samples across multiple chains when plotting. If true (the default), samples from all chains are pooled together; if false, samples from each chain are plotted separately.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"Plots.histogram"},{"category":"function","location":"plots/#FlexiChains.Plots.meanplot","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.meanplot(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nPlot the running mean of the specified parameter(s) in the given FlexiChain using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with meanplot(chn, :).\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"FlexiChains.Plots.meanplot"},{"category":"function","location":"plots/#FlexiChains.Plots.meanplot!","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.meanplot!(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nSame as FlexiChains.Plots.meanplot, but uses plot! instead of plot.\n\n\n\n\n\n","title":"FlexiChains.Plots.meanplot!"},{"category":"function","location":"plots/#FlexiChains.Plots.rankplot","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.rankplot(\n    chn::FlexiChain{TKey}[, param_or_params];\n    overlay::Bool=false,\n    kwargs...\n)\n\nPlot a histogram of ranks for the specified parameter(s) in the given FlexiChain using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with rankplot(chn, :).\n\nIf overlay is false (the default), a separate histogram is plotted for each chain. If true, the histograms for all chains are overlaid on a single plot with different colours.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"FlexiChains.Plots.rankplot"},{"category":"function","location":"plots/#FlexiChains.Plots.rankplot!","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.rankplot!(\n    chn::FlexiChain{TKey}[, param_or_params];\n    overlay::Bool=false,\n    kwargs...\n)\n\nSame as FlexiChains.Plots.rankplot, but uses plot! instead of plot.\n\n\n\n\n\n","title":"FlexiChains.Plots.rankplot!"},{"category":"function","location":"plots/#FlexiChains.Plots.autocorplot","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.autocorplot(\n    chn::FlexiChain{TKey}[, param_or_params];\n    lags=1:min(niters(chn)-1, round(Int,10*log10(niters(chn)))),\n    demean=true,\n    kwargs...\n)\n\nPlot the autocorrelation of the specified parameter(s) in the given FlexiChain using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with autocorplot(chn, :).\n\nThe lags keyword argument specifies which lags to plot. By default, this is set to the integers from 1 to min(niters-1, round(Int,10*log10(niters))), mimicking the default behaviour of StatsBase.autocor.\n\nThe demean keyword argument specifies whether to subtract the mean before computing the autocorrelation (default true), and is passed to StatsBase.autocor.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"FlexiChains.Plots.autocorplot"},{"category":"function","location":"plots/#FlexiChains.Plots.autocorplot!","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.autocorplot!(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nSame as FlexiChains.Plots.autocorplot, but uses plot! instead of plot.\n\n\n\n\n\n","title":"FlexiChains.Plots.autocorplot!"},{"category":"function","location":"plots/#FlexiChains.Plots.mixeddensity","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.mixeddensity(\n    chn::FlexiChain{TKey}[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...\n)\n\nPlot a density estimate or histogram for the specified parameter(s) in the given FlexiChain using Plots.jl. Continuous-valued parameters are plotted as density estimates, discrete-valued parameters as histograms.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with mixeddensity(chn, :).\n\nThe pool_chains keyword argument specifies whether to pool samples across multiple chains when plotting. If true (the default), samples from all chains are pooled together; if false, samples from each chain are plotted separately.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"FlexiChains.Plots.mixeddensity"},{"category":"function","location":"plots/#FlexiChains.Plots.mixeddensity!","page":"Plotting: Plots.jl","text":"FlexiChains.Plots.mixeddensity!(\n    chn::FlexiChain{TKey}[, param_or_params];\n    kwargs...\n)\n\nSame as FlexiChains.Plots.mixeddensity, but uses plot! instead of plot.\n\n\n\n\n\n","title":"FlexiChains.Plots.mixeddensity!"},{"category":"function","location":"plots/#StatsPlots.cornerplot","page":"Plotting: Plots.jl","text":"StatsPlots.cornerplot(\n    chn::FlexiChain[, param_or_params];\n    kwargs...\n)\n\nMake a corner plot of chn using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with cornerplot(chn, :).\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions.\n\n\n\n\n\n","title":"StatsPlots.cornerplot"},{"category":"function","location":"plots/#Plots.violin","page":"Plotting: Plots.jl","text":"Plots.violin(\n    chn::FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    with_box::Bool=false,\n    kwargs...\n)\n\nMake a violin plot of chn using Plots.jl.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with violin(chn, :).\n\nThe pool_chains keyword argument specifies whether to pool samples across multiple chains when plotting. If true (the default), samples from all chains are pooled together; if false, samples from each chain are plotted separately.\n\nIf with_box=true, a box plot will additionally be overlaid on the violin plot.\n\nOther keyword arguments are forwarded to the underlying Plots.jl functions. (For with_box=true, keyword arguments are passed to both the violin and box plot components.)\n\n\n\n\n\n","title":"Plots.violin"},{"category":"section","location":"makie/#Plotting:-Makie.jl","page":"Plotting: Makie.jl","text":"Many parts of the Makie integration in FlexiChains are heavily lifted from the (unreleased) ChainsMakie.jl package. This includes some custom code to generate (e.g.) shared legends, which leads to slightly nicer plots than for Plots.jl (on top of Makie generally yielding nicer plots out of the box).\n\nnote: Note\nYou can also use Makie as a backend to make pair plots! This requires loading a Makie backend as well as PairPlots.jl. Please see the PairPlots.jl integration section for more details.","title":"Plotting: Makie.jl"},{"category":"section","location":"makie/#makie-interface","page":"Plotting: Makie.jl","text":"For all functions plotfunc shown in the table of the plotting page, you can use the following invocation:\n\nGenerate an entire Makie.Figure. This automatically generates a complete plot for you, including a legend.\nplotfunc(\n    chn[, param_or_params];\n    figure=(;), axis=(;), legend=(;),\n    legend_position=:bottom,\n    kwargs...\n)\nparam_or_params can be anything used to index into a chain (single parameters are also accepted). If not specified, all parameters in the chain will be plotted.\nMost keyword arguments are passed to the underlying Makie plotting functions, but there are some special ones which are handled by FlexiChains. For more information about these, see the customisation section below.\n\nFor functions which create only a single plot per parameter (e.g. density, or traceplot), the following options are also available. The intention is to allow you to build more complex figures using these as building blocks:\n\nPlot a single parameter onto an existing Makie.Axis object: this uses the 'mutating' version with an exclamation mark.\nIf ax is not specified, uses the current axis. Colours are handled the same way as above. param must be a single parameter.\nplotfunc!([ax, ]chn, param; kwargs...)\nPlot a single parameter onto a Makie grid position. This constructs a Makie.Axis for you, and as before you can pass options via the axis keyword argument. Colours are handled the same way as above. param must be a single parameter.\nf = Figure()\ngp = f[1, 1]\nplotfunc!(gp, chn, param; axis=(;), kwargs...)","title":"General interface"},{"category":"section","location":"makie/#Setup","page":"Plotting: Makie.jl","text":"Here, we create a model with different types of parameters (continuous, discrete, and vector-valued). This is the same model as used on the Plots.jl documentation page.\n\nusing FlexiChains, CairoMakie, Turing\n\nimport FlexiChains.Makie as FM # For the plotting functions.\n\n@model function f()\n    x ~ Normal()\n    y ~ Poisson(3)\n    z ~ MvNormal(zeros(2), I)\nend\n\nchn = sample(\n    f(), MH(), MCMCThreads(), 1000, 3;\n    discard_initial=100, chain_type=VNChain, progress=false\n)","title":"Setup"},{"category":"section","location":"makie/#Default-plot","page":"Plotting: Makie.jl","text":"Calling Makie.plot(chn) produces a trace plot and mixed density side-by-side for each parameter.\n\nMakie.plot(chn)\nMakie.save(\"plot_makie.png\", ans.figure); # hide\n\n(Image: Default trace and density plots of the sampled chain)","title":"Default plot"},{"category":"section","location":"makie/#Trace-plots","page":"Plotting: Makie.jl","text":"FM.traceplot(chn)\nMakie.save(\"traceplot_makie.png\", ans.figure); # hide\n\n(Image: Trace plots of the sampled chain)","title":"Trace plots"},{"category":"section","location":"makie/#Density-plots","page":"Plotting: Makie.jl","text":"Makie.density(chn)\nMakie.save(\"density_makie.png\", ans.figure); # hide\n\n(Image: Density plots of the sampled chain)","title":"Density plots"},{"category":"section","location":"makie/#Histograms","page":"Plotting: Makie.jl","text":"Makie.hist(chn)\nMakie.save(\"hist_makie.png\", ans.figure); # hide\n\n(Image: Histograms of the sampled chain)","title":"Histograms"},{"category":"section","location":"makie/#Mixed-density-plots","page":"Plotting: Makie.jl","text":"FM.mixeddensity(chn)\nMakie.save(\"mixeddensity_makie.png\", ans.figure); # hide\n\n(Image: Mixed density plots of the sampled chain)","title":"Mixed density plots"},{"category":"section","location":"makie/#Running-mean-plots","page":"Plotting: Makie.jl","text":"FM.meanplot(chn)\nMakie.save(\"meanplot_makie.png\", ans.figure); # hide\n\n(Image: Running mean plots of the sampled chain)","title":"Running mean plots"},{"category":"section","location":"makie/#Autocorrelation-plots","page":"Plotting: Makie.jl","text":"FM.autocorplot(chn)\nMakie.save(\"autocorplot_makie.png\", ans.figure); # hide\n\n(Image: Autocorrelation plots of the sampled chain)","title":"Autocorrelation plots"},{"category":"section","location":"makie/#Rank-plots","page":"Plotting: Makie.jl","text":"FM.rankplot(chn)\nMakie.save(\"rankplot_makie.png\", ans.figure); # hide\n\n(Image: Rank plots of the sampled chain)","title":"Rank plots"},{"category":"section","location":"makie/#Forest-plots","page":"Plotting: Makie.jl","text":"info: Half-eye plots\nTo get a 'half-eye' look similar to R's ggdist package, you can compose forestplot! and ridgeline!. See e.g. the example here.\n\nFM.forestplot(chn)\nMakie.save(\"forestplot_makie.png\", ans.figure); # hide\n\n(Image: Forest plots of the sampled chain)","title":"Forest plots"},{"category":"section","location":"makie/#Ridgeline-plots","page":"Plotting: Makie.jl","text":"info: Half-eye plots\nTo get a 'half-eye' look similar to R's ggdist package, you can compose forestplot! and ridgeline!. See e.g. the example here.\n\nFM.ridgeline(chn)\nMakie.save(\"ridgeline_makie.png\", ans.figure); # hide\n\n(Image: Ridgeline plots of the sampled chain)","title":"Ridgeline plots"},{"category":"section","location":"makie/#Pushforward-plots","page":"Plotting: Makie.jl","text":"These plots are based on Michael Betancourt's mcmc_visualization_tools. We include a worked example to motivate and demonstrate the use of these plots.","title":"Pushforward plots"},{"category":"section","location":"makie/#Example","page":"Plotting: Makie.jl","text":"A pushforward distribution is obtained by mapping a function over a distribution: specifically, if some random variable θ has distribution p(θ), then the pushforward of θ through a function f is the distribution of Y = f(θ).\n\nIn Bayesian inference, we're often interested in taking posterior draws θ ~ p(θ | D) (as represented in a chain) and pushing this through some function f to obtain a new distribution. For example, f may be the function which generates posterior predictive draws, in which case the pushforward distribution is the posterior predictive distribution.\n\nEach parameter draw θ yields a different draw of f, so the spread of f inherits posterior uncertainty. The pushforward plots in this section visualise that uncertainty as nested quantile bands, displayed as either a fitted curve (pushforward_continuous), per-group summaries (pushforward_discrete), or predictive histograms (pushforward_hist).\n\nWe'll concoct an example with the Palmer penguins dataset to show how these plots can be used.\n\nusing Turing, DataFrames, PalmerPenguins, CairoMakie\nimport FlexiChains.Makie as FM\nusing StatsBase: denserank\n\n# Load data and normalise continuous variables\nstandardize(x) = (x .- mean(x)) ./ std(x)\npenguins = DataFrame(PalmerPenguins.load())\ndropmissing!(penguins)\ntransform!(penguins, names(penguins, Real) .=> standardize => identity)\ntransform!(penguins, :species => denserank => :species_idx)\n\n# Define a model for penguin bill length as a function of species and body mass\n@model function bill_model(species, body_mass)\n    n_species = length(unique(species))\n    beta1 ~ filldist(Normal(0, 1), n_species)\n    beta2 ~ Normal(0, 1)\n    beta3 ~ filldist(Normal(0, 1), n_species)\n    sigma ~ Exponential(1)\n    mu := @. beta1[species] + beta2 * body_mass + beta3[species] * body_mass\n    bill_length_mm ~ MvNormal(mu, sigma)\nend\nnothing # hide\n\nWe model a penguin's bill length as a function of its species (beta1), its body mass (beta2), and the interaction between them (beta3), i.e., we ask \"does the effect of body mass vary by species?\".\n\nNext, we condition the model on the observed data and run MCMC.\n\nprior_model = bill_model(penguins.species_idx, penguins.body_mass_g) \ncond_model = prior_model | (; bill_length_mm = penguins.bill_length_mm)\nchain = sample(cond_model, NUTS(0.8), MCMCThreads(), 1000, 4)\nnothing # hide\n\nA common starting point is to plot the posterior predictive distribution (see also the Turing.jl docs on this); it can help us (at least superficially) test if the model captured basic patterns in the input data. We can plot a summary histogram with uncertainty bands using pushforward_hist. By specifying the observed keyword argument we can also overlay the observed data so that we can visually compare the two distributions.\n\n# Note that we pass the `prior_model` here, not the conditioned model, so that\n# we can sample new draws for the conditioned variables (i.e., bill length).\n# This is explained in the Turing docs linked above.\nppd = predict(prior_model, chain)\n\nobserved = penguins.bill_length_mm\nFM.pushforward_hist(ppd, @varname(bill_length_mm); observed=observed)\n\nWe may also be interested in how predicted bill length changes with increasing body mass, and how this varies by species. For this, we can make use of pushforward_continuous by feeding it a grid of body mass values (z-standardized here).\n\nIn the example below, we have set sigma = 0 to drop the predictive uncertainty; we're interested only in the uncertainty of the means here.\n\n# Set up the grid of body mass values and species indices\npred_body_mass = repeat(range(-3, 3, length = 50), outer = 3)\npred_species = repeat(1:3, inner = 50)\n\n# For each draw of the parameters in the chain, compute the predicted\n# bill length for each combination of species and body mass.\npred_model = fix(bill_model(pred_species, pred_body_mass), (; sigma = 0))\npred = predict(pred_model, chain)\n\n# Plot the predicted means with uncertainty bands, coloured by species.\nfig = Figure()\nax = Axis(fig[1, 1]; xlabel = \"body mass\", ylabel = \"bill length\", limits = ((-3, 3), (-3, 3)))\nfor (s, color) in zip(1:3, Makie.wong_colors())\n    ix = findall(==(s), pred_species)\n    x_grid = pred_body_mass[ix]\n    FM.pushforward_continuous!(ax, pred, @varname(mu[ix]); x_grid=x_grid, color=color)\nend\n\nfig\n\nFinally, if we want to examine the distributions of discrete parameters, such as the main or interaction effect of species, we can use pushforward_discrete. Here, we'll look at the interaction effect to see if there's any evidence for the effect of body mass varying by species.\n\nFM.pushforward_discrete(chain, @varname(beta3))","title":"Example"},{"category":"section","location":"makie/#makie-customisation","page":"Plotting: Makie.jl","text":"As described in the general interface section above, all of the above functions accept keyword arguments to control the appearance of the plot.\n\nThe figure, axis, and legend arguments (which can be, e.g., NamedTuples) allow you to pass extra keyword arguments to the Figure, Axis, and Legend constructors. Then, most other keyword arguments are forwarded to the underlying Makie plotting functions; please refer to the Makie documentation for more details on these.\n\nFinally, there are also some special keyword arguments which are handled by FlexiChains. Here are some examples of these in action.","title":"Customisation"},{"category":"section","location":"makie/#Custom-layout","page":"Plotting: Makie.jl","text":"By default, plots are arranged with one parameter per row. You can pass a tuple of (nrows, ncols) as the layout keyword argument to change this:\n\nMakie.density(chn; layout=(2, 2))\nMakie.save(\"custom_layout_makie.png\", ans.figure); # hide\n\n(Image: Density plots with a 2x2 layout)","title":"Custom layout"},{"category":"section","location":"makie/#Custom-colours","page":"Plotting: Makie.jl","text":"Pass a vector of colours (one per chain) via the color keyword, or a categorical colormap via colormap:\n\nFM.traceplot(chn, [@varname(x), @varname(y)];\n    color=[(:red, 0.6), (:blue, 0.6), (:green, 0.6)],\n    # or e.g. colormap=:tab10\n)\nMakie.save(\"custom_colors_makie.png\", ans.figure); # hide\n\n(Image: Trace plots with custom colours)\n\nnote: Note\nTo get the best effects with colormap, you should pass a categorical colormap such as :tab10. Continuous colormaps like :viridis will give poor results since it will use the first n colours of the colormap, which are all very similar!","title":"Custom colours"},{"category":"section","location":"makie/#Legend-position","page":"Plotting: Makie.jl","text":"Use legend_position to move the legend (:bottom, :right, or :none):\n\nFM.traceplot(chn; legend_position=:right)\nMakie.save(\"custom_legend_makie.png\", ans.figure); # hide\n\n(Image: Trace plots with the legend on the right)","title":"Legend position"},{"category":"function","location":"makie/#Makie.plot","page":"Plotting: Makie.jl","text":"Makie.plot(\n    chn::FC.FlexiChain,\n    param_or_params = FC.Parameter.(FC.parameters(chn));\n    kwargs...,\n)\n\nPlot the parameters in a FlexiChain using Makie. For each parameter, a trace plot and a density plot are created.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with Makie.plot(chn, :).\n\nKeyword arguments\n\npool_chains::Bool: whether to pool data from all chains into a single plot, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"Makie.plot"},{"category":"function","location":"makie/#FlexiChains.Makie.traceplot","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.traceplot(\n    chn::FC.FlexiChain[, param_or_params];\n    kwargs...,\n)\n\nCreate trace plots for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.traceplot(chn, :).\n\nKeyword arguments\n\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.traceplot"},{"category":"function","location":"makie/#FlexiChains.Makie.traceplot!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.traceplot!\n\nMutating version of FlexiChains.Makie.traceplot, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.traceplot!"},{"category":"function","location":"makie/#Makie.density","page":"Plotting: Makie.jl","text":"Makie.density(\n    chn::FC.FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate density plots for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with Makie.density(chn, :).\n\nKeyword arguments\n\npool_chains::Bool: whether to pool data from all chains into a single plot, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"Makie.density"},{"category":"function","location":"makie/#Makie.hist","page":"Plotting: Makie.jl","text":"Makie.hist(\n    chn::FC.FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate histograms for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with Makie.hist(chn, :).\n\nKeyword arguments\n\npool_chains::Bool: whether to pool data from all chains into a single plot, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"Makie.hist"},{"category":"function","location":"makie/#Makie.stephist","page":"Plotting: Makie.jl","text":"Makie.stephist(\n    chn::FC.FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate a step histogram for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with Makie.stephist(chn, :).\n\nKeyword arguments\n\npool_chains::Bool: whether to pool data from all chains into a single plot, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"Makie.stephist"},{"category":"function","location":"makie/#FlexiChains.Makie.mixeddensity","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.mixeddensity(\n    chn::FC.FlexiChain[, param_or_params];\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate mixed density plots (i.e., density plots for continuous parameters and histograms for discrete parameters) for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.mixeddensity(chn, :).\n\nKeyword arguments\n\npool_chains::Bool: whether to pool data from all chains into a single plot, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.mixeddensity"},{"category":"function","location":"makie/#FlexiChains.Makie.mixeddensity!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.mixeddensity!\n\nMutating version of FlexiChains.Makie.mixeddensity, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.mixeddensity!"},{"category":"function","location":"makie/#FlexiChains.Makie.meanplot","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.meanplot(\n    chn::FC.FlexiChain[, param_or_params];\n    kwargs...,\n)\n\nPlot the running mean of the specified parameter(s) in the given FlexiChain using Makie.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.meanplot(chn, :).\n\nKeyword arguments\n\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.meanplot"},{"category":"function","location":"makie/#FlexiChains.Makie.meanplot!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.meanplot!\n\nMutating version of FlexiChains.Makie.meanplot, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.meanplot!"},{"category":"function","location":"makie/#FlexiChains.Makie.autocorplot","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.autocorplot(\n    chn::FC.FlexiChain[, param_or_params];\n    lags=FlexiChains.PlotUtils.default_lags(chn),\n    demean=true,\n    kwargs...,\n)\n\nPlot the autocorrelation of the specified parameter(s) in the given FlexiChain using Makie.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.autocorplot(chn, :).\n\nKeyword arguments\n\nlags: the lags at which to compute the autocorrelation. Defaults to 1:min(niters-1, round(Int, 10*log10(niters))).\ndemean: whether to subtract the mean before computing the autocorrelation. Defaults to true.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.autocorplot"},{"category":"function","location":"makie/#FlexiChains.Makie.autocorplot!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.autocorplot!\n\nMutating version of FlexiChains.Makie.autocorplot, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.autocorplot!"},{"category":"function","location":"makie/#FlexiChains.Makie.rankplot","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.rankplot(\n    chn::FC.FlexiChain[, param_or_params];\n    overlay::Bool=false,\n    kwargs...,\n)\n\nCreate rank plots for the specified parameters in the chain.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.rankplot(chn, :).\n\nKeyword arguments\n\noverlay::Bool: whether to overlay the histograms for all chains on the same axis, or to plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.rankplot"},{"category":"function","location":"makie/#FlexiChains.Makie.rankplot!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.rankplot!\n\nMutating version of FlexiChains.Makie.rankplot, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.rankplot!"},{"category":"function","location":"makie/#FlexiChains.Makie.forestplot","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.forestplot(\n    chn::FC.FlexiChain[, param_or_params];\n    point::Symbol=:median,\n    interval::Symbol=:quantile,\n    hdi_method::Symbol=:unimodal,\n    levels::Tuple=(0.66, 0.95),\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate a forest (caterpillar) plot for the specified parameters in the chain. Each parameter is shown as a point estimate with one or more credible interval bars.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.forestplot(chn, :).\n\nKeyword arguments\n\npoint::Symbol: the point estimate to use. Must be :mean or :median. Defaults to :median.\ninterval::Symbol: the method to use for computing credible intervals. Must be :quantile or :hdi. Defaults to :quantile. Note that to use :hdi you must have PosteriorStats.jl loaded.\nhdi_method::Symbol: if interval=:hdi, the method to use for computing HDIs. Defaults to :unimodal; please see the PosteriorStats.jl documentation for details.\nlevels: a tuple of credible interval widths, e.g. (0.5, 0.94) for 50% and 94% intervals. Wider intervals are drawn with thinner lines.\npool_chains::Bool: whether to pool data from all chains or plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.forestplot"},{"category":"function","location":"makie/#FlexiChains.Makie.forestplot!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.forestplot!\n\nMutating version of FlexiChains.Makie.forestplot, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.forestplot!"},{"category":"function","location":"makie/#FlexiChains.Makie.ridgeline","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.ridgeline(\n    chn::FC.FlexiChain[, param_or_params];\n    scale::Float64=0.8,\n    pool_chains::Bool=false,\n    kwargs...,\n)\n\nCreate a ridgeline (density ridge) plot for the specified parameters in the chain. Each parameter is shown as a filled kernel density estimate stacked vertically.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with FlexiChains.Makie.ridgeline(chn, :).\n\nKeyword arguments\n\nscale::Float64: height of each ridge relative to the spacing between parameters. Values greater than 1.0 cause ridges to overlap. Defaults to 0.8.\npool_chains::Bool: whether to pool data from all chains or plot each chain separately. Defaults to false.\nfigure::NamedTuple: Additional keyword arguments passed to the Makie.Figure constructor.\naxis::NamedTuple: Additional keyword arguments passed to the Makie.Axis constructor for each subplot.\nlegend::NamedTuple: Additional keyword arguments passed to the Makie.Legend constructor, if the legend is added.\nlegend_position::Symbol: Position of the legend. This can be either :right, :bottom or :none for no legend.\nlayout: either nothing (the default), or a tuple of (nrows, ncols) specifying the grid layout for the subplots.\n\nExtra keyword arguments are passed to Makie's plotting functions, which allow you to customise the appearance of the plot.\n\n\n\n\n\n","title":"FlexiChains.Makie.ridgeline"},{"category":"function","location":"makie/#FlexiChains.Makie.ridgeline!","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.ridgeline!\n\nMutating version of FlexiChains.Makie.ridgeline, for use with existing Makie.Axis objects.\n\n\n\n\n\n","title":"FlexiChains.Makie.ridgeline!"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_continuous","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.pushforward_continuous(chn, param_or_params; x_grid=nothing; kwargs...)\n\nPlot the marginal posterior of each component of an array parameter as a quantile ribbon, forming a \"function envelope\" over x_grid. Useful for visualising how a functional quantity (e.g. a fitted curve or spectrum) varies with posterior uncertainty.\n\nThis is a port of Michael Betancourt's plot_conn_pushforward_quantiles.\n\nKeyword arguments\n\nx_grid: the x-values to plot against. Defaults to 1:N, where N is the number of components being plotted.\nquantiles: odd-length vector of levels in 0–1. Defaults to [0.1, 0.2, ..., 0.9].\nbaseline: length-N vector overlaid as a reference line.\nresidual: if true, subtract baseline before banding (requires baseline).\nfigure, axis: NamedTuples forwarded to Makie.Figure / Makie.Axis.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_continuous"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_continuous!","page":"Plotting: Makie.jl","text":"Mutating version of FlexiChains.Makie.pushforward_continuous.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_continuous!"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_discrete","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.pushforward_discrete(chn, param_or_params; vertical=true, kwargs...)\n\nPlot each component of an array parameter as an independent quantile bar, with nested intervals shown as stacked bands. Unlike pushforward_continuous, components are not connected; each bar is separated from its neighbours , making this appropriate when the components have no natural ordering or functional relationship (e.g. group-level intercepts in a hierarchical model).\n\nThis function is a port of Michael Betancourt's plot_disc_pushforward_quantiles.\n\nKeyword arguments\n\n\n\nvertical: if true, bars are vertical; otherwise horizontal. Defaults to true.\nquantiles: odd-length vector of levels in 0–1. Defaults to [0.1, 0.2, ..., 0.9].\nbaseline: length-N vector overlaid per index.\nresidual: if true, subtract baseline before banding (requires baseline).\nfigure, axis: NamedTuples forwarded to Makie.Figure / Makie.Axis.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_discrete"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_discrete!","page":"Plotting: Makie.jl","text":"Mutating version of FlexiChains.Makie.pushforward_discrete.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_discrete!"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_hist","page":"Plotting: Makie.jl","text":"FlexiChains.Makie.pushforward_hist(chn, param_or_params; observed=nothing, nbins=25, kwargs...)\n\nPosterior predictive check via histograms. For each posterior draw, the predictive values are binned into a histogram; the resulting per-bin count distributions are summarised as nested quantile ribbons. Overlaying observed data shows whether the model's predictive distribution is consistent with the observations.\n\nThis function is a port of Michael Betancourt's plot_hist_quantiles.\n\nKeyword arguments\n\nobserved: vector of observed values; its histogram (same bins) is overlaid as a line.\nnbins: number of equal-width bins. Defaults to 25.\nquantiles: odd-length vector of levels in 0–1. Defaults to [0.1, 0.2, ..., 0.9].\nfigure, axis: NamedTuples forwarded to Makie.Figure / Makie.Axis.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_hist"},{"category":"function","location":"makie/#FlexiChains.Makie.pushforward_hist!","page":"Plotting: Makie.jl","text":"Mutating version of FlexiChains.Makie.pushforward_hist.\n\n\n\n\n\n","title":"FlexiChains.Makie.pushforward_hist!"},{"category":"section","location":"indexing/#Indexing","page":"Indexing","text":"A FlexiChain stores data in a  rich format: that means that instead of just storing a raw matrix of data, it also includes information about the iteration numbers and chain numbers.\n\nAdditionally, FlexiSummary objects (which you get when performing any kind of summarisation on a chain, e.g. with summarystats) also sometimes store information about which summary functions were applied (especially when there are multiple of these).\n\nThis information is used when constructing the DimensionalData.DimArray outputs that you see when indexing into a FlexiChain or FlexiSummary object. But, on top of this, it also allows you to more surgically index into these objects using this information.\n\nThis page first begins with some illustrative examples, which might be the clearest way to demonstrate the indexing behaviour. If you prefer reading a fuller specification, the sections below that describe the exact behaviour in more detail.","title":"Indexing"},{"category":"section","location":"indexing/#Examples:-chains","page":"Indexing","text":"Let's first set up a chain:\n\nusing FlexiChains, Turing\n@model function f()\n    x ~ MvNormal(zeros(2), I)\nend\nchn = sample(f(), MH(), MCMCThreads(), 5, 2; discard_initial=100, chain_type=VNChain, progress=false, verbose=false)\n\nNotice how the iteration numbers here start from 101: that is because of the discard_initial argument.\n\n# Picking out a single parameter; this returns a `DimMatrix`.\nchn[@varname(x)]\n\n# This picks out the first of the iterations (note: this has iteration number 101)\nchn[@varname(x), iter=1]\n\nwarning: Keyword arguments to `getindex`\nNote that keyword arguments when indexing with square brackets must be separated from positional arguments by a comma. Using a semicolon will lead to an error.\n\n# You can also select a specific chain\nchn[@varname(x), iter=1, chain=2]\n\n# This picks out iteration number 101\nchn[@varname(x), iter=At(101)]\n\n# This picks out iteration numbers 101 through 103\nchn[@varname(x), iter=101..103]\n\n# If you only want the first element of `x`:\nchn[@varname(x[1])]\n\n# You can specify a vector of parameters\nchn[[@varname(x[1]), :logjoint]]\n\nThis last one returns a FlexiChain object, because multiple keys were specified. The data that we didn't care for, such as @varname(x[2]), are simply dropped.\n\nNotice that this also gives us a way to 'flatten' a FlexiChain object such that all of its keys point to scalar values. We just need to find a full set of sub-VarNames, like the following. In practice you don't need to construct this set yourself: FlexiChains has an internal function, called _split_varnames, that will do this for you.\n\nchn[[@varname(x[1]), @varname(x[2])]]","title":"Examples: chains"},{"category":"section","location":"indexing/#Examples:-summaries","page":"Indexing","text":"Calling any summary function such as mean, std, or summarystats on a FlexiChain object will return a FlexiSummary object.\n\nsm = summarystats(chn)\n\nNotice two things: \n\nThis summary no longer has iter or chain dimensions, because the summary statistics have been calculated over all iterations and chains. However, it has a stat dimension, which we will need to use when accessing the data.\nThe variable x has been broken up for you into its components x[1] and x[2].\n\nIndexing is very similar as for chains, but there is an additional stat dimension which lets you specify which summary statistic you want to access.\n\nsm[@varname(x[1]), stat=At(:mean)]\n\nnote: At()\nNotice that we need stat=At(:mean) rather than just stat=:mean. This seems a bit verbose, but is actually perfectly consistent with DimensionalData.jl's behaviour: stat=1 means the first statistic, and stat=At(:f) means the statistic with the named index :f.\n\nIf only a single summary function was applied, e.g. via mean(chn), then the stat dimension will be automatically collapsed for you; you won't need to again specify At(:mean) when indexing.\n\nsm_mean = mean(chn)\nsm_mean[@varname(x[1])]\n\nIf you don't want to split the VarNames up, you can specify this as a keyword argument.\n\nsm_mean_nosplit = mean(chn; split_varnames=false)\nsm_mean_nosplit[@varname(x)]\n\nIf you collapse only over iterations (for example), then you can specify the chain keyword argument (and likewise for iter if you collapse over chains).\n\nsm_iter = mean(chn; dims=:iter)\nsm_iter[@varname(x[1]), chain=2]","title":"Examples: summaries"},{"category":"section","location":"indexing/#Positional-arguments","page":"Indexing","text":"When indexing into a FlexiChain (or FlexiSummary) object, you can use one optional positional argument. This positional argument can either be an object pointing to a single key, in which case a DimMatrix is returned; or it can be an object pointing to multiple keys, in which case a FlexiChain (or FlexiSummary) is returned.\n\nTo specify a single key, you can use:\n\na parameter name (e.g. for a FlexiChain{T}, an object of type T);\na VarName or sub-VarName, for a FlexiChain{VarName} (i.e. VNChain);\na FlexiChains.Extra for non-parameter keys;\na Symbol, as long as it refers to an unambiguous key;\na FlexiChains.Parameter (this is mentioned for completeness; as a user you probably don't need to do this)\n\nOn the other hand, you could specify multiple keys via:\n\na Vector containing any combination of the above;\na colon :, which refers to all keys in the chain or summary.\n\nIf a positional argument is not specified, it defaults to :.","title":"Positional arguments"},{"category":"section","location":"indexing/#Keyword-arguments:-chains","page":"Indexing","text":"","title":"Keyword arguments: chains"},{"category":"section","location":"indexing/#iter-and-chain","page":"Indexing","text":"In addition to the positional argument, you can also specify the iter and chain keyword arguments when indexing into the FlexiChain object. (FlexiSummary objects are covered right below this.) Both of these are optional, and exist to allow you to select data from specific iterations and/or chains.\n\nwarning: Keyword arguments to getindex\nWhen indexing with square brackets, the keyword arguments must be separated from positional arguments by a comma, not a semicolon as is usual for other Julia functions. That is to say, you should use:chn[param, iter=iter, chain=chain]\n# this is also fine, albeit a bit wordy\ngetindex(chn, param; iter=iter, chain=chain)rather than# this will error\nchn[param; iter=iter, chain=chain]\n\nThe allowed values for these keyword arguments almost exactly mimic the behaviour of DimensionalData.jl. Suppose that you sampled a chain with 100 iterations, but with a thinning factor of 2. FlexiChains will record this information, and its iteration numbers will be 1:2:199 (i.e. 1, 3, 5, ..., 199).\n\nFor clarity, we will refer to the actual iteration numbers (1, 3, 5, ..., 199) as iteration numbers, and the entries in the chain (1st entry, 2nd entry, ..., 100th entry) as entries.\n\nYou can then specify, for example:\n\niter=... Description\n5 the fifth entry in the chain, i.e. iteration number 9\nAt(9) iteration number 9\nNot(5) all entries except the fifth one, i.e. all iteration numbers except 9\nNot(At(9)) all entries except iteration number 9\n6..30 all iteration numbers between 6 and 30, i.e. all but the first entry\n2:10 2nd through 10th entries, i.e. iteration numbers 6 through 30\n[At(9), At(30)] this will get the entries corresponding to iteration numbers 9 and 30\n: all entries (i.e. all iteration numbers)\n\nFor convenience, FlexiChains re-exports the DimensionalData.jl selectors such as Not, At, and ...\n\nThe same applies to the chain keyword argument, except that here you are selecting which chains to include. This is slightly less interesting because chains are always numbered consecutively starting from 1. Consequently, i and At(i) have the same meaning. Nonetheless, you can still use all the same selectors as described above, e.g. Not(2) to drop the second chain.\n\nFinally, note that regular begin and end cannot be used with the iter and chain keyword arguments, because Julia's lowering pass does not handle those appropriately for keyword arguments. In place of this you can use Begin and End, which are semantically equivalent and re-exported from DimensionalData.\n\nchn[@varname(x), iter=(Begin+1):End]\n\nFor more information about DimensionalData's selectors, please see their docs.","title":"iter and chain"},{"category":"section","location":"indexing/#stack","page":"Indexing","text":"The stack keyword argument only affects parameters which are arrays; other parameters ignore it.\n\nIf stack=false (the default), then accessing an array-valued parameter will return a DimArray of Arrays. If stack=true, then the array-valued parameter will be stacked into a single DimArray. Note that the parameter's axes will be placed at the end of the returned DimArray. Thus, for example if the parameter :x is a Vector{T}, then chn[:x] will return a DimArray{T,3} with shape (iters, chains, length(x)).\n\nnote: DimArray\nIn the current version of FlexiChains, for DimArray-valued parameters the stacking happens by default. This will be removed in a future version: you will have to explicitly specify stack=true to get this behaviour.","title":"stack"},{"category":"section","location":"indexing/#Keyword-arguments:-summaries","page":"Indexing","text":"note: Positional arguments\nThe positional argument when indexing into a FlexiSummary objects is exactly the same as for FlexiChain. Only keyword arguments behave differently.\n\nThere are two differences between a FlexiChain and a FlexiSummary in terms of their indexing behaviour:\n\nFlexiSummary objects contain one additional dimension, called stat.\nFlexiSummary dimensions may be collapsed, meaning that they cannot be indexed into.\n\nConsequently, there are three possible keyword arguments: iter, chain, and stat; but depending on which dimensions have been collapsed, you may not be able to use them.","title":"Keyword arguments: summaries"},{"category":"section","location":"indexing/#iter-and-chain-2","page":"Indexing","text":"In general, if you apply a summary function like mean without specifying dimensions, then both iter and chain dimensions will be collapsed.\n\nIf you have performed the mean over a single dimension only, such as via summary = mean(chn; dims=:iter), then the iter dimension will be collapsed, but you can still index into the chain dimension using summary[key, chain=...].","title":"iter and chain"},{"category":"section","location":"indexing/#stat","page":"Indexing","text":"In general, the stat dimension is generally:\n\nnot collapsed if multiple summary functions were applied, e.g. via summarystats(chn);\ncollapsed if a single summary function was applied, e.g. via mean(chn).\n\nUnlike the iter and chain dimensions, the stat dimension's indices are Symbols instead of numbers. Thus, for example, if you have a summary that contains the mean and std of the chain, you could use:\n\nstat=... Description\n1 the first statistic, i.e. :mean\nAt(:mean) the :mean statistic\nNot(At(:mean)) everything but the :mean statistic","title":"stat"},{"category":"section","location":"indexing/#stack-2","page":"Indexing","text":"The stack keyword argument behaves the same as for FlexiChain objects.","title":"stack"},{"category":"section","location":"migration/#Migrating-from-MCMCChains","page":"Migrating from MCMCChains","text":"This page contains a few examples of how to adapt code written for MCMCChains to work with FlexiChains instead. These are collated from my experience updating Turing's docs and tests.\n\nIf you have any other questions or usage patterns, please do open an issue! I will be more than happy to add more examples here.","title":"Migrating from MCMCChains"},{"category":"section","location":"migration/#Setup","page":"Migrating from MCMCChains","text":"For the purposes of these docs, we will have to write a function, prior_chain, that creates a chain without using Turing explicitly. This is because Turing depends on FlexiChains, and if we make a breaking release of FlexiChains, then the docs will break as there will not yet be a version of Turing that is compatible.\n\nThe code is shown here for demonstration purposes, but don't worry about the implementation of this too much! It is really the same as calling sample(model, Prior(), MCMCSerial(), n_iters, n_chains; chain_type=Tchn).\n\ndetails: Prior chain sampling\nusing DynamicPPL, AbstractMCMC, MCMCChains, FlexiChains, Random, Distributions\n\nfunction prior_chain(\n    rng::Random.AbstractRNG,\n    model::DynamicPPL.Model,\n    niters::Int,\n    nchains::Int,\n    ::Type{Tchn}\n) where {Tchn}\n    vi = DynamicPPL.OnlyAccsVarInfo()\n    vi = DynamicPPL.setacc!!(vi, DynamicPPL.RawValueAccumulator(true))\n    ps = [DynamicPPL.ParamsWithStats(last(DynamicPPL.init!!(rng, model, vi, InitFromPrior(), UnlinkAll()))) for _ in 1:niters, _ in 1:nchains]\n    return AbstractMCMC.from_samples(Tchn, ps)\nend","title":"Setup"},{"category":"section","location":"migration/#A1.-Extracting-scalar-valued-samples","page":"Migrating from MCMCChains","text":"@model f() = x ~ Normal()\nmodel = f()\nmchain = prior_chain(Xoshiro(468), model, 5, 2, MCMCChains.Chains)\nfchain = prior_chain(Xoshiro(468), model, 5, 2, FlexiChains.VNChain)\nnothing # hide","title":"A1. Extracting scalar-valued samples"},{"category":"section","location":"migration/#MCMCChains","page":"Migrating from MCMCChains","text":"mchain[:x]\n\ngives you an niters × nchains AxisArray of samples for the variable x.","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains","page":"Migrating from MCMCChains","text":"It is recommended to index into the chain using VarNames because this is clearer:\n\nfchain[@varname(x)]\n\nThis will also give you an niters × nchains matrix of samples but it is a DimArray instead.\n\nYou can also use fchain[:x] as long as the Symbol can be unambiguously resolved to a key in the chain.","title":"FlexiChains"},{"category":"section","location":"migration/#A2.-Extracting-an-element-of-an-array-valued-sample","page":"Migrating from MCMCChains","text":"@model g() = x ~ filldist(Normal(), 2, 3)\nmodel = g()\nmchain = prior_chain(Xoshiro(468), model, 5, 2, MCMCChains.Chains)\nfchain = prior_chain(Xoshiro(468), model, 5, 2, FlexiChains.VNChain)\nnothing # hide","title":"A2. Extracting an element of an array-valued sample"},{"category":"section","location":"migration/#MCMCChains-2","page":"Migrating from MCMCChains","text":"With MCMCChains, the array x will have been split up into its scalar elements. You can access, for example:\n\nmchain[Symbol(\"x[1, 2]\")]\n\nwhich again gives you an niters × nchains AxisArray of samples for the variable x[1, 2].","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-2","page":"Migrating from MCMCChains","text":"With FlexiChains, the array x will be kept together as a single variable. There is no single variable in the chain that corresponds to x[1, 2]. However, you can still access the element x[1, 2] using VarNames.\n\nfchain[@varname(x[1, 2])]\n\n(In fact you can also use other indexing patterns, such as fchain[@varname(x[end])] to get the last element of x.)","title":"FlexiChains"},{"category":"section","location":"migration/#A3.-Extracting-array-valued-samples","page":"Migrating from MCMCChains","text":"Consider the same model as above, but suppose we want the entire array of x values.\n\n@model g() = x ~ filldist(Normal(), 2, 3)","title":"A3. Extracting array-valued samples"},{"category":"section","location":"migration/#MCMCChains-3","page":"Migrating from MCMCChains","text":"With MCMCChains, you have to use the group function to subset the chain to variables that begin with x:\n\nmchain_xonly = group(mchain, :x)\n\nThis gives you a Chains object, whose internal data has dimensions niters × nparams × nchains. Here, nparams is the number of scalar parameters that begin with x (in this case, 6). The internal data can be accessed by converting the Chains object to an array.\n\nTo get, for example, the first sample of x, you can then do:\n\nx_vec = Array(mchain_xonly[1, :, 1])\nx = reshape(x_vec, 2, 3)\n\nIf you want to get (for example) the mean of x, or an array of all x values, you will similarly need some combination of group + reshape or permutedims.","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-3","page":"Migrating from MCMCChains","text":"With FlexiChains, you can directly access the entire array of x values using VarNames.\n\nFor example,\n\nfchain[@varname(x)]\n\nreturns a DimArray of dimensions niters × nchains where each element is a 2 × 3 array of samples for x.\n\nTo get the first sample of x you can either index into the DimMatrix above (i.e., fchain[@varname(x)][1, 1]), or you can directly index with\n\nfchain[@varname(x), iter=1, chain=1]\n\nIf you need to process all x samples at once, you can also use\n\nfchain[@varname(x), stack=true]\n\nto get a DimArray of dimensions niters × nchains × 2 × 3, where the last two dimensions correspond to the dimensions of x.","title":"FlexiChains"},{"category":"section","location":"migration/#A4.-Subsetting-a-chain-to-specific-parameters,-iterations,-or-chains","page":"Migrating from MCMCChains","text":"@model function h()\n    x ~ Normal()\n    y ~ Normal()\n    z ~ Normal()\nend\nmodel = h()\nmchain = prior_chain(Xoshiro(468), model, 5, 2, MCMCChains.Chains)\nfchain = prior_chain(Xoshiro(468), model, 5, 2, FlexiChains.VNChain)\nnothing # hide","title":"A4. Subsetting a chain to specific parameters, iterations, or chains"},{"category":"section","location":"migration/#MCMCChains-4","page":"Migrating from MCMCChains","text":"MCMCChains allows you to subset a chain simply by indexing into it as if it were a 3D array of dimensions niters × nparams × nchains.\n\nThis means that if you want to drop the first 2 iterations, and only retain the parameters x and y, you can for example do:\n\nmchain_subset = mchain[3:end, [:x, :y], :]","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-4","page":"Migrating from MCMCChains","text":"With FlexiChains, parameter subsetting is done with a positional argument, but iteration and chain subsetting are done with keyword arguments.\n\nUnfortunately, Julia does not (yet?) support begin and end in keyword arguments, so you will have to explicitly calculate what end should be.\n\nn_iters = FlexiChains.niters(fchain)  # or equivalently size(fchain, 1)\nfchain_subset = fchain[[@varname(x), @varname(y)], iter=3:n_iters]","title":"FlexiChains"},{"category":"section","location":"migration/#A5.-Subsetting-the-chain-to-parameters-only","page":"Migrating from MCMCChains","text":"With MCMCChains you need to know that the parameters are stored in a section called :parameters, and extract that section specifically. (In general, a chain can have any section with any name, but Turing makes sure to create :parameters and :internals sections.)\n\nMCMCChains.get_sections(mchain, :parameters)\n\nFlexiChains has a strict notion of parameters and other keys, so there is a dedicated function for this:\n\nFlexiChains.subset_parameters(fchain)","title":"A5. Subsetting the chain to parameters only"},{"category":"section","location":"migration/#B1.-Extracting-summary-statistics","page":"Migrating from MCMCChains","text":"MCMCChains has a few functions which notionally can do the same thing:\n\nsummarystats(mchain)     # calculate summary stats\n\n# Also:\n# summarize(mchain)      # same as summarystats for the most part\n# describe(mchain)       # prints them but returns nothing\n\nFlexiChains only uses StatsBase.summarystats:\n\nsummarystats(fchain)\n\nBoth MCMCChains and FlexiChains accept keyword arguments which give you more control over which statistics are calculated. For FlexiChains please see the summarising docs for more info.","title":"B1. Extracting summary statistics"},{"category":"section","location":"migration/#B2.-Extracting-the-mean-of-a-scalar-valued-variable","page":"Migrating from MCMCChains","text":"This applies to any statistic, not just the mean.\n\nusing LinearAlgebra\n@model function k()\n    x ~ Normal()\n    y ~ MvNormal(zeros(2), I)\nend\nmodel = k()\nmchain = prior_chain(Xoshiro(468), model, 5, 2, MCMCChains.Chains)\nfchain = prior_chain(Xoshiro(468), model, 5, 2, FlexiChains.VNChain)\nnothing # hide","title":"B2. Extracting the mean of a scalar-valued variable"},{"category":"section","location":"migration/#MCMCChains-5","page":"Migrating from MCMCChains","text":"mmean = mean(mchain)\n\nThis returns a ChainDataFrame object, which is slightly confusing to work with (and isn't really documented at all). To actually access the mean value, you have to do\n\nmmean[:x, :mean]\n\nThere is a more direct way of getting the mean of a specific variable, which is to pass that as a second argument to mean:\n\nmean(mchain, :x)","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-5","page":"Migrating from MCMCChains","text":"There are two ways to get the mean of x with FlexiChains, which are essentially equivalent.\n\nThe first way is to the mean of the entire chain and index into it.\n\nfmean = mean(fchain)\n\nThis returns a FlexiSummary object, which can be indexed into with a VarName:\n\nfmean[@varname(x)]\n\nOr, perhaps more efficiently since this avoids calculating the mean of all other variables, you can index into the chain first to extract the variable x and then take the mean of that:\n\nfmean_x = mean(fchain[@varname(x)])","title":"FlexiChains"},{"category":"section","location":"migration/#B3.-Mean-of-an-array-valued-variable","page":"Migrating from MCMCChains","text":"","title":"B3. Mean of an array-valued variable"},{"category":"section","location":"migration/#MCMCChains-6","page":"Migrating from MCMCChains","text":"In the example above we had y ~ MvNormal(zeros(2), I), so y is a 2-dimensional array-valued variable. With MCMCChains, you have to first subset the chain to variables that begin with y using group, and then calculate the mean:\n\nmchain_yonly = group(mchain, :y)\nmmean_yonly = mean(mchain_yonly)\n\nWith this ChainDataFrame object in hand, you can index into it:\n\nmmean_yonly[:, :mean]","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-6","page":"Migrating from MCMCChains","text":"The same description applies to y as to x in the previous section, except that now y is an array-valued variable.\n\n# Make sure to avoid splitting `y` up.\nfmean = mean(fchain; split_varnames=false)\nfmean[@varname(y)]\n\nAlternatively, recall that fchain[@varname(y)] returns a DimArray of vectors, so you can take the mean of that directly:\n\nfmean_y = mean(fchain[@varname(y)])","title":"FlexiChains"},{"category":"section","location":"migration/#B4.-Accessing-the-mean-of-all-parameters","page":"Migrating from MCMCChains","text":"Suppose you just wanted mean(x), mean(y[1]), mean(y[2]) all at once as a single vector.","title":"B4. Accessing the mean of all parameters"},{"category":"section","location":"migration/#MCMCChains-7","page":"Migrating from MCMCChains","text":"With MCMCChains you can do this in several ways. One way is to first subset the chain to parameters only (see the next section), convert that to an array, and then take the mean across the iteration and chain dimensions. (Recall that with MCMCChains, the array layout is niters × nparams × nchains.)\n\nmchain_params = MCMCChains.get_sections(mchain, :parameters)\nmchain_params_array = Array(mchain_params)\nmmean_params = mean(mchain_params_array, dims=(1, 3))","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-7","page":"Migrating from MCMCChains","text":"With FlexiChains, the easiest way is to take the mean of the chain, and then convert that FlexiSummary object to an array. The array conversion retains only parameters by default, but this can be changed with the parameters_only keyword argument (see the docstring for more info).\n\nfmean = mean(fchain)\nDimArray(fmean)  # or `Array(fmean)` if you don't need dimensions","title":"FlexiChains"},{"category":"section","location":"migration/#B5.-Taking-the-per-chain-means","page":"Migrating from MCMCChains","text":"","title":"B5. Taking the per-chain means"},{"category":"section","location":"migration/#MCMCChains-8","page":"Migrating from MCMCChains","text":"mean(mchain, append_chains=false)\n\nThis returns a vector of ChainDataFrames, one per chain, which can be worked with as described above.","title":"MCMCChains"},{"category":"section","location":"migration/#FlexiChains-8","page":"Migrating from MCMCChains","text":"The syntax in FlexiChains is closer to that in base Julia, where you specify the dimensions you do want to reduce over:\n\nmean(fchain; dims=:iter)\n\nIf you want a flattened array you can likewise convert this to a DimArray, which will have dimensions chains × parameters:\n\nDimArray(mean(fchain; dims=:iter))","title":"FlexiChains"},{"category":"section","location":"migration/#C1.-Listing-all-parameters","page":"Migrating from MCMCChains","text":"MCMCChains.names(mchain, :parameters)\n\nSimilar to the above, FlexiChains has a dedicated function for this:\n\nFlexiChains.parameters(fchain)","title":"C1. Listing all parameters"},{"category":"section","location":"migration/#C2.-Listing-all-*keys*-(not-just-parameters)","page":"Migrating from MCMCChains","text":"MCMCChains.names(mchain)\n\nA FlexiChain is really a dictionary mapping keys to matrices, so you can just use Base.keys:\n\ncollect(keys(fchain))","title":"C2. Listing all keys (not just parameters)"},{"category":"section","location":"migration/#C3.-Getting-the-number-of-samples","page":"Migrating from MCMCChains","text":"size(mchain)  # niters × nparams × nchains\n\nsize(fchain)  # niters × nchains\n\nFor FlexiChains, since the parameters do not form an array dimension, it is not exposed as part of the 'size': however, if you want to get the number of parameters, you can do length(FlexiChains.parameters(fchain)), or length(keys(fchain)) if you want to count all keys, not just parameters.","title":"C3. Getting the number of samples"},{"category":"section","location":"migration/#D1.-get-and-get_params","page":"Migrating from MCMCChains","text":"MCMCChains.get and MCMCChains.get_params allow you to extract a NamedTuple mapping parameter names to their samples. This is needed because MCMCChains stores samples as a 3D array.\n\nFor example:\n\np = MCMCChains.get_params(mchain)\np.x     # all samples for x\n\nWith FlexiChains these functions are no longer needed since indexing into the chain with VarNames already gives you the samples in the correct format.\n\nfchain[@varname(x)]","title":"D1. get and get_params"},{"category":"section","location":"migration/#E1.-DynamicPPL.predict","page":"Migrating from MCMCChains","text":"By default FlexiChains returns a new chain that includes both the new predicted variables, as well as the original variables from the input chain. To disable this use predict(...; include_all=false).\n\nMCMCChains's default is include_all=false, so if you do want FlexiChains's behaviour, you can likewise just pass include_all=true to it.","title":"E1. DynamicPPL.predict"},{"category":"section","location":"migration/#E2.-Final-sampler-states","page":"Migrating from MCMCChains","text":"note: Note\nThe code examples here are not run since they require Turing.\n\nWhen sampling a single chain with Turing + MCMCChains, if you specify save_state=true, the final sampler state will be bundled inside the chain. Importantly, this is stored as a single sampler state object. This means that if you want to resume sampling from this state, you can do something like\n\nlast_state = Turing.loadstate(mchain)\nresume_chain = sample(model, sampler, N; initial_state=last_state)\n\nWith FlexiChains, note that the final sampler state is always stored as a vector of sampler states, one per chain.\n\nThat means you need to extract the first (or whichever) sampler state from this vector before passing it to sample:\n\nlast_states = Turing.loadstate(fchain)\nresume_chain = sample(model, sampler, N; initial_state=last_states[1])\n\nIf you instead sample multiple chains and save its state, the saved state will always be a vector of sampler states, even with MCMCChains.","title":"E2. Final sampler states"},{"category":"section","location":"migration/#F1.-Plotting","page":"Migrating from MCMCChains","text":"Please see the plotting docs for information on what plotting functionality is available in FlexiChains.\n\n@model function q()\n    x ~ Normal()\n    y ~ MvNormal(zeros(2), I)\n    z ~ Normal()\nend\nmodel = q()\nmchain = prior_chain(Xoshiro(468), model, 5, 2, MCMCChains.Chains)\nfchain = prior_chain(Xoshiro(468), model, 5, 2, FlexiChains.VNChain)\nnothing # hide\n\nPlotting functions in FlexiChains all allow you to pass a collection of parameters as a second, optional argument. Thus, for example, if you wanted to plot only y, then you could do:\n\nusing StatsPlots\nplot(fchain, [@varname(y)])\nsavefig(\"fchainplot.svg\"); nothing # hide\n\n(Image: FlexiChains plots)\n\nWith MCMCChains you would probably be best off subsetting the chain to variables that begin with y first, and then plotting that:\n\nmchain_yonly = group(mchain, :y)\nplot(mchain_yonly)\nsavefig(\"mchainplot.svg\"); nothing # hide\n\n(Image: MCMCChains plots)","title":"F1. Plotting"},{"category":"section","location":"integrations/#Integrations-with-other-packages","page":"Integrations with other packages","text":"FlexiChains is most obviously tied to the Turing.jl ecosystem, as described on the previous pages. However, it also contains some useful links to other packages (listed here in alphabetical order).","title":"Integrations with other packages"},{"category":"section","location":"integrations/#(Your-custom-sampler-here...!)","page":"Integrations with other packages","text":"If you have written a sampler that conforms to the AbstractMCMC interface, then you can get 'free' support for bundling into a FlexiChain{VarName} and a FlexiChain{Symbol} respectively by overloading these functions:\n\nAs a concrete example, let's use the following setup:\n\nusing FlexiChains: FlexiChains, FlexiChain\nusing AbstractMCMC\n\nstruct M <: AbstractMCMC.AbstractModel end\nstruct S <: AbstractMCMC.AbstractSampler end\nstruct T end\nfunction AbstractMCMC.step(rng, ::M, ::S, state=nothing; kwargs...)\n    T(), nothing\nend\n\nHere T represents what AbstractMCMC calls a 'transition': it is the first return value of AbstractMCMC.step. Of course, in practice, your transition will carry more information than this.\n\nNow suppose you want to sample with this and obtain a FlexiChain{Symbol}. As the docstrings above allude to, you will want to overload to_nt_and_stats:\n\nFlexiChains.to_nt_and_stats(::T) = ((;hello=1.0), (;world=2.0))\n\nThen you can do\n\nsample(M(), S(), 10; chain_type=FlexiChain{Symbol})\n\nLikewise for VarNamedTuple (although in this case, you should think carefully about whether you really need VarNamedTuple: if your sampler transition does not carry enough information to justify the richer data type, then it would probably be more meaningful to stick to NamedTuple and FlexiChain{Symbol}).\n\nusing DynamicPPL: VarNamedTuple, VarName\nFlexiChains.to_vnt_and_stats(::T) = (VarNamedTuple(hello=1.0), (;world=2.0))\nsample(M(), S(), 10; chain_type=FlexiChain{VarName})","title":"(Your custom sampler here...!)"},{"category":"section","location":"integrations/#integrations-advancedhmc","page":"Integrations with other packages","text":"Documentation for AdvancedHMC.jl\n\nFlexiChains.to_nt_and_stats is overloaded for AdvancedHMC.Transition, so you can sample with AdvancedHMC into a FlexiChain{Symbol}. This is a slightly simplified version of the example in the AdvancedHMC README (here we use an analytical gradient rather than automatic differentiation):\n\nusing AdvancedHMC, AbstractMCMC\nusing LogDensityProblems\nusing FlexiChains: FlexiChain\n\n# Set up AD-aware log-density function\nstruct LogTargetDensity\n    dim::Int\nend\nLogDensityProblems.logdensity(::LogTargetDensity, θ) = -sum(abs2, θ) / 2\nLogDensityProblems.logdensity_and_gradient(::LogTargetDensity, θ) = (-sum(abs2, θ) / 2, -θ)\nLogDensityProblems.dimension(p::LogTargetDensity) = p.dim\nfunction LogDensityProblems.capabilities(::Type{LogTargetDensity})\n    return LogDensityProblems.LogDensityOrder{1}()\nend\n\nchn = AbstractMCMC.sample(\n    LogTargetDensity(10), AdvancedHMC.NUTS(0.8), 20;\n    n_adapts=10, chain_type=FlexiChain{Symbol},\n)","title":"AdvancedHMC.jl"},{"category":"section","location":"integrations/#ArviZ.jl","page":"Integrations with other packages","text":"Documentation for ArviZ.jl\n\nFlexiChains contains an extension that allows you to convert a FlexiChain into an InferenceObjects.InferenceData (InferenceObjects.jl is one of the sublibraries of ArviZ.jl, and is re-exported by ArviZ).\n\nFor example:\n\nusing InferenceObjects, FlexiChains, DynamicPPL, Distributions\n\n@model function f(y)\n    x ~ Normal()\n    y ~ Normal(x)\nend\nmodel = f(1.0)\n\nchn = FlexiChains._make_prior_chain(model, 100, 2)\nidata = InferenceObjects.convert_to_inference_data(chn)\n\nYou can combine multiple InferenceData objects with merge:\n\nllike_chn = DynamicPPL.pointwise_loglikelihoods(model, chn)\nidata2 = InferenceObjects.convert_to_inference_data(llike_chn; group=:log_likelihood)\nidata_merged = merge(idata, idata2)\n\nFrom here you can use the full functionality of ArviZ.jl, which includes various plotting and analysis tools: please see the ArviZ.jl documentation for more info.","title":"ArviZ.jl"},{"category":"section","location":"integrations/#DimensionalDistributions.jl","page":"Integrations with other packages","text":"Documentation for DimensionalDistributions.jl\n\nnote: Note\nDimensionalDistributions.jl is not yet registered in the Julia package registry; at present you will need to install it from GitHub using ]add https://github.com/sethaxen/DimensionalDistributions.jl.\n\nDimensionalDistributions.jl provides a withdims wrapper which lets you create a distribution that returns DimVectors:\n\nusing Turing # reexports MvNormal and I\nusing DimensionalData: Dim\nusing DimensionalDistributions\n\nschool_dim = Dim{:school}([:a, :b, :c])\ndim_mvnormal = withdims(MvNormal(zeros(3), I), school_dim)\nrand(dim_mvnormal)\n\nIf you use this in a Turing model, then this information will be carried through all the way to FlexiChains, and indexing into this parameter will let you get a DimArray of DimVectors. This leads to a particularly elegant outcome when accessing this parameter with the stack=true keyword argument: FlexiChains will return a 3-dimensional DimArray with full dimensional information retained.\n\nusing FlexiChains\n@model f() = x ~ dim_mvnormal\nchn2 = sample(f(), MH(), MCMCThreads(), 5, 2; chain_type=VNChain, progress=false)\nchn2[@varname(x), stack=true]\n\nnote: Default behaviour\nFor DimArray-valued parameters, the stack=true keyword argument is not necessary in the current version of FlexiChains as stacking happens by default. However in a future version this will be changed such that the default behaviour even for DimArrays is to not stack. Thus it is recommended that you explicitly specify stack=true if you want this behaviour.\n\nSub-VarName indexing also works.\n\nchn2[@varname(x[1])]\n\nIn principle, you should be able to even use DimensionalData selectors in the VarName, e.g. chn2[@varname(x[At(:b)])]; however, support for this is slightly flaky due to incomplete implementations of Base.checkbounds for DimensionalData (which is not something that FlexiChains can control). If you try this and find that something doesn't work, please do feel free to open an issue and we can help to upstream it.","title":"DimensionalDistributions.jl"},{"category":"section","location":"integrations/#JLD2.jl","page":"Integrations with other packages","text":"Documentation for JLD2.jl\n\nFlexiChain and FlexiSummary objects can be serialised with JLD2.jl with no fuss. See the Serialization section below for an example.","title":"JLD2.jl"},{"category":"section","location":"integrations/#integrations-pairplots","page":"Integrations with other packages","text":"Documentation for PairPlots.jl\n\nFlexiChains provides two ways to interact with PairPlots.jl.\n\nFirstly, you can directly call pairplot(chn[, param_or_params]) on a FlexiChain. This is a convenience method which includes extra functionality for e.g. highlighting divergent transitions.\n\nusing PairPlots, FlexiChains, Turing, CairoMakie\n\nJ = 8\ny = [28, 8, -3, 7, -1, 1, 18, 12]\nsigma = [15, 10, 16, 11, 9, 11, 10, 18]\n@model function eightsch(J, y, sigma)\n    mu ~ Normal(0, 5)\n    tau ~ truncated(Cauchy(0, 5); lower=0)\n    theta ~ MvNormal(fill(mu, J), tau^2 * I)\n    for i in 1:J\n        y[i] ~ Normal(theta[i], sigma[i])\n    end\nend\nmodel = eightsch(J, y, sigma)\nchn = sample(model, NUTS(), MCMCSerial(), 2000, 4; chain_type=VNChain, progress=false, verbose=false)\n\n# Limit the parameters being plotted for readability\nvns = [@varname(tau), @varname(theta[1]), @varname(theta[2])]\n\n# For Turing.jl HMC/NUTS chains, the Boolean indicating whether or not the\n# transition was divergent is stored in the `:numerical_error` key. If you\n# have a different sampler you just need to specify the appropriate key.\npairplot(chn, vns; divergences=:numerical_error)\nMakie.save(\"pairplot.png\", ans); # hide\n\n(Image: Pair plot of the sampled chain)\n\nFor more low-level control, you can also convert a FlexiChain into a PairPlots.Series, so that you can combine it with other data or fixed values in custom plots.","title":"PairPlots.jl"},{"category":"section","location":"integrations/#PosteriorDB.jl","page":"Integrations with other packages","text":"Documentation for PosteriorDB.jl\n\nIf you have loaded a PosteriorDB.ReferencePosterior, you can transform it into a FlexiChain using FlexiChains.from_posteriordb_ref:\n\nusing PosteriorDB, FlexiChains\n\npdb = PosteriorDB.database()\npost = PosteriorDB.posterior(pdb, \"eight_schools-eight_schools_centered\")\nref = PosteriorDB.reference_posterior(post)\n\nchn = FlexiChains.from_posteriordb_ref(ref)\n\nYou can then use all the functionality of FlexiChains on chn:\n\nsummarystats(chn)","title":"PosteriorDB.jl"},{"category":"section","location":"integrations/#integrations-posteriorstats","page":"Integrations with other packages","text":"Documentation for PosteriorStats.jl\n\nPosteriorStats.jl provides the hdi and eti functions for computing highest density intervals and equal-tailed intervals, respectively. These are overloaded for FlexiChain objects in much the same way as Statistics.mean, Statistics.std, etc. (see the Summarising page for more information). Here is an example:\n\nusing FlexiChains, DynamicPPL, Distributions, PosteriorStats\n\n@model function g(z)\n    x ~ Normal()\n    y ~ Normal(x)\n    z ~ Normal(y)\nend\nmodel = g(1.0)\n\nchn = FlexiChains._make_prior_chain(model, 100, 2)\nPosteriorStats.hdi(chn; prob=0.95, split_interval=true)\n\nPosteriorStats.loo, which computes Pareto-smoothed importance sampling leave-one-out cross-validation (PSIS-LOO), is also overloaded for FlexiChain objects. You can either pass a chain of log-likelihood values, which can be computed via DynamicPPL.pointwise_loglikelihoods(model, chain), or (perhaps more easily) a Turing model plus a posterior chain which just does that for you. For example (although note that the chain above is sampled from the prior — so this is only meant to be a demonstration of the API):\n\nPosteriorStats.loo(model, chn)","title":"PosteriorStats.jl"},{"category":"section","location":"integrations/#integrations-serialization","page":"Integrations with other packages","text":"Calling this an 'integration' is a bit of a stretch, because it simply works out of the box (no extra code needed), but it had to be documented somewhere...\n\nYou can serialise and deserialise FlexiChain and FlexiSummary objects using the Serialization.jl standard library.\n\nusing FlexiChains, Turing, Serialization\n\n@model f() = x ~ Normal()\nchn = sample(f(), NUTS(), 10; chain_type=VNChain, progress=false)\nfname = \"mychain\"\nserialize(fname, chn)\n\nchn2 = deserialize(fname)\nisequal(chn, chn2)\n\nSerialisation with JLD2.jl works as well:\n\nusing JLD2\n\nfname, key = \"chain.jld2\", \"chain\"\nsave(fname, Dict(key => chn))\nchn2 = load(fname, key)\n\nisequal(chn, chn2)\n\nNote two things:\n\nIf the serialisation and deserialisation steps are performed in different Julia sessions, you need to make sure that you have all necessary packages loaded in the second session. In particular, if you use save_state=true, then you should load Turing.jl before deserialising, because Turing provides the sampler state types.\nIf you are testing the integrity of (de)serialisation, you may find that isequal() on sampler state types may not return true even when the sampler states are the same. This is because Julia's default definition of equality for structs is based on object identity, not on field values. For example, the following returns false because [1] !== [1].\nstruct Foo{T}\n    t::T\nend\n\nFoo([1]) == Foo([1])","title":"Serialization.jl"},{"category":"section","location":"integrations/#Stan","page":"Integrations with other packages","text":"FlexiChains has a function to read in chains stored in Stan CSV format:\n\nThis of course assumes that you already have the CSV files on disk somewhere, e.g. if you have run Stan externally.\n\nIf you are using StanSample.jl you can also read data directly from a SampleModel once you have sampled with it.\n\nnote: Note\nThis code block is not run, as running it would necessitate setting up CmdStan on GitHub runners.\n\nusing StanSample, FlexiChains\n\nsm = SampleModel(...)\n# Note: this mutates `sm` -- StanSample has a rather unconventional interface\nstan_sample(sm; data) \n\n# The key type has to be `Symbol`.\nchn = FlexiChain{Symbol}(sm)\n\nIf you are using BridgeStan.jl and/or StanLogDensityProblems.jl plus a native Julia sampler, you should check whether there is an integration for the sampler in question. For example if you use AdvancedHMC.jl then you can pass the chain_type=FlexiChain{Symbol} keyword argument (see above).\n\n(If the sampler in question does not have an integration with FlexiChains, please feel free to open an issue!)","title":"Stan"},{"category":"section","location":"integrations/#integrations-tables","page":"Integrations with other packages","text":"Documentation for Tables.jl\n\nFlexiChains implements a Tables.jl interface which allows you to easily convert a FlexiChain or FlexiSummary into any type that consumes tabular data, e.g., a DataFrame.\n\nIn fact, FlexiChains implements two different Tables.jl interfaces for chains: one for wide data and one for long data.\n\nThis is best demonstrated with an example. First let's sample a chain as usual:\n\nusing FlexiChains, Turing, DataFrames\n\n@model function f()\n    x ~ Normal(10.0)\n    y ~ Bernoulli()\n    z ~ MvNormal(zeros(2), I)\nend\n\nchn = sample(f(), MH(), MCMCThreads(), 4, 2; chain_type=VNChain, progress=false)\n\nNow we can convert this into a DataFrame in wide format (this is also the default for unwrapped FlexiChains, so you technically don't have to wrap chn in Wide if you don't want to):\n\nDataFrame(Wide(chn))\n\nor long format (although notice that this will promote y to Float64):\n\nDataFrame(Long(chn))\n\nBoth the Wide and Long wrapper structs accept keyword arguments which determine whether array-valued parameters (like z) are split up, and whether or not to include the Extra keys in the chain as well, like Turing log-probabilities.\n\nFor FlexiSummary, the long format is not (yet?) supported: only the wide format is implemented. In contrast to Wide(::FlexiChain), where each parameter is given a different column, the wide format for FlexiSummary splits each statistic into a separate column.\n\nfs = summarystats(chn)\nDataFrame(Wide(fs))\n\nLike for FlexiChain, the Wide wrapper is the default Tables.jl implementation for FlexiSummary, so you can also just do DataFrame(fs).\n\nWide(::FlexiSummary) takes the same keyword arguments as Wide(::FlexiChain).\n\nnote: Splitting VarNames\nPlease note that the act of summarising will typically already cause array-valued variables to be split up. If this has already been done, then using Wide(...; split_varnames=false) cannot reverse this!w = Wide(mean(chn), split_varnames=false)\nDataFrame(w)Notice how z[1] and z[2] are already split up despite the split_varnames=false argument. If you want to prevent this, you need to specify split_varnames=false at the summary step as well:w = Wide(mean(chn; split_varnames=false), split_varnames=false)\nDataFrame(w)","title":"Tables.jl"},{"category":"function","location":"integrations/#FlexiChains.to_vnt_and_stats","page":"Integrations with other packages","text":"to_vnt_and_stats(transition)::Tuple{VarNamedTuple,NamedTuple}\n\nConvert the first output (i.e. the 'transition') of an AbstractMCMC sampler into a VarNamedTuple mapping parameter names to their values, plus a NamedTuple of any additional statistics.\n\nThe VarNamedTuple will be converted into Parameter keys, and the NamedTuple into Extra keys.\n\nIf you are writing a custom AbstractMCMC sampler and want to allow users to collect the samples as a FlexiChain{VarName}, i.e., \n\nsample(...; chain_type=FlexiChain{VarName})\n\nthen you should ensure that your method of AbstractMCMC.step returns a transition that can be passed to this method. (The other return value, the state, is not relevant.)\n\nNote that this method is already implemented for DynamicPPL.VarNamedTuple (in which case the stats are empty) as well as DynamicPPL.ParamsWithStats. Thus the easiest solution is to just return one of those types.\n\n\n\n\n\n","title":"FlexiChains.to_vnt_and_stats"},{"category":"function","location":"integrations/#FlexiChains.to_nt_and_stats","page":"Integrations with other packages","text":"to_nt_and_stats(transition)::Tuple{NamedTuple,NamedTuple}\n\nConvert the first output (i.e. the 'transition') of an AbstractMCMC sampler into a NamedTuple mapping parameter names to their values, plus a NamedTuple of any additional statistics.\n\nThe first NamedTuple will be converted into Parameter keys, and the second NamedTuple into Extra keys.\n\nIf you are writing a custom AbstractMCMC sampler and want to allow users to collect the samples as a FlexiChain{Symbol}, i.e., \n\nsample(...; chain_type=FlexiChain{Symbol})\n\nthen you should ensure that your method of AbstractMCMC.step returns a transition that can be passed to this method. (The other return value, the state, is not relevant.)\n\n\n\n\n\n","title":"FlexiChains.to_nt_and_stats"},{"category":"function","location":"integrations/#InferenceObjects.convert_to_inference_data","page":"Integrations with other packages","text":"InferenceObjects.convert_to_inference_data(\n    chain::FlexiChain{<:TKey}; group::Symbol = posterior, kwargs...\n) where {TKey}\n\nConvert a FlexiChain to an InferenceObjects.InferenceData object.\n\nThe group keyword argument specifies the group name for the chain's parameters. The chain's extras are assigned to :sample_stats if group is :posterior, and to :sample_stats_<group> otherwise.\n\nOther keyword arguments are passed to InferenceObjects.convert_to_dataset.\n\n\n\n\n\n","title":"InferenceObjects.convert_to_inference_data"},{"category":"function","location":"integrations/#PairPlots.pairplot","page":"Integrations with other packages","text":"PairPlots.pairplot(\n    chn::FlexiChain[, param_or_params];\n    args::Tuple=(),\n    pool_chains::Bool=false,\n    divergences=nothing,\n    divergences_kwargs::NamedTuple=(; markersize=3, color=:red),\n    kwargs...\n)\n\nCreate a pair plot for the given chain. Note that PairPlots.jl uses Makie.jl as its plotting backend, so you will additionally need to load a Makie backend (e.g. with using GLMakie or using CairoMakie) before calling this function.\n\nIf no parameters are specified, this will plot all parameters in the chain. Note that non-parameter, i.e. Extra, keys are excluded by default. If you want to plot all keys, you can explicitly pass all keys with pairplot(chn, :).\n\nKeyword arguments\n\nargs: a tuple of additional arguments to pass to PairPlots.pairplot. This can be used to specify additional series to plot, for example.\npool_chains: controls whether to pool all chains together into a single series, or to plot each chain separately.\ndivergences: specifies a key name in the chain that contains a boolean array indicating which samples are divergences. If provided, divergent samples will be highlighted in the plot. Note that for HMC/NUTS chains sampled with Turing.jl, the key name for divergences is (as of Turing v0.43) :numerical_error. By default this is nothing, which disables plotting of divergences (because not all chains will have this information).\ndivergences_kwargs: a NamedTuple of keyword arguments which is eventually passed to Makie.scatter, which can be used to control the appearance of the points. Defaults to (; markersize=3, color=:red).\n\nOther keyword arguments are passed to PairPlots.pairplot.\n\n\n\n\n\n","title":"PairPlots.pairplot"},{"category":"type","location":"integrations/#PairPlots.Series","page":"Integrations with other packages","text":"PairPlots.Series(chn::FlexiChain; split_varnames=true, kwargs...)\n\nCreate a PairPlots.Series from the given FlexiChain. The series data will contain one column for each key in the chain.\n\nNote that this function will include all keys in the chain, including Extra keys. If you only want to include a subset of keys, you should first subset the chain, for example with chn[[key1, key2, ...]], and then pass that subsetted chain in.\n\nIf split_varnames is true, then parameters in the chain will be split into their constituent real-valued elements. This is necessary for plotting. In practice there should never be any reason to set this to false, unless you already know that your chain contains only scalar variables and you want to avoid the cost of splitting the variable names again.\n\n\n\n\n\n","title":"PairPlots.Series"},{"category":"function","location":"integrations/#FlexiChains.from_posteriordb_ref","page":"Integrations with other packages","text":"FlexiChains.from_posteriordb_ref(\n    ref::PosteriorDB.ReferencePosterior\n)::FlexiChain{String}\n\nLoad a PosteriorDB.ReferencePosterior into a FlexiChain. The keys are stored as strings, which matches the storage format in PosteriorDB.jl.\n\n\n\n\n\n","title":"FlexiChains.from_posteriordb_ref"},{"category":"method","location":"integrations/#PosteriorStats.hdi-Tuple{FlexiChains.FlexiChain}","page":"Integrations with other packages","text":"PosteriorStats.hdi(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the highest density interval across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to PosteriorStats.hdi; please see its documentation for details of supported keyword arguments.\n\nSplitting intervals\n\nThe split_interval keyword argument, if set to true, causes the output FlexiSummary to have one statistic column per interval bound, i.e., hdi_lower and hdi_upper. Note that this will only be valid if you request a single interval (i.e., method=:unimodal, which is the default in PosteriorStats.jl).\n\nIf you specify method=:multimodal, the returned FlexiSummary will have a single statistic column named hdi that contains a vector of intervals, and the split_interval argument will be ignored.\n\n\n\n\n\n","title":"PosteriorStats.hdi"},{"category":"method","location":"integrations/#PosteriorStats.eti-Tuple{FlexiChains.FlexiChain}","page":"Integrations with other packages","text":"PosteriorStats.eti(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the equal-tailed interval across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to PosteriorStats.eti; please see its documentation for details of supported keyword arguments.\n\nSplitting intervals\n\nThe split_interval keyword argument, if set to true, causes the output FlexiSummary to have one statistic column per interval bound, i.e., eti_lower and eti_upper.\n\n\n\n\n\n","title":"PosteriorStats.eti"},{"category":"method","location":"integrations/#PosteriorStats.loo-Tuple{FlexiChains.FlexiChain}","page":"Integrations with other packages","text":"PosteriorStats.loo(chn::FlexiChains; kwargs...)\n\nCalculates the leave-one-out cross-validation (LOO) statistic for a FlexiChain object. The chain must contain only log-likelihood values, and they must all be stored as parameters (not extras). Parameters that map to arrays of log-likelihood values are supported: they will be flattened before being passed to PosteriorStats.loo.\n\nReturns a struct with the following fields:\n\nparam_names::Vector: A vector of parameter names whose log-likelihood values were used.\nloo::PosteriorStats.PSISLOOResult: The return value of PosteriorStats.loo applied to the log-likelihood values extracted from the FlexiChain. This contains the statistics of interest.\n\nAdditional keyword arguments are forwarded to PosteriorStats.loo.\n\n\n\n\n\n","title":"PosteriorStats.loo"},{"category":"method","location":"integrations/#PosteriorStats.loo-Tuple{DynamicPPL.Model, FlexiChains.FlexiChain}","page":"Integrations with other packages","text":"PosteriorStats.loo(\n    model::DynamicPPL.Model,\n    posterior_chn::FlexiChains;\n    factorize::Bool=false,\n    kwargs...\n)\n\nCalculates the leave-one-out cross-validation (LOO) statistic, given a model plus a posterior chain. This first uses the model and posterior chain to calculate pointwise log-likelihoods, and then uses those to calculate the LOO statistic.\n\nReturns a struct with the following fields:\n\nparam_names::Vector: A vector of parameter names whose log-likelihood values were used.\nloo::PosteriorStats.PSISLOOResult: The return value of PosteriorStats.loo applied to the log-likelihood values extracted from the FlexiChain. This contains the statistics of interest.\n\nThe factorize keyword argument is passed to DynamicPPL.pointwise_loglikelihoods. If factorize=true, factorised log-densities will be calculated for distributions that can be partitioned into blocks (e.g. MvNormal). Please see the docstring of DynamicPPL.pointwise_loglikelihoods for more details.\n\nAdditional keyword arguments are forwarded to PosteriorStats.loo.\n\n\n\n\n\n","title":"PosteriorStats.loo"},{"category":"function","location":"integrations/#FlexiChains.from_stan_csv","page":"Integrations with other packages","text":"FlexiChains.from_stan_csv(\n    base_path::AbstractString, num_chains::Integer\n)::FlexiChain{Symbol}\n\nReads the Stan CSV files from {base_path}_1.csv through to {base_path}_{num_chains}.csv. This is a convenience method that recognises the fact that CmdStan saves its outputs in this format.\n\nnote: Note\nYou do not need to provide the underscore before the chain number in the filename! That is, if your files are example_1.csv etc., then base_path should just be \"example\".\n\n\n\n\n\nFlexiChains.from_stan_csv(\n    csv_paths::AbstractVector{<:AbstractString}\n)::FlexiChain{Symbol}\n\nParse a set of Stan CSV files at the given paths.\n\nThese files must correspond to a set of compatible chains, i.e., their parameter names, lengths (i.e., number of iterations), and other data should be consistent. This will be the case if they were all drawn from the same call to Stan's sampling. However, note that FlexiChains does only a rudimentary set of checks to ensure consistency: the user should be responsible for ensuring that the CSVs read in are valid.\n\nColumns that end in double-underscores (__) are treated as Extras (the underscores will be stripped), and all other columns as Parameters.\n\n\n\n\n\n","title":"FlexiChains.from_stan_csv"},{"category":"type","location":"integrations/#FlexiChains.Wide","page":"Integrations with other packages","text":"FlexiChains.Wide(\n    chn::Union{<:FlexiChain,<:FlexiSummary};\n    split_varnames::Bool=true,\n    parameters_only::Bool=true\n)\n\nA wrapper struct indicating a 'wide' table format. The exact meaning depends on whether the input is a FlexiChain or a FlexiSummary. A FlexiChain will have each parameter in a separate column; conversely, a FlexiSummary will have each summary statistic in a separate column.\n\nExample (FlexiChain)\n\nusing Turing, FlexiChains, DataFrames\n\n@model function f()\n    x ~ Normal()\n    b ~ Bernoulli()\nend\nchn = sample(f(), Prior(), MCMCThreads(), 10, 2; chain_type=VNChain)\n\ndf = DataFrame(Wide(chn))\n\nreturns a DataFrame that looks like the following. Each parameter is a different column, and the iter and chain dimensions are represented as separate columns as well; iter varies faster than chain.\n\nnote: Note\nBecause all parameter names must be converted to Symbol for column names, this may lead to clashes between e.g. parameters and extras which convert to the same Symbol. FlexiChains will error in such a situation.\n\n20×4 DataFrame\n Row │ iter   chain  x          b\n     │ Int64  Int64  Float64    Bool\n─────┼────────────────────────────────\n   1 │     1      1  -1.38809   false\n   2 │     2      1  -0.511805   true\n   3 │     3      1  -1.37277   false\n  ⋮  │   ⋮      ⋮        ⋮        ⋮\n  18 │     8      2   2.31312   false\n  19 │     9      2   1.58254    true\n  20 │    10      2  -1.14516   false\n\nKeyword arguments\n\nsplit_varnames: whether to split array-valued parameters into scalar leaves. If true (the default), then array-valued parameters are split into scalar leaves, e.g. a vector-valued parameter x would be split into x[1], x[2], etc.\nparameters_only: whether to include only parameters (and skip extras) in the resulting table. Defaults to true.\n\nExample (FlexiSummary)\n\njulia> df = DataFrame(Wide(summarystats(chn)))\n2×10 DataFrame\n Row │ param     mean       std       mcse      ess_bulk  ess_tail  rh ⋯\n     │ VarName…  Float64    Float64   Float64   Float64   Float64   Fl ⋯\n─────┼──────────────────────────────────────────────────────────────────\n   1 │ x         -0.253812  0.987327  0.193554   26.0206    25.641     ⋯\n   2 │ b          0.5       0.512989  0.113426   20.4545   NaN      Na\n                                                       4 columns omitted\n\njulia> df = DataFrame(Wide(mean(chn)))\n2×2 DataFrame\n Row │ param     stat\n     │ VarName…  Float64\n─────┼─────────────────────\n   1 │ x         -0.253812\n   2 │ b          0.5\n\n\n\n\n\n","title":"FlexiChains.Wide"},{"category":"type","location":"integrations/#FlexiChains.Long","page":"Integrations with other packages","text":"FlexiChains.Long(\n    chn::FlexiChain;\n    split_varnames::Bool=true,\n    parameters_only::Bool=true\n)\n\nA wrapper struct indicating that a FlexiChain should be converted to a 'long' table format, where all values are stacked into a single column, and there is an additional column indicating the parameter name.\n\nnote: Note\nBecause all parameter values are stacked into a single column, note that the resulting element type of the value column will be the common supertype of all parameter values. This can cause data to be promoted to a type that is not the same as its original type. For example, the b parameter below is converted to Float64.\n\nExample\n\nusing Turing, FlexiChains, DataFrames\n\n@model function f()\n    x ~ Normal()\n    b ~ Bernoulli()\nend\nchn = sample(f(), Prior(), MCMCThreads(), 10, 2; chain_type=VNChain)\n\ndf = DataFrame(Long(chn))\n\nreturns a DataFrame that looks like the following. The iter and chain dimensions are represented as separate columns as before, but now the parameter names are stacked into a single param column, and the values are stacked into a single value column.\n\nThe iter column varies faster than the chain column, which in turn varies faster than the param column.\n\n40×4 DataFrame\n Row │ iter   chain  param     value\n     │ Int64  Int64  VarName…  Float64\n─────┼─────────────────────────────────\n   1 │     1      1  x         -1.38809\n   2 │     2      1  x         -0.511805\n   3 │     3      1  x         -1.37277\n  ⋮  │   ⋮      ⋮      ⋮         ⋮\n  38 │     8      2  b          0.0\n  39 │     9      2  b          1.0\n  40 │    10      2  b          0.0\n\nKeyword arguments\n\nsplit_varnames: whether to split array-valued parameters into scalar leaves. If true (the default), then array-valued parameters are split into scalar leaves, e.g. a vector-valued parameter x would be split into x[1], x[2], etc.\nparameters_only: whether to include only parameters (and skip extras) in the resulting table. Defaults to true.\n\nAdditionally, when parameters_only=true (the default), the Parameter wrapper is stripped from keys. Otherwise, the Parameter/Extra wrappers are retained. If you want to unwrap them, you can use FlexiChains.get_name on the param column of the resulting table.\n\n\n\n\n\n","title":"FlexiChains.Long"},{"category":"section","location":"whynot/#Why-not-FlexiChains?","page":"Why not FlexiChains?","text":"Since I wrote one whole page saying why you should use FlexiChains, here are the counterarguments, or at least those I can think of.","title":"Why not FlexiChains?"},{"category":"section","location":"whynot/#Performance-and-type-stability","page":"Why not FlexiChains?","text":"FlexiChains uses dictionaries as its internal storage, and it is fundamentally not type stable to index into a dictionary with an abstract value type. Consequently, the vast majority of operations in FlexiChains are not type stable. They are potentially also slower than equivalent operations in MCMCChains.\n\nPersonally, I consider this to be really unimportant, because chain manipulation and data access are hardly performance bottlenecks in a typical Bayesian workflow. If you find an instance where FlexiChains is unbearably slow, please do open an issue.\n\nNote that when interfacing with Turing models, FlexiChains is typically faster.","title":"Performance and type stability"},{"category":"section","location":"whynot/#Feature-set","page":"Why not FlexiChains?","text":"MCMCChains probably still has more plotting and statistics functions available, even though FlexiChains is catching up quite rapidly. This is mainly because MCMCChains has been around for longer, and has had more contributors.\n\nI would be very happy to accept PRs porting some of this functionality to FlexiChains!\n\nIn the meantime, you can always convert your FlexiChain to a DataFrame, or an MCMCChains.Chains if you really need to.","title":"Feature set"},{"category":"section","location":"whynot/#Interface-stability","page":"Why not FlexiChains?","text":"FlexiChains is still quite young, development is happening quite rapidly, and thus its interface may not be fully stable. On the other hand MCMCChains is largely stable (although you may consider a lack of development to be a drawback).\n\nMy aim is to release a version 1.0 as soon as possible. (In general, I strongly subscribe to the view that packages that are used by the public should release 1.0 as soon as possible: see this issue.) My preconditions for FlexiChains 1.0 are twofold:\n\nI am happy with the core design and APIs of the package. For example, I don't really care about which statistic functions are implemented, but I do care that they return sensible data structures.\nThe package has been tested by a people in the wild, to catch any obvious gaps or drawbacks.\n\nAs of April 2026, I think that (1) is already satisfied: the data structures and APIs are pretty much where I want them to be, and I expect that future changes will mostly be addition of new functionality rather than breaking changes. However, I am still waiting for (2) to really happen, and I would like to see more people using the package and giving feedback before I release 1.0.","title":"Interface stability"},{"category":"section","location":"api/#Public-API","page":"Public API","text":"The types and functions documented on this page form part of FlexiChains's public API.\n\nOn Julia 1.11 and later, these are marked with the public keyword (or are extensions of other packages' APIs).\n\nFlexiChains also guarantees that any changes to these APIs will be reflected with a breaking version bump.","title":"Public API"},{"category":"section","location":"api/#The-FlexiChain-and-FlexiSummary-types","page":"Public API","text":"The FlexiChain and FlexiSummary types are technically marked as public, but you should not handle them or their fields directly. For dispatch purposes, it is guaranteed that the first type parameter is always the key type; this should typically be all you need.\n\nThere are aliases for common FlexiChain key types:\n\nBoth types have extra type parameters, but they are considered internal and may change at any time.\n\nYou can also use Base.keytype:\n\nIf you ever need to construct a FlexiChain from scratch, there are exactly three ways to do so:\n\nwith an array of dictionaries, i.e., one dictionary per iteration;\nwith a dictionary of arrays, i.e., the values for each key are already grouped together;\nfrom a 3D array of iters x chains x params (see the converting from 3D arrays page for discussion of this).\n\nNote that, although the dictionaries themselves may have loose types, the key type of the FlexiChain must be specified (and the keys of the dictionaries will be checked against this).\n\nFlexiSummary objects should always be constructed by summarising a FlexiChain. If for some reason you need to construct one from scratch, please refer to the source code (and do let me know, so that I can make this part of the public API).","title":"The FlexiChain and FlexiSummary types"},{"category":"section","location":"api/#Equality","page":"Public API","text":"","title":"Equality"},{"category":"section","location":"api/#Sizes","page":"Public API","text":"","title":"Sizes"},{"category":"section","location":"api/#Indices","page":"Public API","text":"","title":"Indices"},{"category":"section","location":"api/#Key-types","page":"Public API","text":"","title":"Key types"},{"category":"section","location":"api/#Accessing-key-value-pairs","page":"Public API","text":"","title":"Accessing key-value pairs"},{"category":"section","location":"api/#Indexing-by-key","page":"Public API","text":"For the public interface of getindex, please see the Indexing page.","title":"Indexing by key"},{"category":"section","location":"api/#Renaming-keys","page":"Public API","text":"","title":"Renaming keys"},{"category":"section","location":"api/#Accessing-metadata","page":"Public API","text":"Chains additionally store some metadata:","title":"Accessing metadata"},{"category":"section","location":"api/#Combining-chains","page":"Public API","text":"","title":"Combining chains"},{"category":"section","location":"api/#Subsetting-chains","page":"Public API","text":"You can do these with getindex too, but these functions are sometimes more convenient.","title":"Subsetting chains"},{"category":"section","location":"api/#Converting-to-flattened-arrays","page":"Public API","text":"Please see the converting to/from arrays page.","title":"Converting to flattened arrays"},{"category":"section","location":"api/#Extracting-per-iteration-samples","page":"Public API","text":"Please see the samples page.","title":"Extracting per-iteration samples"},{"category":"section","location":"api/#Integration-with-Turing.jl","page":"Public API","text":"","title":"Integration with Turing.jl"},{"category":"section","location":"api/#Summaries","page":"Public API","text":"The summaries interface is documented on the summarising page.","title":"Summaries"},{"category":"section","location":"api/#Plotting","page":"Public API","text":"The plotting interface is documented on the plotting pages.","title":"Plotting"},{"category":"type","location":"api/#FlexiChains.FlexiChain","page":"Public API","text":"struct FlexiChain{\n    TKey,TMetadata<:NTuple{N,FlexiChainMetadata}\n} <: AbstractMCMC.AbstractChains\n\nAn MCMC chain.\n\nwarning: Warning\nPlease note that all fields of the FlexiChain type are considered internal.\n\nTODO: Document further.\n\nFields\n\n_data::OrderedCollections.OrderedDict{Union{FlexiChains.Parameter{var\"#s1\"}, FlexiChains.Extra} where var\"#s1\"<:TKey, Matrix} where TKey: Internal per-iteration data for parameters and extra keys. To access the data in here, you should index into the chain.\n\n_metadata::FlexiChains.FlexiChainMetadata: Other items associated with the chain. These are not necessarily per-iteration (for example there may only be one per chain).\nYou should not access this field directly; instead you should use the accessor functions provided: FlexiChains.iter_indices, FlexiChains.chain_indices, FlexiChains.sampling_time, and FlexiChains.last_sampler_state.\n\n_structures::Matrix: A 'structure' object, one per sample, that can be used to upconvert the OrderedDict representation of the data into a richer information format. This is in general nothing, but Turing.jl in particular hijacks this field to store a skeleton VarNamedTuple, allowing for functions like parameters_at to return VarNamedTuples instead of OrderedDict{VarName}.\n\n\n\n\n\n","title":"FlexiChains.FlexiChain"},{"category":"type","location":"api/#FlexiChains.FlexiSummary","page":"Public API","text":"FlexiChains.FlexiSummary{\n    TKey,\n    TIIdx<:Union{DimensionalData.Lookup,Nothing},\n    TCIdx<:Union{DimensionalData.Lookup,Nothing},\n    TSIdx<:DimensionalData.Categorical,\n}\n\nA data structure containing summary statistics of a FlexiChain.\n\nConstruction\n\nCalling summary functions such as mean or std on a FlexiChain will return a FlexiSummary. For more flexibility, you can use FlexiChains.collapse to apply one or more summary functions to a FlexiChain.\n\nUsers should not need to construct FlexiSummary objects directly.\n\nIndexing\n\nA FlexiSummary{TKey} can be indexed into using exactly the same techniques as a FlexiChain{TKey}. That is to say:\n\nwith Parameter{TKey} or Extra to unambiguously get the summary statistics for that key\nwith TKey for automatic conversion to Parameter{TKey}\nwith Symbol to find unambiguous matches\nif TKey<:VarName, using VarName or sub-VarNames to additionally extract part of the data.\n\nThe returned value will be either a DimensionalData.DimArray (if there are one or more non-collapsed dimensions), or a single value (if all dimensions are collapsed).\n\nIf a DimArray is returned, the dimensions that you will see are: :iter (if the summary function was only applied over chains), :chain (same but for iterations), and :stat (typically seen when multiple summary functions were applied).\n\nExtended help\n\nInternal data layout\n\nA FlexiSummary, much like a FlexiChain, contains a mapping of keys to arrays of data. However, the dimensions of a FlexiSummary are substantially different. In particular:\n\nThe :iter and/or :chain dimensions may have been collapsed via the act of calculating a summary over iterations or chains.\nThere is an additional, third, dimension: the statistic dimension, represented by `:stat. This dimension records which statistic(s) have been calculated.\nThe :stat dimension may also have been collapsed. This can happen if only one statistic was computed and drop_stat_dim=true was used when calling FlexiChains.collapse. The purpose of this is to avoid making the user deal with a redundant singleton dimension when calling a function such as mean(chain).\n\nRegardless of which dimensions have been collapsed, the internal data of a FlexiSummary always contains all three dimensions (some of which may have size 1).\n\nInformation about which dimensions are collapsed is therefore not stored in the arrays. Instead, it is stored in the _iter_indices and _chain_indices fields of the FlexiSummary, as well as their types. If either of these is nothing, then that dimension has been collapsed. For the stat dimension, _stat_indices always contains a DimensionalData.Categorical with the stat name(s), but the _drop_stat_dim field controls whether the stat dimension is presented to the user.\n\nThis information is later used in the _get_raw_data and _raw_to_user_data functions.\n\n\n\n\n\n","title":"FlexiChains.FlexiSummary"},{"category":"type","location":"api/#FlexiChains.VNChain","page":"Public API","text":"VNChain\n\nAlias for FlexiChain{VarName}.\n\n\n\n\n\n","title":"FlexiChains.VNChain"},{"category":"type","location":"api/#FlexiChains.SymChain","page":"Public API","text":"SymChain\n\nAlias for FlexiChain{Symbol}.\n\n\n\n\n\n","title":"FlexiChains.SymChain"},{"category":"method","location":"api/#Base.keytype-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"Base.keytype(cs::ChainOrSummary{TKey})\n\nReturns TKey.\n\n\n\n\n\n","title":"Base.keytype"},{"category":"method","location":"api/#FlexiChains.FlexiChain-Union{Tuple{TKey}, Tuple{Int64, Int64, AbstractArray{<:AbstractDict}}} where TKey","page":"Public API","text":"FlexiChain{TKey}(\n    niters::Int,\n    nchains::Int,\n    array_of_dicts::AbstractArray{<:AbstractDict,N};\n    structures::AbstractArray{<:Any,N}=fill(nothing, size(array_of_dicts)),\n    iter_indices::AbstractVector{Int}=1:niters,\n    chain_indices::AbstractVector{Int}=1:nchains,\n    sampling_time::AbstractVector{<:Union{Real,Missing}}=fill(missing, nchains),\n    last_sampler_state::AbstractVector=fill(missing, nchains),\n) where {TKey,N}\n\nConstruct a FlexiChain from a vector or matrix of dictionaries. Each dictionary corresponds to one iteration.\n\nData\n\nEach dictionary must be a mapping from a ParameterOrExtra{<:TKey} (i.e., either a Parameter{<:TKey} or an Extra) to its value at that iteration.\n\nIf array_of_dicts is a vector (i.e., N = 1), then niter is the length of the vector and nchains is 1. If array_of_dicts is a matrix (i.e., N = 2), then (niter, nchains) = size(dicts).\n\nOther values of N will error.\n\nStructures\n\nThe structures keyword argument can be used to provide an array of 'structures'. See the docstring for the _structures field for more information. The array must have the same size as array_of_dicts.\n\nMetadata\n\niter_indices and chain_indices can be used to specify the iteration and chain indices, respectively. By default, these are 1:niters and 1:nchains, but can be any vector of integers of the appropriate length. sampling_time and last_sampler_state are used to store metadata about each chain. They should be given as vectors of length nchains (even if there is only one chain).\n\nExample usage\n\nusing FlexiChains: FlexiChain, Parameter, Extra\n\nd = fill(\n    Dict(Parameter(:x) => rand(), Extra(\"y\") => rand()), 200, 3\n)\nchn = FlexiChain{Symbol}(200, 3, d)\n\n\n\n\n\n","title":"FlexiChains.FlexiChain"},{"category":"method","location":"api/#FlexiChains.FlexiChain-Union{Tuple{N}, Tuple{TKey}, Tuple{Int64, Int64, AbstractDict{<:Any, <:AbstractArray{<:Any, N}}}} where {TKey, N}","page":"Public API","text":"FlexiChain{TKey}(\n    niters::Int,\n    nchains::Int,\n    dict_of_arrays::AbstractDict{<:Any,<:AbstractArray{<:Any,N}};\n    structures::AbstractArray{<:Any,N}=fill(nothing, niters, nchains),\n    iter_indices::AbstractVector{Int}=1:niters,\n    chain_indices::AbstractVector{Int}=1:nchains,\n    sampling_time::AbstractVector{<:Union{Real,Missing}}=fill(missing, nchains),\n    last_sampler_state::AbstractVector=fill(missing, nchains),\n) where {TKey,N}\n\nConstruct a FlexiChain from a dictionary of arrays.\n\nData\n\nEach key in the dictionary must subtype ParameterOrExtra{<:TKey} (i.e., it is either a Parameter{<:TKey} or an Extra). The values of the dictionary must all be of the same size.\n\nIf the values are vectors (i.e., N = 1), then niters will be the length of the vector, and nchains will be 1. If the values are matrices (i.e., N = 2), then (niter, nchains) = size(array).\n\nOther values of N will error.\n\nMetadata\n\niter_indices and chain_indices can be used to specify the iteration and chain indices, respectively. By default, these are 1:niters and 1:nchains, but can be any vector of integers of the appropriate length. sampling_time and last_sampler_state are used to store metadata about each chain. They should be given as vectors of length nchains (even if there is only one chain).\n\nExample usage\n\nusing FlexiChains: FlexiChain, Parameter, Extra\n\nd = Dict(\n    Parameter(:x) => rand(200, 3),\n    Extra(\"y\") => rand(200, 3),\n)\nchn = FlexiChain{Symbol}(200, 3, d)\n\n\n\n\n\n","title":"FlexiChains.FlexiChain"},{"category":"method","location":"api/#Base.:(==)-Tuple{FlexiChains.FlexiChain, FlexiChains.FlexiChain}","page":"Public API","text":"Base.:(==)(c1::FlexiChain{TKey1}, c2::FlexiChain{TKey2}) where {TKey1,TKey2}\nBase.:(==)(c1::FlexiSummary{TKey1}, c2::FlexiSummary{TKey2}) where {TKey1,TKey2}\n\nEquality operator for FlexiChains and FlexiSummarys. Two chains (or summaries) are equal if they have the same key type, the same size, the same data for each key, and the same metadata (which includes dimensional indices, sampling time, and sampler states).\n\nnote: Note\nBecause missing == missing returns missing, and NaN == NaN returns false, this function will not return true if there are any missing or NaN values in the chains, even if they appear in the same positions. To test for equality with such data, use isequal(c1, c2) instead.\n\ntip: Tip\nIf you want to only compare equality of the data, you can use FlexiChains.has_same_data(c1, c2; strict=true).\n\ndanger: Danger\nBecause (==) on OrderedCollections.OrderedDict does not check key order, two chains with the same keys but in different orders will also be considered equal. If you think this is a mistake, please see this issue.\n\n\n\n\n\n","title":"Base.:(==)"},{"category":"method","location":"api/#Base.isequal-Tuple{FlexiChains.FlexiChain, FlexiChains.FlexiChain}","page":"Public API","text":"Base.isequal(c1::FlexiChain{TKey1}, c2::FlexiChain{TKey2})::Bool where {TKey1,TKey2}\nBase.isequal(c1::FlexiSummary{TKey1}, c2::FlexiSummary{TKey2})::Bool where {TKey1,TKey2}\n\nEquality operator for FlexiChains that treats missing and NaN values as equal if they appear in the same positions.\n\ntip: Tip\nIf you want to only compare equality of the data, you can use FlexiChains.has_same_data(c1, c2; strict=false).\n\ndanger: Danger\nBecause (==) on OrderedCollections.OrderedDict does not check key order, two chains with the same keys but in different orders will also be considered equal. If you think this is a mistake, please see this issue.\n\n\n\n\n\n","title":"Base.isequal"},{"category":"method","location":"api/#FlexiChains.has_same_data-Tuple{FlexiChains.FlexiChain, FlexiChains.FlexiChain}","page":"Public API","text":"FlexiChains.has_same_data(\n    c1::FlexiChain{TKey1},\n    c2::FlexiChain{TKey2};\n    strict=false\n) where {TKey1,TKey2}\n\nCheck if two FlexiChains have the same data, ignoring metadata such as sampling time, iteration indices, and chain indices.\n\nIf strict=true, then Base.:(==) is used to compare the data, which propagates missing values and treats NaN values as unequal. If strict=false (the default), then Base.isequal is used, which treats missing and NaN values as equal to themselves.\n\n\n\n\n\n","title":"FlexiChains.has_same_data"},{"category":"method","location":"api/#Base.size-Tuple{FlexiChains.FlexiChain}","page":"Public API","text":"Base.size(chain::FlexiChain[, dim::Int])\n\nReturns (niters, nchains), or niters or nchains if dim=1 or dim=2 is specified.\n\nnote: MCMCChains difference\nMCMCChains returns a 3-tuple of (niters, nkeys, nchains) where nkeys is the total number of parameters. FlexiChains does not do this because the keys are not considered an axis of their own. If you want the total number of keys in a FlexiChain, you can use length(keys(chain)).\n\n\n\n\n\n","title":"Base.size"},{"category":"method","location":"api/#Base.size-Tuple{FlexiChains.FlexiSummary}","page":"Public API","text":"Base.size(summary::FlexiSummary[, dim::Int])\n\nReturns (niters, nchains, nstats), or niters, nchains, or nstats if dim=1, dim=2, or dim=3 is specified. If any of the dimensions have been collapsed, the corresponding value will be 0.\n\n\n\n\n\n","title":"Base.size"},{"category":"function","location":"api/#FlexiChains.niters","page":"Public API","text":"FlexiChains.niters(chain::FlexiChain)\n\nThe number of iterations in the FlexiChain. Equivalent to size(chain, 1).\n\n\n\n\n\nFlexiChains.niters(summary::FlexiSummary)\n\nThe number of iterations in the FlexiSummary. Equivalent to size(summary, 1). Returns 0 if the iteration dimension has been collapsed.\n\n\n\n\n\n","title":"FlexiChains.niters"},{"category":"function","location":"api/#FlexiChains.nchains","page":"Public API","text":"FlexiChains.nchains(chain::FlexiChain)\n\nThe number of chains in the FlexiChain. Equivalent to size(chain, 2).\n\n\n\n\n\nFlexiChains.nchains(summary::FlexiSummary)\n\nThe number of chains in the FlexiSummary. Equivalent to size(summary, 2). Returns 0 if the chain dimension has been collapsed.\n\n\n\n\n\n","title":"FlexiChains.nchains"},{"category":"function","location":"api/#FlexiChains.nstats","page":"Public API","text":"FlexiChains.nstats(summary::FlexiSummary)\n\nThe number of statistics in the FlexiSummary. Equivalent to size(summary, 3). Returns 0 if the statistics dimension has been collapsed (this means that there is a single statistic, but its name is not stored or displayed to the user).\n\n\n\n\n\n","title":"FlexiChains.nstats"},{"category":"function","location":"api/#FlexiChains.iter_indices","page":"Public API","text":"iter_indices(chain::FlexiChain)::DimensionalData.Lookup\n\nThe indices of each MCMC iteration in the chain. This tries to reflect the actual iteration numbers from the sampler: for example, if you discard the first 100 iterations and sampled 100 iterations but with a thinning factor of 2, this will be 101:2:300.\n\nThe accuracy of this field is reliant on the sampler providing this information, though.\n\n\n\n\n\niter_indices(summary::FlexiSummary)::DimensionalData.Lookup\n\nThe iteration indices, which are either the same as in the original chain, or nothing if the iter dimension has been collapsed.\n\n\n\n\n\n","title":"FlexiChains.iter_indices"},{"category":"function","location":"api/#FlexiChains.chain_indices","page":"Public API","text":"chain_indices(chain::FlexiChain)::DimensionalData.Lookup\n\nThe indices of each MCMC chain in the chain. This will pretty much always be 1:nchains(chain) (unless the chain has been subsetted, or chain indices have been manually specified).\n\n\n\n\n\nchain_indices(summary::FlexiSummary)::DimensionalData.Lookup\n\nThe chain indices, which are either the same as in the original chain, or nothing if the chain dimension has been collapsed.\n\n\n\n\n\n","title":"FlexiChains.chain_indices"},{"category":"function","location":"api/#FlexiChains.stat_indices","page":"Public API","text":"stat_indices(summary::FlexiSummary)\n\nThe indices for each statistic in the summary. Returns nothing if the stat dimension has been collapsed (i.e. drop_stat_dim=true was used).\n\n\n\n\n\n","title":"FlexiChains.stat_indices"},{"category":"function","location":"api/#FlexiChains.renumber_iters","page":"Public API","text":"renumber_iters(\n    chain::FlexiChain{TKey},\n    iter_indices::AbstractVector{<:Integer}=1:niters(chain)\n)::FlexiChain{TKey} where {TKey}\n\nReturn a copy of chain with the iteration indices replaced by iter_indices.\n\n\n\n\n\n","title":"FlexiChains.renumber_iters"},{"category":"function","location":"api/#FlexiChains.renumber_chains","page":"Public API","text":"renumber_chains(\n    chain::FlexiChain{TKey},\n    chain_indices::AbstractVector{<:Integer}=1:nchains(chain)\n)::FlexiChain{TKey} where {TKey}\n\nReturn a copy of chain with the chain indices replaced by chain_indices.\n\n\n\n\n\n","title":"FlexiChains.renumber_chains"},{"category":"type","location":"api/#FlexiChains.Parameter","page":"Public API","text":"Parameter{T}(name::T)\n\nA named parameter in a FlexiChain. The name can be of any type, but all parameters in a FlexiChain must have the same type for their names.\n\nSpecifically, if you have a FlexiChain{TKey}, then all parameters must be of type Parameter{TKey}.\n\n\n\n\n\n","title":"FlexiChains.Parameter"},{"category":"type","location":"api/#FlexiChains.Extra","page":"Public API","text":"Extra{T}(name::T)\n\nA key in a FlexiChain that is not a parameter. The name of the key itself can be of any type and is not constrained by the type of the FlexiChain.\n\n\n\n\n\n","title":"FlexiChains.Extra"},{"category":"function","location":"api/#FlexiChains.get_name","page":"Public API","text":"FlexiChains.get_name(p::Parameter{T})::T\nFlexiChains.get_name(e::Extra{T})::T\n\nUnwrap the Parameter or Extra to get its name.\n\n\n\n\n\n","title":"FlexiChains.get_name"},{"category":"type","location":"api/#FlexiChains.ParameterOrExtra","page":"Public API","text":"ParameterOrExtra{T}\n\nAlias for Union{Parameter{T},Extra}.\n\nAll keys in a FlexiChain{T} must satisfy k isa ParameterOrExtra{<:T}.\n\n\n\n\n\n","title":"FlexiChains.ParameterOrExtra"},{"category":"type","location":"api/#FlexiChains.Prefixed","page":"Public API","text":"Prefixed(vn::VarName)\n\nA struct that represents a VarName that might have an arbitrary prefix. This is useful for indexing into a chain with VarNames when the exact prefix is not known (or too verbose to construct). For example, with Turing.jl, this allows for data processing code that is agnostic towards whether a submodel was used or not.\n\nWhen indexing into a chain with a Prefixed{VarName} key, the chain will be searched for any VarName that ends with the target VarName. For example, if the key is Prefixed(@varname(x)), and the chain contains @varname(a.x), the data corresponding to @varname(a.x) will be returned. (If there are multiple matches, an error is thrown.)\n\nNote that Prefixed is only supported for VarNames, and not for general keys.\n\n\n\n\n\n","title":"FlexiChains.Prefixed"},{"category":"method","location":"api/#Base.keys-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"Base.keys(cs::ChainOrSummary)\n\nReturns the keys of the FlexiChain (or summary thereof).\n\nTo obtain only the parameters, use FlexiChains.parameters.\n\n\n\n\n\n","title":"Base.keys"},{"category":"method","location":"api/#Base.haskey-Union{Tuple{TKey}, Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}}, TKey}} where TKey","page":"Public API","text":"Base.haskey(cs::ChainOrSummary{TKey}, key::TKey) where {TKey}\n\nReturns true if the FlexiChain or summary contains the given name as a parameter key.\n\n\n\n\n\n","title":"Base.haskey"},{"category":"method","location":"api/#Base.haskey-Union{Tuple{TKey}, Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}}, Union{FlexiChains.Parameter{var\"#s3\"}, FlexiChains.Extra} where var\"#s3\"<:TKey}} where TKey","page":"Public API","text":"Base.haskey(cs::ChainOrSummary{TKey}, key::ParameterOrExtra{<:TKey}) where {TKey}\n\nReturns true if the FlexiChain or summary contains the given key.\n\n\n\n\n\n","title":"Base.haskey"},{"category":"method","location":"api/#FlexiChains.parameters-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"FlexiChains.parameters(cs::ChainOrSummary{TKey}) where {TKey}\n\nReturns a vector of parameter names in the FlexiChain or summary thereof.\n\n\n\n\n\n","title":"FlexiChains.parameters"},{"category":"method","location":"api/#FlexiChains.extras-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"FlexiChains.extras(cs::ChainOrSummary)\n\nReturns a vector of non-parameter names in the FlexiChain or summary thereof.\n\n\n\n\n\n","title":"FlexiChains.extras"},{"category":"method","location":"api/#Base.values-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"Base.values(cs::ChainOrSummary; parameters_only::Bool=false)\n\nReturns the values of the FlexiChain or FlexiSummary, i.e., the matrices obtained by indexing into the chain with each key.\n\nIf parameters_only is true, only the values corresponding to parameter keys are returned.\n\n\n\n\n\n","title":"Base.values"},{"category":"method","location":"api/#Base.pairs-Tuple{Union{FlexiChains.FlexiChain{TKey}, FlexiChains.FlexiSummary{TKey}} where TKey}","page":"Public API","text":"Base.pairs(cs::ChainOrSummary)\n\nReturns an iterator over the key-value pairs of the FlexiChain or FlexiSummary.\n\nIf parameters_only is true, only the values corresponding to parameter keys are returned.\n\ntip: Tip\nNote that this function allows you to decompose a FlexiChain into a dict-of-arrays, e.g. with OrderedDict(pairs(chain; parameters_only=...)).\n\n\n\n\n\n","title":"Base.pairs"},{"category":"function","location":"api/#FlexiChains.map_keys","page":"Public API","text":"FlexiChains.map_keys(f, cs::ChainOrSummary{T})::ChainOrSummary{S} where {T,S}\n\nRename the keys of a FlexiChain or FlexiSummary by applying the function f to each key.\n\nf must have the signature f(::ParameterOrExtra{<:T}) -> ParameterOrExtra{<:S}. It must return a unique key for each input key.\n\n\n\n\n\n","title":"FlexiChains.map_keys"},{"category":"function","location":"api/#FlexiChains.map_parameters","page":"Public API","text":"FlexiChains.map_parameters(f, cs::ChainOrSummary{T})::ChainOrSummary{S} where {T,S}\n\nRename the parameters of a FlexiChain or FlexiSummary by applying the function f to each parameter name.\n\nf must have the signature f(::T) -> S. It must return a unique parameter for each input parameter.\n\n\n\n\n\n","title":"FlexiChains.map_parameters"},{"category":"function","location":"api/#FlexiChains.sampling_time","page":"Public API","text":"sampling_time(chain::FlexiChain):Vector\n\nReturn the time taken to sample each chain (in seconds). If the time was not recorded, this will be missing.\n\nNote that this always returns a vector with length equal to the number of chains.\n\n\n\n\n\n","title":"FlexiChains.sampling_time"},{"category":"function","location":"api/#FlexiChains.last_sampler_state","page":"Public API","text":"last_sampler_state(chain::FlexiChain)::Vector\n\nReturn the final state of the sampler used to generate the chain, if the save_state=true keyword argument was passed to sample. This can be used for resuming MCMC sampling.\n\nIf the state was not saved for a given chain, its entry in the vector will be missing.\n\nnote: Returns a vector\nThis function always returns a vector of sampler states, even if there is only one chain. Consequently, if you are resuming a single-chain MCMC run like sample(model, spl, N), you will need to extract the sole element of the returned vector before passing it as the initial_state keyword argument to sample(). Please see the 'using with Turing' page, or the Turing.jl documentation page on initial_state, for more explanation of this.\n\n\n\n\n\n","title":"FlexiChains.last_sampler_state"},{"category":"method","location":"api/#Base.vcat-Union{Tuple{T}, Tuple{FlexiChains.FlexiChain{T}, FlexiChains.FlexiChain{T}}} where T","page":"Public API","text":"Base.vcat(cs...::FlexiChain{TKey}) where {TKey}\n\nConcatenate one or more FlexiChains along the iteration dimension. Both c1 and c2 must have the same number of chains and the same key type.\n\nThe resulting chain's keys are the union of both input chains' keys. Any keys that only have data in one of the arguments will be assigned missing data in the other chain during concatenation.\n\nThe resulting chain's sampling time is the sum of the input chains' sampling times, and the last sampler state is taken from the second chain.\n\n\n\n\n\n","title":"Base.vcat"},{"category":"method","location":"api/#Base.hcat-Union{Tuple{T}, Tuple{FlexiChains.FlexiChain{T}, FlexiChains.FlexiChain{T}}} where T","page":"Public API","text":"Base.hcat(cs...::FlexiChain{TKey}) where {TKey}\n\nConcatenate one or more FlexiChains along the chain dimension. Both c1 and c2 must have the same number of iterations and the same key type.\n\nThe resulting chain's keys are the union of both input chains' keys. Any keys that only have data in one of the arguments will be assigned missing data in the other chain during concatenation.\n\nThe resulting chain's sampling times and last sampler states are obtained by concatenating those of the input chains.\n\n\n\n\n\n","title":"Base.hcat"},{"category":"method","location":"api/#Base.merge-Union{Tuple{T}, Tuple{FlexiChains.FlexiChain{T}, FlexiChains.FlexiChain{T}}} where T","page":"Public API","text":"Base.merge(\n    c1::FlexiChain{TKey1},\n    c2::FlexiChain{TKey2}\n) where {TKey1,TKey2}\n\nMerge the contents of two FlexiChains. If there are keys that are present in both chains, the values from c2 will overwrite those from c1.\n\nIf the key types are different, the resulting FlexiChain will have a promoted key type, and a warning will be issued.\n\nThe two FlexiChains being merged must have the same dimensions.\n\nThe chain indices and metadata are taken from the second chain. Those in the first chain are silently ignored.\n\n\n\n\n\n","title":"Base.merge"},{"category":"function","location":"api/#FlexiChains.subset_parameters","page":"Public API","text":"subset_parameters(cs::ChainOrSummary)\n\nSubset a chain or summary, retaining only the Parameter keys.\n\n\n\n\n\n","title":"FlexiChains.subset_parameters"},{"category":"function","location":"api/#FlexiChains.subset_extras","page":"Public API","text":"subset_extras(cs::ChainOrSummary)\n\nSubset a chain or summary, retaining only the keys that are Extras (i.e. not parameters).\n\n\n\n\n\n","title":"FlexiChains.subset_extras"},{"category":"function","location":"api/#DynamicPPL.logprior","page":"Public API","text":"DynamicPPL.logprior(model::DynamicPPL.Model, chain::FlexiChain{<:VarName})\n\nReturns a DimMatrix of the log-prior probabilities, re-evaluated using the parameters at each iteration of the chain.\n\n\n\n\n\n","title":"DynamicPPL.logprior"},{"category":"function","location":"api/#StatsAPI.loglikelihood","page":"Public API","text":"DynamicPPL.loglikelihood(model::DynamicPPL.Model, chain::FlexiChain{<:VarName})\n\nReturns a DimMatrix of the log-likelihoods, re-evaluated using the parameters at each iteration of the chain.\n\n\n\n\n\n","title":"StatsAPI.loglikelihood"},{"category":"function","location":"api/#DynamicPPL.logjoint","page":"Public API","text":"DynamicPPL.logjoint(model::DynamicPPL.Model, chain::FlexiChain{<:VarName})\n\nReturns a DimMatrix of the log-joint probabilities, re-evaluated using the parameters at each iteration of the chain.\n\n\n\n\n\n","title":"DynamicPPL.logjoint"},{"category":"function","location":"api/#DynamicPPL.returned","page":"Public API","text":"DynamicPPL.returned(model::DynamicPPL.Model, chain::FlexiChain{<:VarName})\n\nReturns a DimMatrix of the model's return values, re-evaluated using the parameters in each iteration of the chain.\n\nIf the return value is a DimArray, the dimensions will be stacked.\n\n\n\n\n\n","title":"DynamicPPL.returned"},{"category":"function","location":"api/#StatsAPI.predict","page":"Public API","text":"DynamicPPL.predict(\n    [rng::Random.AbstractRNG,]\n    model::DynamicPPL.Model,\n    chain::FlexiChain{<:VarName};\n    include_all::Bool=true,\n)\n\nReturns a new FlexiChain containing predictions for variables in the model, conditioned on the parameters in each iteration of the input chain.\n\nThe returned FlexiChain by default will contain all the predicted variables, as well as the variables already present in the input chain. If you only want the predicted variables, set include_all=false.\n\nThe returned chain will also contain log-probabilities corresponding to the re-evaluation of the model. In particular, the log probability for the newly predicted variables are now considered as prior terms. However, note that the log-prior of the returned chain will also contain the log-prior terms of the parameters already present in the input chain. Thus, if you want to obtain the log-probability of the predicted variables only, you can subtract the two log-prior terms. The include_all keyword argument has no effect on the log-probability fields.\n\n\n\n\n\n","title":"StatsAPI.predict"},{"category":"function","location":"api/#DynamicPPL.pointwise_logdensities","page":"Public API","text":"DynamicPPL.pointwise_logdensities(\n    model::Model,\n    chain::FlexiChain{<:VarName};\n    factorize::Bool=false,\n)::FlexiChain{VarName}\n\nCalculate the log probability density associated with each variable in the model, for each iteration in the FlexiChain.\n\nReturns a new FlexiChain with the same structure as the input chain, mapping the variables to their log probabilities.\n\nIf factorize=true, additionally attempt to provide factorised log-densities for distributions that can be partitioned into blocks, using PartitionedDistributions.jl.\n\nFor example, if factorize=true, then y ~ MvNormal(...) will return a vector of log-densities, one for each element of y. The i-th element of this vector will be the conditional log-probability of y[i] given all the other elements of y (often denoted log p(y_{i} | y_{-i})): in particular this is exactly the log-density required for leave-one-out cross-validation.\n\nIn contrast, if factorize=false, then the log-density for y ~ MvNormal(...) will be a single scalar corresponding to logpdf(MvNormal(...), y).\n\nNote that the sum of the factorised log-densities may not, in general, be equal to the log-density of the full distribution: they will only be equal if the original distribution can be completely factorised into independent components. For example, if y ~ MvNormal(μ, Σ) where Σ is diagonal, then each element of y is independent and the sum of the factorised log-densities will be equal to the log-density of the full distribution. In contrast, if Σ has off-diagonal entries, then the elements of y are not independent.\n\n\n\n\n\n","title":"DynamicPPL.pointwise_logdensities"},{"category":"function","location":"api/#DynamicPPL.pointwise_loglikelihoods","page":"Public API","text":"DynamicPPL.pointwise_loglikelihoods(\n    model::Model,\n    chain::FlexiChain{<:VarName};\n    factorize::Bool=false,\n)::FlexiChain{VarName}\n\nCalculate the log likelihood associated with each observed variable in the model, for each iteration in the FlexiChain.\n\nReturns a new FlexiChain with the same structure as the input chain, mapping the observed variables to their log probabilities.\n\nIf factorize=true, additionally attempt to provide factorised log-densities for distributions that can be partitioned into blocks, using PartitionedDistributions.jl.\n\nFor example, if factorize=true, then y ~ MvNormal(...) will return a vector of log-densities, one for each element of y. The i-th element of this vector will be the conditional log-probability of y[i] given all the other elements of y (often denoted log p(y_{i} | y_{-i})): in particular this is exactly the log-density required for leave-one-out cross-validation.\n\nIn contrast, if factorize=false, then the log-density for y ~ MvNormal(...) will be a single scalar corresponding to logpdf(MvNormal(...), y).\n\nNote that the sum of the factorised log-densities may not, in general, be equal to the log-density of the full distribution: they will only be equal if the original distribution can be completely factorised into independent components. For example, if y ~ MvNormal(μ, Σ) where Σ is diagonal, then each element of y is independent and the sum of the factorised log-densities will be equal to the log-density of the full distribution. In contrast, if Σ has off-diagonal entries, then the elements of y are not independent.\n\n\n\n\n\n","title":"DynamicPPL.pointwise_loglikelihoods"},{"category":"function","location":"api/#DynamicPPL.pointwise_prior_logdensities","page":"Public API","text":"DynamicPPL.pointwise_prior_logdensities(\n    model::Model,\n    chain::FlexiChain{<:VarName};\n    factorize::Bool=false, \n)::FlexiChain{VarName}\n\nCalculate the log prior associated with each random variable in the model, for each iteration in the FlexiChain.\n\nReturns a new FlexiChain with the same structure as the input chain, mapping the observed variables to their log probabilities.\n\nIf factorize=true, additionally attempt to provide factorised log-densities for distributions that can be partitioned into blocks, using PartitionedDistributions.jl.\n\nFor example, if factorize=true, then y ~ MvNormal(...) will return a vector of log-densities, one for each element of y. The i-th element of this vector will be the conditional log-probability of y[i] given all the other elements of y (often denoted log p(y_{i} | y_{-i})): in particular this is exactly the log-density required for leave-one-out cross-validation.\n\nIn contrast, if factorize=false, then the log-density for y ~ MvNormal(...) will be a single scalar corresponding to logpdf(MvNormal(...), y).\n\nNote that the sum of the factorised log-densities may not, in general, be equal to the log-density of the full distribution: they will only be equal if the original distribution can be completely factorised into independent components. For example, if y ~ MvNormal(μ, Σ) where Σ is diagonal, then each element of y is independent and the sum of the factorised log-densities will be equal to the log-density of the full distribution. In contrast, if Σ has off-diagonal entries, then the elements of y are not independent.\n\n\n\n\n\n","title":"DynamicPPL.pointwise_prior_logdensities"},{"category":"type","location":"api/#DynamicPPL.InitFromParams","page":"Public API","text":"DynamicPPL.InitFromParams(\n    chn::FlexiChain{<:VarName},\n    iter::Union{Int,DD.At},\n    chain::Union{Int,DD.At},\n    fallback::Union{AbstractInitStrategy,Nothing}=InitFromPrior()\n)::DynamicPPL.InitFromParams\n\nUse the parameters stored in a FlexiChain as an initialisation strategy.\n\n\n\n\n\n","title":"DynamicPPL.InitFromParams"},{"category":"type","location":"api/#MCMCChains.Chains","page":"Public API","text":"MCMCChains.Chains(chain::FlexiChain)\n\nConvert a FlexiChain to an MCMCChains.Chains object.\n\nArray-valued parameters are split up into their individual real-valued elements, much like the output that you get directly from sampling with Turing + MCMCChains.\n\n\n\n\n\n","title":"MCMCChains.Chains"},{"category":"function","location":"api/#AbstractMCMC.from_samples","page":"Public API","text":"AbstractMCMC.from_samples(\n    ::Type{<:VNChain},\n    params_and_stats::AbstractMatrix{<:DynamicPPL.ParamsWithStats}\n)::VNChain\n\nConvert a matrix of DynamicPPL.ParamsWithStats to a VNChain.\n\n\n\n\n\nAbstractMCMC.from_samples(\n    ::Type{<:VNChain},\n    params_and_stats::AbstractMatrix{<:DynamicPPL.VarNamedTuple}\n)::VNChain\n\nConvert a matrix of DynamicPPL.VarNamedTuple to a VNChain.\n\n\n\n\n\n","title":"AbstractMCMC.from_samples"},{"category":"function","location":"api/#AbstractMCMC.to_samples","page":"Public API","text":"AbstractMCMC.to_samples(\n    ::Type{DynamicPPL.ParamsWithStats},\n    chain::VNChain,\n    [model::DynamicPPL.Model]\n)::DimensionalData.DimMatrix{DynamicPPL.ParamsWithStats}\n\nConvert a VNChain to a DimMatrix of DynamicPPL.ParamsWithStats.\n\nThe axes of the DimMatrix are the same as those of the input VNChain.\n\n\n\n\n\n","title":"AbstractMCMC.to_samples"},{"category":"section","location":"summarising/#summarising","page":"Summarising","text":"In general a FlexiChain contains data in matrices of size (niters, nchains). Often it is useful to summarise this data along one or both dimensions.\n\nFlexiChains therefore allows you to calculate one or more statistics for each variable stored in a FlexiChain. The result is a FlexiSummary object, which can be indexed into in a very similar way to FlexiChains: see the Indexing page for full details, or the examples on this page.","title":"Summarising"},{"category":"section","location":"summarising/#Unsupported-data-types","page":"Summarising","text":"Before we launch into the available summary statistics, it is worth mentioning one point about data types. Since FlexiChains allows for storage of completely arbitrary data types, it can contain data for which the mean (or other statistic) is not defined. For example, the mean of String values is not defined. Thus, when calculating mean(chain), any String-valued parameters will be dropped from the result.\n\nIf multiple summary statistics are requested (e.g. with summarystats), the key is only dropped if all of them fail. If at least one statistic is successfully computed for a key, that key will be included in the result, with missing values for the statistics that failed.\n\nTo give a flavour of how this works, here is an example of a model that generates parameters of different types:\n\nusing FlexiChains, Turing\n\n@model function f()\n    f ~ Normal()               # float\n    v ~ MvNormal(zeros(2), I)  # vector\n    s := \"a string\"            # string\nend\n\nchain = sample(f(), MH(), MCMCThreads(), 20, 3; chain_type=VNChain)\n\nOver the course of this page we will see what happens to each of these parameters when we try to compute summary statistics.","title":"Unsupported data types"},{"category":"section","location":"summarising/#Overall-summary-statistics","page":"Summarising","text":"If you want a quick overview of what's in your chain, summarystats provides a handy selection of commonly used statistics:\n\nst = summarystats(chain)\n\nNotice that the string-valued parameter s has been dropped from the result, since no statistics could be computed for it. (If you run this in the terminal, you will see a warning about this, so it is not completely silent; it's just not shown in the docs.)\n\nYou can index with a variable name (or names!) and the stat dimension:\n\nst[@varname(v[1]), stat=At(:mean)]  # Mean of first element of vector v\n\nnote: At()\nNotice to access the mean you have to use stat=At(:mean) rather than just stat=:mean. This seems a bit verbose, but is actually perfectly consistent with DimensionalData.jl's behaviour: stat=1 means the first statistic, and stat=At(:f) means the statistic with the named index :f.\n\nFor more details on indexing, please see the Indexing page.\n\nIn the result above, the vector-valued v has been broken up into its individual elements v[1] and v[2]. This happens automatically for chains with VarName keys; you can disable this behaviour by passing split_varnames=false:\n\nst2 = summarystats(chain; split_varnames=false)\n\nNow, the summary statistics are calculated with each vector v being a single entity. The key v is still present in the result (since mean and std could be computed for it). However, notice that other statistics such as ess are no longer defined, and so return missing values. Just like before, the string s is dropped since no statistics could be computed for it.","title":"Overall summary statistics"},{"category":"section","location":"summarising/#Individual-statistics","page":"Summarising","text":"Sometimes you may only want to calculate a single statistic.\n\nThe following functions are all overloaded to accept FlexiChain objects. In all cases, they can be called with dims=:both, dims=:iter, or dims=:chain to specify the dimension over which to compute the statistic; the default is dims=:both.\n\nAll of these functions return a FlexiSummary where the :stat dimension has already been collapsed. That means that if you want to access the mean of a variable @varname(a) you don't need to further use the stat dimension:\n\nmn = mean(chain)\n# Not needed: mean_f = m[@varname(f), stat=At(:mean)]\n# Just do:\nmean_f = mn[@varname(f)]\n\nIn fact, behind the scenes, the actual name of the statistic is retained. This means that, for example, if you convert the result to a DataFrame, the column will be named mean rather than stat:\n\nusing DataFrames; DataFrame(mn)\n\nFor highest density intervals and equal-tailed intervals, you will need to load PosteriorStats as these are defined in an extension.","title":"Individual statistics"},{"category":"section","location":"summarising/#Custom-statistics","page":"Summarising","text":"There are two scenarios where the above are not enough:\n\nyou want to calculate a specific set of statistics that is not the same as what summarystats does; or\nyou want to calculate a completely custom statistic, which is not implemented above.\n\nIn both cases, you can directly use [FlexiChains.collapse] to achieve this. (But in the latter case, please do also consider opening an issue so that we can implement it!)\n\nAs an example, suppose you have a statistic that calculates the sum of the mean and standard deviation. (This is of course quite contrived: if you have a real example, again, please do open an issue!)\n\nWe start by defining our own function:\n\nusing Statistics\n\nfunction mean_std_sum(x::AbstractVector{<:Real})\n    return mean(x) + std(x)\nend\n\nAs noted in the docstring of FlexiChains.collapse, the function you provide must accept a vector argument and return a single value. Of course, it can also have other methods, but this is the one which collapse uses.\n\nNow we can use collapse to apply this function to all variables in the chain. The second argument is a vector, which in this case will only contain our one function:\n\ncustom_stat = FlexiChains.collapse(chain, [mean_std_sum]; dims=:both)\n\nThere are two things worth mentioning, which we will note in passing here without demonstrating (since they are also covered in the docstring):\n\nIf there is only one function provided, you can additionally pass drop_stat_dim=true to remove the :stat dimension from the result, much like what mean(chain) et al. do.\nThe name of the statistic is inferred from the function. Sometimes this doesn't work out nicely, for example if you pass an anonymous function. In this case you can provide a tuple of (:name, function) instead of just the function.","title":"Custom statistics"},{"category":"section","location":"summarising/#Merging-summaries","page":"Summarising","text":"As an alternative to using collapse, you can also calculate separate summaries and then merge them.\n\nmn = mean(chain)\nsd = std(chain)\n\nmerge(mn, sd)","title":"Merging summaries"},{"category":"section","location":"summarising/#Density-intervals","page":"Summarising","text":"It is worth just noting briefly here that FlexiChains has a PosteriorStats extension which allows you to calculate highest density intervals and equal-tailed intervals. Please see the PosteriorStats integration section for details.","title":"Density intervals"},{"category":"section","location":"summarising/#MCMC-diagnostics","page":"Summarising","text":"There are a number of functions in MCMCDiagnosticTools.jl which do not nicely fit the notion of 'collapse over one or more dimensions'. For example, quantities like the Gelman diagnostic are calculated over the entire dataset.\n\nFor these functions, you can simply pass a FlexiChain to them. For example:\n\ngelmandiag(chain)\n\nThe available functions are as follows (and FlexiChains reexports all of these too, for convenience).","title":"MCMC diagnostics"},{"category":"section","location":"summarising/#Flattening-summaries","page":"Summarising","text":"Finally, FlexiSummary objects can be converted to DimArrays or DataFrames, just like FlexiChains. This is useful for various analyses and visualisations.\n\nst = summarystats(chain)\nDimArray(st)\n\nusing DataFrames\n\nDataFrame(st)","title":"Flattening summaries"},{"category":"method","location":"summarising/#StatsBase.summarystats-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"StatsBase.summarystats(\n    chain::FlexiChain{TKey};\n    split_varnames::Bool=true,\n    warn::Bool=true,\n) where {TKey}\n\nCompute a standard set of summary statistics for each key in the chain. The statistics include:\n\nmean (using Statistics.mean)\nstandard deviation (Statistics.std)\nMonte Carlo standard error (MCMCDiagnosticTools.mcse)\nbulk effective sample size (MCMCDiagnosticTools.ess)\ntail effective sample size\nR-hat diagnostic (MCMCDiagnosticTools.rhat)\n5th, 50th (median), and 95th percentiles (Statistics.quantile)\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. The splitting is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types, this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nIf any of the statistics cannot be computed for a key, a missing value is returned. If none of the statistics can be computed for a key, that key will be dropped from the resulting FlexiSummary, and a warning issued. The warning can be suppressed by setting warn=false.\n\n\n\n\n\n","title":"StatsBase.summarystats"},{"category":"method","location":"summarising/#Statistics.mean-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Statistics.mean(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the mean across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Statistics.mean; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Statistics.mean"},{"category":"method","location":"summarising/#Statistics.median-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Statistics.median(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the median across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Statistics.median; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Statistics.median"},{"category":"method","location":"summarising/#Statistics.std-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Statistics.std(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the standard deviation across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Statistics.std; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Statistics.std"},{"category":"method","location":"summarising/#Statistics.var-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Statistics.var(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the variance across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Statistics.var; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Statistics.var"},{"category":"method","location":"summarising/#Statistics.quantile-Tuple{FlexiChains.FlexiChain, Any}","page":"Summarising","text":"Statistics.quantile(\n    chain::FlexiChain{TKey},\n    p;\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the quantile across all iterations and chains for each key in the chain. If it cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse.\n\n:iter: collapse the iteration dimension only\n:chain: collapse the chain dimension only\n:both: collapse both the iteration and chain dimensions (default)\n\nThe argument p specifies the quantile to compute, and is forwarded to Statistics.quantile, along with any other keyword arguments.\n\n\n\n\n\n","title":"Statistics.quantile"},{"category":"method","location":"summarising/#Base.minimum-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Base.minimum(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the minimum across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Base.minimum; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Base.minimum"},{"category":"method","location":"summarising/#Base.maximum-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Base.maximum(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the maximum across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Base.maximum; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Base.maximum"},{"category":"method","location":"summarising/#Base.sum-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Base.sum(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the sum across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Base.sum; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Base.sum"},{"category":"method","location":"summarising/#Base.prod-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"Base.prod(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the product across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to Base.prod; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"Base.prod"},{"category":"method","location":"summarising/#MCMCDiagnosticTools.ess-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"MCMCDiagnosticTools.ess(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the effective sample size across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.ess; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.ess"},{"category":"method","location":"summarising/#MCMCDiagnosticTools.rhat-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"MCMCDiagnosticTools.rhat(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the R-hat diagnostic across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.rhat; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.rhat"},{"category":"method","location":"summarising/#MCMCDiagnosticTools.mcse-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"MCMCDiagnosticTools.mcse(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the Monte Carlo standard error across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.mcse; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.mcse"},{"category":"method","location":"summarising/#StatsBase.mad-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"StatsBase.mad(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the median absolute deviation across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to StatsBase.mad; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"StatsBase.mad"},{"category":"method","location":"summarising/#StatsBase.geomean-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"StatsBase.geomean(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the geometric mean across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to StatsBase.geomean; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"StatsBase.geomean"},{"category":"method","location":"summarising/#StatsBase.harmmean-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"StatsBase.harmmean(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the harmonic mean across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to StatsBase.harmmean; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"StatsBase.harmmean"},{"category":"method","location":"summarising/#StatsBase.iqr-Tuple{FlexiChains.FlexiChain}","page":"Summarising","text":"StatsBase.iqr(\n    chain::FlexiChain{TKey};\n    dims::Symbol=:both,\n    warn::Bool=true,\n    split_varnames::Bool=true,\n    kwargs...\n) where {TKey}\n\nCalculate the interquartile range across all iterations and chains for each key in the chain. If the statistic cannot be computed for a key, that key is skipped and a warning is issued (which can be suppressed by setting warn=false).\n\nThe dims keyword argument specifies which dimensions to collapse. The default value of :both collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe split_varnames keyword argument, if true, will first split up variables in the chain such that each key corresponds to a single scalar value. This is only supported for chains with TKey<:VarName or TKey==Symbol; for other key types this will be a no-op as long as the data already contain scalar values for every key, but will error if the data contain non-scalar values.\n\nOther keyword arguments are forwarded to StatsBase.iqr; please see its documentation for details of supported keyword arguments.\n\n\n\n\n\n","title":"StatsBase.iqr"},{"category":"function","location":"summarising/#FlexiChains.collapse","page":"Summarising","text":"FlexiChains.collapse(\n    chain::FlexiChain,\n    funcs::AbstractVector;\n    dims::Symbol=:both,\n    warn::Bool=true,\n    drop_stat_dim::Bool=false,\n)\n\nLow-level function to collapse one or both dimensions of a FlexiChain by applying a list of summary functions.\n\nThe funcs argument must be a vector which contains either:\n\ntuples of the form (statistic_name::Symbol, func::Function); or\njust functions, in which case the statistic name is obtained from the function name.\n\nThe dims keyword argument specifies which dimensions to collapse. By default, dims is :both, which collapses both the iteration and chain dimensions. Other valid values are :iter or :chain, which respectively collapse only the iteration or chain dimension.\n\nThe functions in funcs must map a vector to a single value. For example, both Statistics.mean and Statistics.std satisfy this:\n\nusing FlexiChains: collapse\nusing Statistics: mean, std\n\ncollapse(chn, [mean, std]; dims=:both)\n\nIf dims=:iter or dims=:chain are selected, then the functions are automatically applied to each column or row as appropriate. No adjustment to the functions is necessary:\n\ncollapse(chn, [mean, std]; dims=:iter)\ncollapse(chn, [mean, std]; dims=:chain)\n\nFor dims=:both, the function is applied to all the samples stacked together as a single vector.\n\nSometimes, for more complicated functions like quantile, you have to pass an anonymous function (such as x -> quantile(x, 0.05) or a closure (such as Base.Fix2(quantile, 0.05)). In this case, to get a sensible statistic name, instead of just passing the function you can pass a tuple of the form (statistic_name::Symbol, func::Function).\n\ncollapse(chn, [\n    mean,\n    std,\n    (:q5, x -> quantile(x, 0.05)),\n    (:q95, x -> quantile(x, 0.95)),\n])\n\nIf a statistic function errors when applied to a key, that key is skipped and a warning is issued. The warning can be suppressed by setting warn=false.\n\nIf the drop_stat_dim keyword argument is true and only one function is provided in funcs, then the resulting FlexiSummary will have the stat dimension dropped. This allows for easier indexing into the result when only one statistic is computed. It is an error to set drop_stat_dim=true when more than one function is provided.\n\n\n\n\n\n","title":"FlexiChains.collapse"},{"category":"method","location":"summarising/#Base.merge-Tuple{FlexiChains.FlexiSummary, FlexiChains.FlexiSummary}","page":"Summarising","text":"Base.merge(\n    s1::FlexiSummary{TKey1},\n    s2::FlexiSummary{TKey2},\n) where {TKey1,TKey2}\n\nMerge two FlexiSummarys along both the key and statistic dimensions.\n\nThe two summaries must have the same iteration and chain indices.\n\nThe merged result contains the union of keys and the union of stat names from both summaries. For each (key, stat) pair, the value from s2 takes priority over s1. Pairs that exist in neither summary are filled with missing.\n\nIf the key types are different, the resulting FlexiSummary will have a promoted key type, and a warning will be issued.\n\n\n\n\n\n","title":"Base.merge"},{"category":"function","location":"summarising/#MCMCDiagnosticTools.gelmandiag","page":"Summarising","text":"MCMCDiagnosticTools.gelmandiag(\n    chain::FlexiChain{TKey};\n    warn::Bool=true,\n    kwargs...\n) where {TKey}\n\nCompute the Gelman–Rubin–Brooks diagnostic (Potential Scale Reduction Factor, PSRF) for each parameter in the chain.\n\nReturns a FlexiSummary with two statistics per parameter: :psrf (the point estimate) and :psrfci (the upper confidence limit).\n\nThe FlexiChain must contain at least 2 chains. Array-valued parameters are automatically split into their constituent scalars. Non-Real-valued keys are skipped with a warning (which can be suppressed via warn=false).\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.gelmandiag.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.gelmandiag"},{"category":"function","location":"summarising/#MCMCDiagnosticTools.gelmandiag_multivariate","page":"Summarising","text":"MCMCDiagnosticTools.gelmandiag_multivariate(\n    chain::FlexiChain{TKey};\n    warn::Bool=true,\n    kwargs...\n) where {TKey}\n\nCompute the multivariate Gelman–Rubin–Brooks diagnostic for the chain. This requires at least 2 parameters and at least 2 chains.\n\nReturns a NamedTuple with two fields:\n\nsummary: a FlexiSummary with :psrf and :psrfci statistics (same as what gelmandiag returns)\npsrf_multivariate: the multivariate potential scale reduction factor (Float64)\n\nArray-valued parameters are automatically split into their constituent scalars. Non-Real-valued keys are skipped with a warning (which can be suppressed via warn=false).\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.gelmandiag_multivariate.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.gelmandiag_multivariate"},{"category":"function","location":"summarising/#MCMCDiagnosticTools.discretediag","page":"Summarising","text":"MCMCDiagnosticTools.discretediag(\n    chain::FlexiChain{TKey};\n    warn::Bool=true,\n    kwargs...\n) where {TKey}\n\nCompute the discrete diagnostic for each parameter in the chain. This diagnostic is designed for discrete (categorical) MCMC samples.\n\nReturns a NamedTuple with two fields:\n\nbetween: a FlexiSummary with between-chain diagnostics (:stat, :df, :pvalue) per parameter\nwithin: a FlexiSummary with within-chain diagnostics (:stat, :df, :pvalue) per parameter and per chain\n\nThe FlexiChain must contain at least 2 chains. Array-valued parameters are automatically split into their constituent scalars. Non-Integer-valued keys are skipped with a warning (which can be suppressed via warn=false).\n\nOther keyword arguments are forwarded to MCMCDiagnosticTools.discretediag.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.discretediag"},{"category":"function","location":"summarising/#MCMCDiagnosticTools.bfmi","page":"Summarising","text":"MCMCDiagnosticTools.bfmi(\n    chain::FlexiChain,\n    energy_key\n)\n\nCalculate the Bayesian fraction of missing information (BFMI) from the given chain, using the specified energy_key to identify the key in the chain that corresponds to the Hamiltonian energy. Returns a DimVector of BFMI values, one per chain (note that even if there is only one chain, the result will still be a vector of length 1).\n\nFor chains sampled with Turing.jl's HMC/NUTS, the energy key is :hamiltonian_energy.\n\n\n\n\n\n","title":"MCMCDiagnosticTools.bfmi"},{"category":"section","location":"arrays/#arrays","page":"Converting to/from arrays","text":"Many MCMC libraries return samples in the form of 3D arrays. FlexiChains therefore provides methods to convert from and to 3D arrays, to maximise interoperability with other libraries.\n\nFlexiChains' representation of MCMC samples is richer than a plain array: it includes parameter/extra names, extra information about the structure of each parameter, metadata, and so on. This means that when converting from an array, you need to provide some additional information. Likewise, when converting to an array, some information will inevitably be lost. However, the conversion methods are designed to bridge this gap as far as possible.\n\nFlexiChains works with arrays that have dimensions iters x chains x parameters.","title":"Converting to/from arrays"},{"category":"section","location":"arrays/#api-fromarray","page":"Converting to/from arrays","text":"You can use a FlexiChain constructor to convert a 3D array to a FlexiChain. It is easiest to explain by example (the docstring, which has all the necessary detail, is at the bottom of this page). Here is a simple example where each column in the array corresponds to a scalar parameter:\n\nusing FlexiChains: FlexiChain, Parameter, Extra\n\narr = rand(10, 4, 3)   # 10 iterations, 4 chains, 3 parameters\nnames = (Parameter(:x), Parameter(:y), Extra(:lp))  # note: must be tuple\n\nFlexiChain{Symbol}(arr, names)\n\nFor convenience, this constructor also allows you to specify parameters that are array-valued, by specifying their sizes. In this example, the last four columns of arr will be interpreted as a single parameter z that is a 2x2 matrix:\n\narr = rand(10, 4, 6)   # 10 iterations, 4 chains, 6 (scalar) parameters\n\nnames = (\n    Parameter(:x),            # Scalar\n    Parameter(:y),            # Scalar\n    Parameter(:z) => (2, 2)   # Matrix\n)\n\nchn = FlexiChain{Symbol}(arr, names)\n\nYou can see the reshaped parameter z here\n\nchn[:z, iter=1, chain=1]\n\nis derived from\n\narr[1, 1, 3:6]","title":"Converting from arrays"},{"category":"section","location":"arrays/#api-flatten","page":"Converting to/from arrays","text":"When flattening a FlexiChain to an array, the structure of each parameter is lost: array-valued parameters, for example, are flattened to consecutive columns.\n\nTo retain parameter names, you can convert to a DimArray, which will have a :param dimension that stores the names:\n\nusing FlexiChains: DimArray\n\n# Create a chain from an array, much like above.\narr = randn(10, 4, 6)   # 10 iterations, 4 chains, 6 parameters\nchn = FlexiChain{Symbol}(arr, (\n    Parameter(:x),            # Scalar\n    Parameter(:y),            # Scalar\n    Parameter(:z) => (2, 2)   # Matrix\n))\n\n# Convert it back to an array.\nda = DimArray(chn)\n\nThis should recover the data in the original array:\n\nda == arr\n\nIf the parameter names are not needed, you can convert to a plain Array as well.\n\nArray(chn)","title":"Converting to arrays"},{"category":"section","location":"arrays/#Summaries","page":"Converting to/from arrays","text":"FlexiSummary objects can also be converted to arrays: depending on how many of their dimensions were collapsed, the resulting array will have different dimensions. However, there is presently no way to convert an array into a FlexiSummary.\n\nusing FlexiChains: summarystats\n\nfs = summarystats(chn)\nda = DimArray(fs)","title":"Summaries"},{"category":"section","location":"arrays/#Docstrings","page":"Converting to/from arrays","text":"","title":"Docstrings"},{"category":"method","location":"arrays/#FlexiChains.FlexiChain-Union{Tuple{T}, Tuple{AbstractArray{T, 3}, Any}} where T","page":"Converting to/from arrays","text":"FlexiChain{TKey}(\n    arr::AbstractArray{T,3},\n    key_spec::Union{TKey,Tuple};\n    iter_indices = 1:size(arr, 1),\n    chain_indices = 1:size(arr, 2),\n    sampling_time = fill(missing, size(arr, 2)),\n    last_sampler_state = fill(missing, size(arr, 2)),\n) where {T}\n\nConstruct a FlexiChain from a 3D array with dimensions (iters, chains, params).\n\nKey specification\n\nThe key_spec argument can be a single parameter::TKey, in which case the resulting FlexiChain will have a single vector-valued parameter with that name.\n\nAlternatively, you can pass a tuple which specifies how columns in the third dimension are mapped to Parameter or Extra keys. Columns are consumed left-to-right. Each element of key_spec must either be:\n\nkey::ParameterOrExtra{<:TKey}: consumes one column and stores it as a scalar value with the given key; or\nkey::ParameterOrExtra => (dims...): consumes prod(dims) columns, reshapes each sample to dims, and stores it with the given key.\n\nThe total number of columns consumed must equal size(arr, 3).\n\nMetadata\n\niter_indices and chain_indices can be used to specify the iteration and chain indices, respectively. By default, these are 1:niters and 1:nchains, but can be any vector of integers of the appropriate length. sampling_time and last_sampler_state are used to store metadata about each chain. They should be given as vectors of length nchains (even if there is only one chain).\n\nExample usage\n\nusing FlexiChains: FlexiChain, Parameter, Extra\n\narr = randn(100, 4, 6) # iters x chains x params\n\n# `x` and `lp` are scalars; but `z` is a 2x2 matrix, so it consumes 4 columns.\nchn = FlexiChain{Symbol}(arr, (Parameter(:x), Parameter(:z) => (2,2), Extra(:lp)))\n\n\n\n\n\n","title":"FlexiChains.FlexiChain"},{"category":"method","location":"arrays/#DimensionalData.DimArray-Tuple{FlexiChains.FlexiChain}","page":"Converting to/from arrays","text":"DimensionalData.DimArray(\n    chain::FlexiChain{TKey};\n    warn::Bool=true,\n    eltype_filter=Any,\n    parameters_only::Bool=true,\n) where {TKey}\n\nConvert a FlexiChain into a 3-dimensional DimArray with dimensions (:iter, :chain, :param).\n\nThis proceeds by first splitting array-valued parameters into scalar leaves, then extracting all scalar parameters whose element type subtypes eltype_filter and stacking them into a 3D array.\n\nKeys whose values do not subtype eltype_filter after splitting are skipped (with a warning if warn=true).\n\nIf parameters_only=true (the default), then two things happen:\n\nOnly parameters (not extras) are included in the DimArray. Otherwise, both parameters and extras are included.\nThe keys in the :param dimension of the resulting DimArray are just TKey, i.e., the Parameter wrapper is removed. Otherwise, the keys will be Union{Parameter{<:TKey},Extra}.\n\n\n\n\n\nDimensionalData.DimArray(\n    summary::FlexiSummary{TKey};\n    warn::Bool=true,\n    eltype_filter=Any,\n    parameters_only::Bool=true,\n) where {TKey}\n\nConvert a FlexiSummary into a DimArray with a :param dimension appended after the non-collapsed dimensions of the summary. For example:\n\nSummary produced via Dimensions of resulting DimArray\nmean(chn) (:param)\nmean(chn; dims=:iter) (:chain, :param)\nmean(chn; dims=:chain) (:iter, :param)\nsummarystats(chn) (:stat, :param)\ncollapse(chn, [mean, std]; dims=:iter) (:chain, :stat, :param)\n\nKeyword arguments\n\neltype_filter::Any: retain only parameters whose values subtype eltype_filter. For example, if eltype_filter=Float64, then integer-valued parameters are dropped.\nparameters_only::Bool=true: whether to include only parameters (not extras) in the resulting DimArray.\nwarn::Bool=true: whether to issue a warning if any keys are skipped due to their values not subtyping eltype_filter.\n\n\n\n\n\n","title":"DimensionalData.DimArray"},{"category":"method","location":"arrays/#Core.Array-Tuple{FlexiChains.FlexiChain}","page":"Converting to/from arrays","text":"Base.Array(\n    chain::FlexiChain;\n    kwargs...\n)\n\nConvert a FlexiChain into a standard Array with dimensions (iter, chain, param). This is the same as the conversion to DimensionalData.DimArray, except that the dimension metadata is discarded.\n\nSee DimensionalData.DimArray(::FlexiChains.FlexiChain) for more details on the conversion process and available keyword arguments.\n\n\n\n\n\nBase.Array(\n    summary::FlexiSummary;\n    kwargs...\n)\n\nConvert a FlexiSummary into a standard Array. This is the same as the conversion to DimensionalData.DimArray, except that the dimension metadata is discarded.\n\nSee DimensionalData.DimArray(::FlexiChains.FlexiSummary) for details.\n\n\n\n\n\n","title":"Core.Array"},{"category":"method","location":"arrays/#DimensionalData.DimArray-Tuple{FlexiChains.FlexiSummary}","page":"Converting to/from arrays","text":"DimensionalData.DimArray(\n    chain::FlexiChain{TKey};\n    warn::Bool=true,\n    eltype_filter=Any,\n    parameters_only::Bool=true,\n) where {TKey}\n\nConvert a FlexiChain into a 3-dimensional DimArray with dimensions (:iter, :chain, :param).\n\nThis proceeds by first splitting array-valued parameters into scalar leaves, then extracting all scalar parameters whose element type subtypes eltype_filter and stacking them into a 3D array.\n\nKeys whose values do not subtype eltype_filter after splitting are skipped (with a warning if warn=true).\n\nIf parameters_only=true (the default), then two things happen:\n\nOnly parameters (not extras) are included in the DimArray. Otherwise, both parameters and extras are included.\nThe keys in the :param dimension of the resulting DimArray are just TKey, i.e., the Parameter wrapper is removed. Otherwise, the keys will be Union{Parameter{<:TKey},Extra}.\n\n\n\n\n\nDimensionalData.DimArray(\n    summary::FlexiSummary{TKey};\n    warn::Bool=true,\n    eltype_filter=Any,\n    parameters_only::Bool=true,\n) where {TKey}\n\nConvert a FlexiSummary into a DimArray with a :param dimension appended after the non-collapsed dimensions of the summary. For example:\n\nSummary produced via Dimensions of resulting DimArray\nmean(chn) (:param)\nmean(chn; dims=:iter) (:chain, :param)\nmean(chn; dims=:chain) (:iter, :param)\nsummarystats(chn) (:stat, :param)\ncollapse(chn, [mean, std]; dims=:iter) (:chain, :stat, :param)\n\nKeyword arguments\n\neltype_filter::Any: retain only parameters whose values subtype eltype_filter. For example, if eltype_filter=Float64, then integer-valued parameters are dropped.\nparameters_only::Bool=true: whether to include only parameters (not extras) in the resulting DimArray.\nwarn::Bool=true: whether to issue a warning if any keys are skipped due to their values not subtyping eltype_filter.\n\n\n\n\n\n","title":"DimensionalData.DimArray"},{"category":"method","location":"arrays/#Core.Array-Tuple{FlexiChains.FlexiSummary}","page":"Converting to/from arrays","text":"Base.Array(\n    chain::FlexiChain;\n    kwargs...\n)\n\nConvert a FlexiChain into a standard Array with dimensions (iter, chain, param). This is the same as the conversion to DimensionalData.DimArray, except that the dimension metadata is discarded.\n\nSee DimensionalData.DimArray(::FlexiChains.FlexiChain) for more details on the conversion process and available keyword arguments.\n\n\n\n\n\nBase.Array(\n    summary::FlexiSummary;\n    kwargs...\n)\n\nConvert a FlexiSummary into a standard Array. This is the same as the conversion to DimensionalData.DimArray, except that the dimension metadata is discarded.\n\nSee DimensionalData.DimArray(::FlexiChains.FlexiSummary) for details.\n\n\n\n\n\n","title":"Core.Array"},{"category":"section","location":"why/#Why-FlexiChains?","page":"Why FlexiChains?","text":"If you are a Turing user (or developer!), you may well be asking why you should consider using FlexiChains.jl.\n\nIn one sentence: FlexiChains is a more faithful representation of data. MCMCChains.jl places extremely strong restrictions on its data structure, which leads to an irrevocable loss of information.\n\nOf course, on its own this doesn't mean much (unless you are a software engineering purist!). So this page will demonstrate a few concrete scenarios where FlexiChains has a practical advantage.","title":"Why FlexiChains?"},{"category":"section","location":"why/#Documentation","page":"Why FlexiChains?","text":"This package is much better documented. In my opinion. (To be fair, I did put a lot of work into it, and if I spent the same amount of time on MCMCChains, I suppose its docs would also look better.)","title":"Documentation"},{"category":"section","location":"why/#Heterogeneous-parameter-types","page":"Why FlexiChains?","text":"using Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model function f()\n    x ~ Normal()\n    y ~ Poisson(3.0)\nend\n\nWhen sampling from this model, one should expect that the samples of x are stored as floats, whereas the samples of y are stored as integers, because that is what these distributions produce.\n\nUnder the hood, MCMCChains stores the values of all parameters in a single array, which means that all samples get converted into the same type.\n\nmchain = sample(Xoshiro(468), f(), MH(), 50; chain_type=MCMCChains.Chains);\nfchain = sample(Xoshiro(468), f(), MH(), 50; chain_type=FlexiChains.VNChain);\n\n(eltype(mchain[:y]), eltype(fchain[:y]))\n\nNow, Distributions.jl kindly allows you to call logpdf(::Poisson, x::Float64) and returns the correct value if isinteger(x). So, if your model is simple enough, you can still use functions such as returned and predict without running into errors, even with MCMCChains.\n\nHowever, if you attempt to use the value of x somewhere where it needs to be an integer, such as...\n\n@model function f2()\n    # A more realistic scenario is `n ~ Poisson(...)`.\n    # That errors with MCMCChains for a *different* reason;\n    # we'll come to that in a while.\n    n ~ DiscreteUniform(2, 2)\n    x ~ MvNormal(zeros(n), I)\n    return sum(x)\nend\n\nmchain = sample(Xoshiro(468), f2(), MH(), 50; chain_type=MCMCChains.Chains);\n\nyou will find that it errors, because n is stored as 2.0 in the chain:\n\ntry #hide\nreturned(f2(), mchain)\ncatch e; showerror(stdout, e); end # hide\n\nNow, you could work around this with zeros(Int(n)), but that's deeply unsatisfying, because n really should be an integer. Good news: FlexiChains will store it as an integer for you!\n\nfchain = sample(Xoshiro(468), f2(), MH(), 50; chain_type=FlexiChains.VNChain)\nreturned(f2(), fchain)","title":"Heterogeneous parameter types"},{"category":"section","location":"why/#Missing-data","page":"Why FlexiChains?","text":"As a variant on the above bug with proper type representation, consider the case where some data is missing:\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model function f()\n    x ~ Normal()\n    if x > 0\n        y ~ Normal(x)\n    end\nend\n\nmchain = sample(Xoshiro(468), f(), MH(), 50; chain_type=MCMCChains.Chains)\nfchain = sample(Xoshiro(468), f(), MH(), 50; chain_type=FlexiChains.VNChain)\n\nIn some samples, y will be present and not in others. Because MCMCChains forces all parameters to be in the same array, this means that the entire array must have an element type of Union{Missing, Float64}. With MCMCChains this gets propagated to all parameters, even those that are never missing, such as x.\n\n(eltype(mchain[:x]), eltype(fchain[:x]))\n\nOkay, maybe you don't ever have such weird models. It turns out though that you can still run into this. In Turing's MCMC sampling, the first step is not an actual MCMC step, but rather just the initial parameters (either sampled or provided by the user). Thus, there are no 'sampler statistics' for the first step, and these are stored as missing in MCMCChains. That means that all the parameters become Union{Missing, Float64}!","title":"Missing data"},{"category":"section","location":"why/#Variable-length-parameters","page":"Why FlexiChains?","text":"Above we used an example with DiscreteUniform(2, 2) to demonstrate what happens when Int-valued parameters get converted to Floats. This is of course a bit pointless since sampling from that always gives 2. Let's see now what happens when we have a truly variable-length parameter:\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model function varlen()\n    n ~ Poisson(3.5)\n    x ~ MvNormal(zeros(n), I)\n    y ~ Normal(sum(x))\nend\n\nmodel = varlen()\ncond_model = varlen() | (; y = 2.0)\n\nmchain = sample(Xoshiro(468), cond_model, MH(), 50; chain_type=MCMCChains.Chains)\nfchain = sample(Xoshiro(468), cond_model, MH(), 50; chain_type=FlexiChains.VNChain)\n\nSo far, so good; we can sample from everything just fine. The trouble comes when you want to use something like predict or returned which involves feeding the samples from the chain back into the model.\n\ntry #hide\npredict(model, mchain)\ncatch e; showerror(stdout, e); end # hide\n\nwarning: Warning\nThe above will probably fail, but it can actually run successfully, if you are lucky enough to get a sample where n is larger than or equal to that in all the other samples. If the built docs don't show an error, try running it in the REPL, and you'll find that it will almost always fail.\n\nThe reason why MCMCChains fails here is because it does two things:\n\nIt stores x as a series of elements x[1], x[2], and so on.\nWhen reconstructing the value of x for use in the model, it doesn't know how long x is supposed to be. It determines this by running the model once, and taking the value of x from that specific run of the model.\n\nThis of course ignores the fact that x can have different lengths.\n\nIn contrast, FlexiChains does two things:\n\nWhere possible, it will store x as a single parameter.\nAs a guard against situations where this isn't possible (e.g. if not all values of x are filled in), it also stores the structure of x as part of the chain, so that it can always reconstruct it correctly.\n\nThis means that regardless of what value n takes in the samples, predict and returned will always work correctly with FlexiChains.\n\npredict(model, fchain)","title":"Variable-length parameters"},{"category":"section","location":"why/#Arbitrary-types","page":"Why FlexiChains?","text":"Turing provides this very nice operator :=, which lets you store arbitrary values in the chain during an MCMC run.\n\nThe problem with MCMCChains is, as ever, you can only store things that are Real or some array thereof. Even a simple string will fail:\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model function hasstring()\n    x ~ Normal()\n    y := \"$x\"\nend\ntry #hide\nmchain = sample(Xoshiro(468), hasstring(), MH(), 50; chain_type=MCMCChains.Chains)\ncatch e; showerror(stdout, e); end # hide\n\nFlexiChains will let you store anything you like! This includes strings, but also more complex objects such as the output of a differential equation solver, or customised structs, as demonstrated in this Gaussian process tutorial.\n\nfchain = sample(Xoshiro(468), hasstring(), MH(), 50; chain_type=FlexiChains.VNChain)","title":"Arbitrary types"},{"category":"section","location":"why/#Reconstructing-parameters","page":"Why FlexiChains?","text":"Suppose you have some array-valued parameter.\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model lkj() = x ~ LKJCholesky(3, 2.0)\n\nmchain = sample(Xoshiro(468), lkj(), NUTS(), 50; chain_type=MCMCChains.Chains);\nfchain = sample(Xoshiro(468), lkj(), NUTS(), 50; chain_type=FlexiChains.VNChain);\n\nWith FlexiChains the Cholesky samples are kept together:\n\nfchain[@varname(x)][iter=1, chain=1]\n\nBecause MCMCChains stores all its data in a single array, it has to flatten this parameter, so good luck trying to reconstruct it.\n\n(mchain[Symbol(\"x.L[1, 1]\")][1, 1], mchain[Symbol(\"x.L[2, 1]\")][1, 1]) # ...","title":"Reconstructing parameters"},{"category":"section","location":"why/#why-varnames-as-keys","page":"Why FlexiChains?","text":"Did you notice in that last line we had to write something like Symbol(\"x.L[1, 1]\")? And since it's a Symbol, we have to get the name exactly right, we couldn't do (for example) x.L[1,1] without the space after the comma?\n\nThat's because MCMCChains uses AxisArrays.jl under the hood, which allows you to index into the chain using Symbols — but only Symbols. FlexiChains retains the original VarNames used by Turing, which is a far richer type and allows you to use keys that actually carry meaning, rather than just being strings that have to match exactly.\n\nfchain[@varname(x.L[1,1])][iter=1, chain=1] # No space!\n\nfchain[@varname(x.L[:, 1])][iter=1, chain=1] # Index into `x` any way you like\n\nIf you have x stored as a full vector, you can also do fancy things like indexing into x[end], which will give you the last element of x regardless of how long it is in that particular sample.\n\n@model function varlen_again()\n    n ~ Poisson(3.5)\n    x ~ MvNormal(zeros(n), I)\nend\n\nfchain = sample(Xoshiro(468), varlen_again(), MH(), 5; chain_type=FlexiChains.VNChain);\nfchain[@varname(x)]\n\nfchain[@varname(x[end])]","title":"VarNames as keys"},{"category":"section","location":"why/#Interoperability-with-the-rest-of-Turing","page":"Why FlexiChains?","text":"Suppose you have sampled a chain, and you want to use something from it as the starting point for a new chain, or a new optimisation, or something.\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n@model function twonorm()\n    x ~ Normal()\n    y ~ Normal()\n    return x + y\nend\n\nmchain = sample(Xoshiro(468), twonorm(), MH(), 50; chain_type=MCMCChains.Chains);\nfchain = sample(Xoshiro(468), twonorm(), MH(), 50; chain_type=FlexiChains.VNChain);\n\nLet's say you want to use the last sample of x as the starting point for another sampler.\n\nArray(mchain)[50, :, :]\n\nEven at this early point, it is already quite ugly: you have to know that the samples are stored in a 3D array, and that the first dimension corresponds to iterations, the second to parameters, and the third to chains. Secondly, it gives you a vector of parameters, and it's nontrivial to figure out from this how these map to the original variables in the model. You can use\n\nnames(MCMCChains.get_sections(mchain, :parameters))\n\nand for this trivial model it's very clear that the first one is x and the second is y. However, this generalises to complex samples very poorly (consider e.g. matrices and Cholesky samples again), and is very difficult to write in a programmatic way!\n\nIn contrast, with FlexiChains, you can just do\n\nvnt = FlexiChains.parameters_at(fchain, 50, 1)\n\nwhich directly gives you a VarNamedTuple that will 'just work' with the rest of Turing. For example, you can use it as the starting point for a new sampler:\n\ninit = InitFromParams(vnt)\nsample(twonorm(), NUTS(), 50; initial_params=init, chain_type=FlexiChains.VNChain)\n\nor use it to begin an optimisation (a bit pointless for our trivial model, but you get the idea!):\n\nmaximum_a_posteriori(twonorm(); initial_params=init)\n\nor pass it to a function like returned:\n\nreturned(twonorm(), vnt)","title":"Interoperability with the rest of Turing"},{"category":"section","location":"why/#why-perf","page":"Why FlexiChains?","text":"In the following model, y is a single parameter that is a vector of length N. That means that when you use functions like returned or predict on a chain, MCMCChains has to somehow reconstruct the vector y from its components which are all stored separately.\n\nusing Turing, FlexiChains, MCMCChains, Random\nsetprogress!(false) # hide\n\n@model function longvec(N)\n    m ~ Normal(0)\n    y ~ filldist(Normal(m), N)\nend\n\nIt turns out that if N is small, MCMCChains does just fine, and is in fact even faster than FlexiChains (because constructing a FlexiChain has more overhead).\n\nusing Chairmarks: @be\n\nfunction benchmark(N)\n    model = (longvec(N) | (y = rand(Xoshiro(468), Normal(2.0), N),))\n    mchain = sample(Xoshiro(468), model, NUTS(), 500; chain_type=MCMCChains.Chains);\n    fchain = sample(Xoshiro(468), model, NUTS(), 500; chain_type=FlexiChains.VNChain);\n    mt = @be predict(longvec(N), mchain)\n    ft = @be predict(longvec(N), fchain)\n    return (N=N, mcmcchains=median(mt).time, flexichains=median(ft).time)\nend\n\nbenchmark(2) # The results are in seconds\n\nBut MCMCChains scales really poorly with N.\n\nbenchmark(1000)","title":"Performance (on important things)"},{"category":"section","location":"why/#Avoiding-name-clashes","page":"Why FlexiChains?","text":"All of Turing.jl's samplers include some 'sampler statistics' in the output chain. These are pretty useful things like the step size, whether a transition was accepted, the log-probabilities, and so on.\n\nBut if you have a parameter that just happens to share a name with these, then MCMCChains will make it pretty hard for you to get one of them.\n\nusing Random, Turing, FlexiChains, MCMCChains\nsetprogress!(false) # hide\n\n# Oops! This will clash with the actual log prior.\n@model pr() = logprior ~ Normal()\n\nmchain = sample(Xoshiro(468), pr(), MH(), 50; chain_type=MCMCChains.Chains);\ncollect(keys(mchain))\n\nThere are two columns labelled :logprior. Of course, one is your parameter, the other is the actual log prior probability. It's a mystery which one is which, and which you get when you do mchain[:logprior]! You could avoid this if you knew exactly which keys the sampler returns, but in general this isn't documented anywhere. (It should be, of course.)\n\nFlexiChains avoids clashes by completely separating Parameter and Extra keys, meaning that you can use any name you like without worrying about a rogue sampler breaking your workflow.","title":"Avoiding name clashes"},{"category":"section","location":"why/#DimensionalData.jl-indexing","page":"Why FlexiChains?","text":"As you will have noticed, FlexiChains uses DimensionalData.jl to return information-rich matrices. That means that you can use all the selectors from DimensionalData.jl to extract exactly what you want. Don't want to use those? No problem; good old 1-based indices work fine too.","title":"DimensionalData.jl indexing"},{"category":"section","location":"why/#If-you're-a-Turing-developer...","page":"Why FlexiChains?","text":"I suppose I am somewhat qualified to comment on this... There are a number of places in TuringLang where the unfaithful data structure of MCMCChains leads to hacky workarounds. Most of them centre around the difficulty of reconstructing vectors x from its flattened components x[1], x[2], and so on.\n\nFor example, AbstractPPLDistributionsExt exists solely for this reason. (None of us are particularly happy about this: see this PR.) The methods defined here allow you to check whether a dictionary like Dict(@varname(x[1]) => 1.0, @varname(x[2]) => 2.0) can be reconstructed into a vector-valued parameter x, given that x is drawn from MvNormal(zeros(2), I). This is precisely because we can only obtain the former dictionary from MCMCChains, but when evaluating a model we need the latter.\n\nOn top of that, MCMCChains doesn't store the keys as VarNames: it stores them as Symbols. That means that any time we need to retrieve the original VarNames, we need to use a secret dictionary that is stored inside chain.info. This is automatically included when sampling using Turing, but it makes it somewhat frustrating to test MCMCChains-related functionality in isolation. In general, though, this is a very fragile solution and relies on Turing 'just happening' to do the right thing.","title":"If you're a Turing developer..."},{"category":"section","location":"plotting/#plotting","page":"Plotting overview","text":"FlexiChains contains some support for visualising chains with Plots.jl and Makie.jl.","title":"Plotting overview"},{"category":"section","location":"plotting/#Available-functions","page":"Plotting overview","text":"Here is a rough overview of the current status:\n\nType of plot Plots.jl Makie.jl\nTrace + mixed density (default) ✅ Plots.plot ✅ Makie.plot\nTrace plots ✅ FlexiChains.Plots.traceplot ✅ FlexiChains.Makie.traceplot\nDensity plots ✅ Plots.density ✅ Makie.density\nHistograms ✅ Plots.histogram ✅ Makie.hist and Makie.stephist\nMixed density plots ✅ FlexiChains.Plots.mixeddensity ✅ FlexiChains.Makie.mixeddensity\nRunning mean plots ✅ FlexiChains.Plots.meanplot ✅ FlexiChains.Makie.meanplot\nAutocorrelation plots ✅ FlexiChains.Plots.autocorplot ✅ FlexiChains.Makie.autocorplot\nRank plots ✅ FlexiChains.Plots.rankplot ✅ FlexiChains.Makie.rankplot\nCorner plots ✅ StatsPlots.cornerplot ✅ PairPlots.pairplot\nViolin plots ✅ StatsPlots.violin 🐌\nEnergy plots 🐌 🐌\nForest plots 🐌 ✅ FlexiChains.Makie.forestplot\nRidgeline plots 🐌 ✅ FlexiChains.Makie.ridgeline\nPredictive check plots 🐌 🐌\n\nAll of the above functions have 'mutating' versions with a ! suffix.\n\nNotice that FlexiChains provides separate functions for Plots.jl and Makie.jl backends, which are namespaced within the FlexiChains.Plots and FlexiChains.Makie modules, respectively. This is necessary for disambiguation, much like how Plots.plot() and Makie.plot() are different functions.","title":"Available functions"},{"category":"section","location":"plotting/#Compositionality","page":"Plotting overview","text":"In general, the plotting interfaces in FlexiChains try to stay as close as possible to the way the original plotting libraries work. That means that you should be able to construct your own plots, insert a FlexiChains plot into a larger figure, and so on, using the interface provided by the original plotting library.\n\nFor example, with Plots.jl you can do things like this:\n\nchn = ...\n# These functions provided / extended by FlexiChains\np1 = FlexiChains.Plots.traceplot(chn, param1)\np2 = Plots.density(chn, param2)\n# This is some completely external Plots.jl plot\np3 = Plots.plot(randn(10, 10))\n# Compose them with Plots.jl\nplot([p1, p2, p3], layout=(3, 1))\n\nand with Makie, a workflow like this should also work:\n\nchn = ...\n# Set up a Makie figure\nf = Makie.Figure()\n# These functions provided / extended by FlexiChains\nFlexiChains.Makie.traceplot!(f[1, 1], chn, param1)\nMakie.density!(f[1, 2], chn, param2)\n# This is some completely external Makie.jl plot\nMakie.plot!(f[1, 3], randn(10, 10))\n# Show the figure\nf\n\nHowever, I'm not the most experienced user of either Plots.jl or Makie.jl, so some inconsistencies will no doubt exist. This may especially be so with Makie.jl because it is quite a bit more difficult to write extensions for Makie than it is for Plots (which has a very powerful recipe system).\n\nPlease do feel free to open issues or pull requests to improve the plotting functionality!","title":"Compositionality"},{"category":"section","location":"samples/#Accessing-samples","page":"Accessing samples","text":"FlexiChains stores data in a dict-of-array format (a column-oriented form, similar to dataframes). This means that it is very easy to access all the data for a given variable, but accessing all the data for a given sample (i.e., iteration / chain number) is a bit more involved.\n\nConsider the following example:\n\nusing FlexiChains, Turing\n\n@model function f()\n    x ~ Normal()\n    y ~ Uniform(1, 2)\nend\n\nchn = sample(f(), Prior(), 10; chain_type=VNChain, progress=false)\n\nHere, we have two variables x and y. FlexiChains makes it very easy to access all the samples for x, or all the samples for y, and so on:\n\nchn[@varname(x)]\n\nHowever, suppose you want to access the samples for a given iteration and chain number. For example, you might want to know what happened at the fifth iteration.\n\nOne way is to subset a chain. The issue is that that gives you just another chain back:\n\nsubsetted = chn[iter=5, chain=1]\n\nand to access the data you will still need to index into it, which is rather awkward:\n\n(x = only(subsetted[@varname(x)]), y = only(subsetted[@varname(y)]))\n\nMore generally speaking, the operation above can be viewed as a transformation from column-oriented data to row-oriented data, or from a dict-of-arrays to an array-of-dicts. FlexiChains provides a few convenient ways for you to do this transformation.","title":"Accessing samples"},{"category":"section","location":"samples/#values_at","page":"Accessing samples","text":"The function values_at is at the core of this transformation. Given iteration and chain indices (as keyword arguments), it returns some container that holds all the values for a given iteration.\n\nvs = FlexiChains.values_at(chn; iter=5, chain=1)\n\nIn the case of a chain sampled from Turing, the returned container is DynamicPPL.ParamsWithStats, which separately stores the parameters and the stats as a VarNamedTuple and NamedTuple respectively. This is a high-fidelity representation of the data, and is exactly what you get when sampling with Turing.jl (for example, if you call sample(...; chain_type=Any), you will get an array of ParamsWithStats objects).\n\ninfo: Info\nThis is accomplished by storing a skeletal VarNamedTuple for each sample in the chain; if you are interested, see the DynamicPPL docs for more info.\n\nThe main benefit of this is that you can feed this right back into Turing's API. For example, to initialise MCMC sampling from the fifth sample, you can write:\n\npws = FlexiChains.values_at(chn; iter=5, chain=1)\nsample(f(), NUTS(), 10; initial_params=InitFromParams(pws), progress=false);\n\nSometimes, though, one might want different output formats. There is some support for converting to NamedTuple or AbstractDict, by passing an optional type argument.\n\nwarning: Warning\nNote that conversion to NamedTuple is lossy if you have VarNames that contain indexing or field access syntax (e.g., x[1] or x.a).\n\nFlexiChains.values_at(chn, NamedTuple; iter=5, chain=1)\n\nFlexiChains.values_at(chn, Dict; iter=5, chain=1)","title":"values_at"},{"category":"section","location":"samples/#Parameters-only","page":"Accessing samples","text":"Often the stats are not of very much interest, and you just want the parameters. In that case, you can use parameters_at:\n\nFor Turing-sampled chains, this returns a VarNamedTuple, which is just the same as the params field of the ParamsWithStats object.\n\nFlexiChains.parameters_at(chn; iter=5, chain=1)","title":"Parameters only"},{"category":"section","location":"samples/#Arrays-of-samples","page":"Accessing samples","text":"To get more than one sample, you can pass arrays of indices.\n\nFlexiChains.parameters_at(chn; iter=[5, 6], chain=1)\n\nAll of DimensionalData's selectors also work:\n\nFlexiChains.parameters_at(chn; iter=Not(1..8), chain=1)\n\nThis also means that you can convert a FlexiChain back into an Array of ParamsWithStats objects by passing : for both the iteration and chain indices, which is essentially the inverse of what sample does (it bundles the array into a FlexiChain). In fact, : is the default for both of these keyword arguments, so you can just write:\n\nFlexiChains.values_at(chn)","title":"Arrays of samples"},{"category":"section","location":"samples/#Drawing-random-samples","page":"Accessing samples","text":"To get a random sample from the chain, you can use rand:\n\nrand(chn)\n\nThis method follows Julia's conventions closely, so you can have an optional rng first argument, and you can also specify the number of samples to draw:\n\nusing Random: Xoshiro\nrand(Xoshiro(468), chn, 2, 2)\n\nIf you only want parameters, pass the parameters_only=true keyword argument:\n\nrand(chn, parameters_only=true)","title":"Drawing random samples"},{"category":"section","location":"samples/#Using-VarNamedTuples","page":"Accessing samples","text":"All of the above functions, when run with Turing-sampled chains, return VarNamedTuples or containers thereof. To access the values, you need to index with a VarName:\n\nvnt = rand(chn, parameters_only=true)\nvnt[@varname(x)]\n\nPlease see the Turing docs for more details on working with VarNamedTuples.","title":"Using VarNamedTuples"},{"category":"section","location":"samples/#Docstrings","page":"Accessing samples","text":"","title":"Docstrings"},{"category":"function","location":"samples/#FlexiChains.values_at","page":"Accessing samples","text":"FlexiChains.values_at(chn::FlexiChain; iter=:, chain=:)\nFlexiChains.values_at(chn::FlexiChain, ::Type{Tout}; iter=:, chain=:)\n\nExtract all values from the chain corresponding to a particular set of MCMC iterations(s).\n\nThe iter and chain keyword arguments can be anything used to index into the respective dimensions of a FlexiChain, such as an integer, a vector of integers, a range, or a DimensionalData.jl selector. Both default to : (i.e. all iterations / all chains).\n\nIn particular, you can convert the entire chain into a DimMatrix of the desired output type by calling this function with no keyword arguments.\n\nTo get only the parameter keys, use FlexiChains.parameters_at.\n\nThe output type can be specified with an (optional) positional argument. Possible options are:\n\nunspecified: uses the structure stored in the chain to determine the output type. Specifically, for chains sampled with Turing.jl, the stored structure will be VarNamedTuple, and consequently the output of parameters_at will be a DynamicPPL.ParamsWithStats per iteration (which stores the parameters as a VarNamedTuple, and the stats/extras as a NamedTuple).\nFor chains that do not have a stored structure, the default output will be an OrderedDict.\nTout <: AbstractDict: returns a dictionary mapping ParameterOrExtra{TKey} to their values. This is the most faithful representation of the data in the chain.\nTout = NamedTuple, or Tout = ComponentArray: attempts to convert every key name to a Symbol, which is used as the field name in the output NamedTuple or ComponentArray.\n\nwarning: Using `NamedTuple` or `ComponentArray`\nThis will throw an error if any key cannot be converted to a Symbol, or if there are duplicate key names after conversion. If you have parameter names that convert to the same Symbol, you can either use OrderedDict, subset the chain before calling this function, or rename your parameters. Furthermore, please be aware that this is a lossy conversion as it does not retain information about whether a key is a parameter or an extra.\n\nFor order-sensitive output types, such as OrderedDict, the keys are returned in the same order as they are stored in the FlexiChain. This also corresponds to the order returned by keys(chn).\n\n\n\n\n\n","title":"FlexiChains.values_at"},{"category":"function","location":"samples/#FlexiChains.parameters_at","page":"Accessing samples","text":"FlexiChains.parameters_at(chn::FlexiChain; iter=:, chain=:)\nFlexiChains.parameters_at(chn::FlexiChain, ::Type{Tout}; iter=:, chain=:)\n\nExtract all parameter values from the chain corresponding to a particular set of MCMC iteration(s), discarding non-parameter (i.e. Extra) keys.\n\nThe iter and chain keyword arguments can be anything used to index into the respective dimensions of a FlexiChain, such as an integer, a vector of integers, a range, or a DimensionalData.jl selector. Both default to : (i.e. all iterations / all chains).\n\nIn particular, you can convert the entire chain into a DimMatrix of the desired output type by calling this function with no keyword arguments.\n\nTo get all keys (not just parameters), use FlexiChains.values_at.\n\nThe output type for each iteration can be specified with the first (optional) positional argument. Possible options are:\n\nunspecified: uses the structure stored in the chain to determine the output type. Specifically, for chains sampled with Turing.jl, the stored structure will be VarNamedTuple, and consequently the output of parameters_at will be a VarNamedTuple per iteration.\nFor chains that do not have a stored structure, the default output will be an OrderedDict.\nTout <: AbstractDict: returns a dictionary mapping TKey to their values.\nTout = NamedTuple or Tout <: ComponentArray: attempts to convert every parameter name  to a Symbol, which is used as the field name in the output NamedTuple or  ComponentArray.\n\nwarning: Using `NamedTuple` or `ComponentArray`\nThis will throw an error if any key cannot be converted to a Symbol, or if there are duplicate key names after conversion. If you have parameter names that convert to the same Symbol, you can either use OrderedDict, subset the chain before calling this function, or rename your parameters. Furthermore, please be aware that this is a lossy conversion as it does not retain information about whether a key is a parameter or an extra.\n\nFor order-sensitive output types, such as OrderedDict, the parameters are returned in the same order as they are stored in the FlexiChain. This also corresponds to the order returned by FlexiChains.parameters(chn).\n\n\n\n\n\n","title":"FlexiChains.parameters_at"},{"category":"function","location":"samples/#Base.rand","page":"Accessing samples","text":"Base.rand(rng::Random.AbstractRNG, chn::FlexiChain, dims::Int...; parameters_only=false)\n\nSample uniformly from a FlexiChain with replacement.\n\nrand([rng,] chn) returns a single sample, whereas rand([rng,] chn, dims...) returns an Array of samples with dimensions dims.... The parameters_only keyword specifies whether the returned samples include only parameters, or both parameters and extras.\n\nIn general, the return type of rand is the same as the return type of FlexiChains.parameters_at or FlexiChains.values_at (but wrapped in an Array if dims is not empty). This means that the return type can depend on the 'structure' stored in the  FlexiChain.\n\nFor example, if chn::FlexiChain{VarName} was constructed using Turing.jl, rand(chn) will return a DynamicPPL.ParamsWithStats, and rand(chn, parameters_only=true) will return a DynamicPPL.VarNamedTuple.\n\n\n\n\n\n","title":"Base.rand"},{"category":"section","location":"#FlexiChains.jl","page":"FlexiChains.jl","text":"Rich, type-preserving storage for Markov chains.\n\nFlexiChains.jl provides a rich data structure for storing and analysing Markov chain Monte Carlo (MCMC) output. Unlike traditional approaches that flatten everything to numeric arrays, FlexiChains preserves the original shapes of your parameters: all Julia types, most notably arrays, are stored without modification.\n\nKey features:\n\nStores any Julia type. Scalars work, but so do vectors, matrices, and everything else.\nFirst-class Turing.jl integration. Simply add chain_type=VNChain to your sample call. (Update: As of Turing v0.45, FlexiChains is the default!)\nPowerful and expressive indexing. You can access parameters in a variety of ways, using the full power of DimensionalData.jl selectors.\nBuilt-in plotting. Both Plots.jl and Makie.jl backends are supported (work in progress!).\nExtensive integrations with other Julia packages. FlexiChains has a growing collection of package extensions to ensure maximum interoperability with the wider Julia ecosystem.\n\nRead on to find out more!","title":"FlexiChains.jl"}]
}
